openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: "This API provides a standardized interface for Automotive Retail operations,\
    \ built upon a formal Retail Ontology. It enables seamless integration between\
    \ Master Catalog systems, Inventory Management, and Financial Invoicing workflows.\
    \ \n\n**Key Capabilities:**\n* **Catalog Management:** Unified definitions for\
    \ parts, assemblies, and BOMs.\n* **Inventory Orchestration:** Real-time visibility\
    \ into warehouse and dealership stock.\n* **Financial Workflows:** Automated invoicing\
    \ and batch processing for high-volume retail transactions.\n\nDesigned for high-reliability\
    \ CI/CD environments and asynchronous batch processing."
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
paths:
  /financial-snapshots:
    get:
      tags:
      - FinancialSnapshot
      summary: Retrieve a list of FinancialSnapshot entities.
      operationId: listFinancialSnapshot
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of FinancialSnapshot entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FinancialSnapshot"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - FinancialSnapshot
      summary: Create a new FinancialSnapshot entity.
      operationId: createFinancialSnapshot
      requestBody:
        description: FinancialSnapshot data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotCreate"
        required: true
      responses:
        "201":
          description: FinancialSnapshot created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /financial-snapshots/PR-1234567
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /financial-snapshots/{financialSnapshotKey}:
    get:
      tags:
      - FinancialSnapshot
      summary: Retrieve a specific FinancialSnapshot entity.
      operationId: getFinancialSnapshotById
      responses:
        "200":
          description: The requested FinancialSnapshot entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshot"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - FinancialSnapshot
      summary: Replace a FinancialSnapshot entity.
      operationId: replaceFinancialSnapshot
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshot"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - FinancialSnapshot
      summary: Delete a FinancialSnapshot entity
      description: Marks the FinancialSnapshot entity as inactive or removes it entirely.
      operationId: deleteFinancialSnapshotEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - FinancialSnapshot
      summary: Partially update a FinancialSnapshot entity.
      operationId: updateFinancialSnapshot
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the specific FinancialSnapshot.
      required: true
      schema:
        type: string
        example: PR-1234567
  /financial-snapshots/{financialSnapshotKey}/financial-snapshot-line-items:
    get:
      tags:
      - FinancialSnapshotLineItem
      summary: Retrieve a list of FinancialSnapshotLineItem entities scoped by financialSnapshotKey.
      operationId: listFinancialSnapshotLineItemByFinancialSnapshotKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of FinancialSnapshotLineItem entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FinancialSnapshotLineItem"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - FinancialSnapshotLineItem
      summary: Create a new FinancialSnapshotLineItem entity.
      operationId: createFinancialSnapshotLineItem
      requestBody:
        description: FinancialSnapshotLineItem data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotLineItemCreate"
        required: true
      responses:
        "201":
          description: FinancialSnapshotLineItem created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /financial-snapshots/{financialSnapshotKey}/financial-snapshot-line-items/{financialSnapshotLineItemKey}:
    get:
      tags:
      - FinancialSnapshotLineItem
      summary: Retrieve a specific FinancialSnapshotLineItem entity.
      operationId: getFinancialSnapshotLineItemById
      responses:
        "200":
          description: The requested FinancialSnapshotLineItem entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotLineItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - FinancialSnapshotLineItem
      summary: Replace a FinancialSnapshotLineItem entity.
      operationId: replaceFinancialSnapshotLineItem
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotLineItem"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - FinancialSnapshotLineItem
      summary: Delete a FinancialSnapshotLineItem entity
      description: Marks the FinancialSnapshotLineItem entity as inactive or removes
        it entirely.
      operationId: deleteFinancialSnapshotLineItemEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - FinancialSnapshotLineItem
      summary: Partially update a FinancialSnapshotLineItem entity.
      operationId: updateFinancialSnapshotLineItem
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotLineItemUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: financialSnapshotLineItemKey
      in: path
      description: The unique identifier for the specific FinancialSnapshotLineItem.
      required: true
      schema:
        type: string
        example: PR-1234567
  /financial-snapshots/{financialSnapshotKey}/moneys:
    get:
      tags:
      - Money
      summary: Retrieve a list of Money entities scoped by financialSnapshotKey.
      operationId: listMoneyByFinancialSnapshotKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Money entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Money"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /financial-snapshots/{financialSnapshotKey}/moneys/{moneyKey}:
    get:
      tags:
      - Money
      summary: Retrieve a specific Money entity.
      operationId: getMoneyById
      responses:
        "200":
          description: The requested Money entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - Money
      summary: Replace a Money entity.
      operationId: replaceMoney
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Money"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: moneyKey
      in: path
      description: The unique identifier for the specific Money.
      required: true
      schema:
        type: string
        example: PR-1234567
  /financial-snapshots/{financialSnapshotKey}/financial-snapshot-criteria:
    get:
      tags:
      - FinancialSnapshotCriteria
      summary: Retrieve a list of FinancialSnapshotCriteria entities scoped by financialSnapshotKey.
      operationId: listFinancialSnapshotCriteriaByFinancialSnapshotKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of FinancialSnapshotCriteria entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FinancialSnapshotCriteria"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - FinancialSnapshotCriteria
      summary: Create a new FinancialSnapshotCriteria entity.
      operationId: createFinancialSnapshotCriteria
      requestBody:
        description: FinancialSnapshotCriteria data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotCriteriaCreate"
        required: true
      responses:
        "201":
          description: FinancialSnapshotCriteria created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /financial-snapshots/{financialSnapshotKey}/financial-snapshot-criteria/{financialSnapshotCriteriaKey}:
    get:
      tags:
      - FinancialSnapshotCriteria
      summary: Retrieve a specific FinancialSnapshotCriteria entity.
      operationId: getFinancialSnapshotCriteriaById
      responses:
        "200":
          description: The requested FinancialSnapshotCriteria entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotCriteria"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - FinancialSnapshotCriteria
      summary: Replace a FinancialSnapshotCriteria entity.
      operationId: replaceFinancialSnapshotCriteria
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotCriteria"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - FinancialSnapshotCriteria
      summary: Delete a FinancialSnapshotCriteria entity
      description: Marks the FinancialSnapshotCriteria entity as inactive or removes
        it entirely.
      operationId: deleteFinancialSnapshotCriteriaEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - FinancialSnapshotCriteria
      summary: Partially update a FinancialSnapshotCriteria entity.
      operationId: updateFinancialSnapshotCriteria
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/FinancialSnapshotCriteriaUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/FinancialSnapshotCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: financialSnapshotKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: financialSnapshotCriteriaKey
      in: path
      description: The unique identifier for the specific FinancialSnapshotCriteria.
      required: true
      schema:
        type: string
        example: PR-1234567
components:
  schemas: {}
