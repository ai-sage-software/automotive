---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /cash-flow-projections:
    get:
      tags:
      - "CashFlowProjection"
      summary: "Retrieve a list of all CashFlowProjection records."
      operationId: "listCashFlowProjections"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CashFlowProjection records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CashFlowProjection"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CashFlowProjection"
      summary: "Create a new CashFlowProjection record."
      operationId: "createCashFlowProjection"
      requestBody:
        description: "CashFlowProjection data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCreate"
        required: true
      responses:
        "201":
          description: "CashFlowProjection created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/cash-flow-projections/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /cash-flow-projections/{cashFlowProjectionKey}:
    get:
      tags:
      - "CashFlowProjection"
      summary: "Retrieve a specific CashFlowProjection record."
      operationId: "getCashFlowProjectionById"
      responses:
        "200":
          description: "The requested CashFlowProjection record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CashFlowProjection"
      summary: "Replace a CashFlowProjection record."
      operationId: "replaceCashFlowProjection"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjection"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CashFlowProjection"
      summary: "Delete a CashFlowProjection Record"
      description: "Marks the CashFlowProjection record as inactive or removes it\
        \ entirely."
      operationId: "deleteCashFlowProjectionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CashFlowProjection"
      summary: "Partially update a CashFlowProjection record."
      operationId: "updateCashFlowProjection"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the specific CashFlowProjection."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/moneys:
    get:
      tags:
      - "Money"
      summary: "Retrieve a list of Money records scoped by cashFlowProjectionKey."
      operationId: "listMoneysByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Money records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Money"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/moneys/{moneyKey}:
    get:
      tags:
      - "Money"
      summary: "Retrieve a specific Money record."
      operationId: "getMoneyById"
      responses:
        "200":
          description: "The requested Money record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Money"
      summary: "Replace a Money record."
      operationId: "replaceMoney"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Money"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "moneyKey"
      in: "path"
      description: "The unique identifier for the specific Money."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios:
    get:
      tags:
      - "CashFlowScenario"
      summary: "Retrieve a list of CashFlowScenario records scoped by cashFlowProjectionKey."
      operationId: "listCashFlowScenariosByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CashFlowScenario records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CashFlowScenario"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CashFlowScenario"
      summary: "Create a new CashFlowScenario record."
      operationId: "createCashFlowScenario"
      requestBody:
        description: "CashFlowScenario data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowScenarioCreate"
        required: true
      responses:
        "201":
          description: "CashFlowScenario created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios/{cashFlowScenarioKey}:
    get:
      tags:
      - "CashFlowScenario"
      summary: "Retrieve a specific CashFlowScenario record."
      operationId: "getCashFlowScenarioById"
      responses:
        "200":
          description: "The requested CashFlowScenario record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CashFlowScenario"
      summary: "Replace a CashFlowScenario record."
      operationId: "replaceCashFlowScenario"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowScenario"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CashFlowScenario"
      summary: "Delete a CashFlowScenario Record"
      description: "Marks the CashFlowScenario record as inactive or removes it entirely."
      operationId: "deleteCashFlowScenarioRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CashFlowScenario"
      summary: "Partially update a CashFlowScenario record."
      operationId: "updateCashFlowScenario"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowScenarioUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "cashFlowScenarioKey"
      in: "path"
      description: "The unique identifier for the specific CashFlowScenario."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by cashFlowProjectionKey."
      operationId: "listIdentifiersByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or removes it entirely."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/accounting-periods:
    get:
      tags:
      - "AccountingPeriod"
      summary: "Retrieve a list of AccountingPeriod records scoped by cashFlowProjectionKey."
      operationId: "listAccountingPeriodsByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of AccountingPeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/AccountingPeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AccountingPeriod"
      summary: "Create a new AccountingPeriod record."
      operationId: "createAccountingPeriod"
      requestBody:
        description: "AccountingPeriod data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodCreate"
        required: true
      responses:
        "201":
          description: "AccountingPeriod created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/accounting-periods/{accountingPeriodKey}:
    get:
      tags:
      - "AccountingPeriod"
      summary: "Retrieve a specific AccountingPeriod record."
      operationId: "getAccountingPeriodById"
      responses:
        "200":
          description: "The requested AccountingPeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AccountingPeriod"
      summary: "Replace a AccountingPeriod record."
      operationId: "replaceAccountingPeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AccountingPeriod"
      summary: "Delete a AccountingPeriod Record"
      description: "Marks the AccountingPeriod record as inactive or removes it entirely."
      operationId: "deleteAccountingPeriodRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AccountingPeriod"
      summary: "Partially update a AccountingPeriod record."
      operationId: "updateAccountingPeriod"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "accountingPeriodKey"
      in: "path"
      description: "The unique identifier for the specific AccountingPeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items:
    get:
      tags:
      - "CashFlowLineItem"
      summary: "Retrieve a list of CashFlowLineItem records scoped by cashFlowProjectionKey."
      operationId: "listCashFlowLineItemsByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CashFlowLineItem records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CashFlowLineItem"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CashFlowLineItem"
      summary: "Create a new CashFlowLineItem record."
      operationId: "createCashFlowLineItem"
      requestBody:
        description: "CashFlowLineItem data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItemCreate"
        required: true
      responses:
        "201":
          description: "CashFlowLineItem created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items/{cashFlowLineItemKey}:
    get:
      tags:
      - "CashFlowLineItem"
      summary: "Retrieve a specific CashFlowLineItem record."
      operationId: "getCashFlowLineItemById"
      responses:
        "200":
          description: "The requested CashFlowLineItem record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CashFlowLineItem"
      summary: "Replace a CashFlowLineItem record."
      operationId: "replaceCashFlowLineItem"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItem"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CashFlowLineItem"
      summary: "Delete a CashFlowLineItem Record"
      description: "Marks the CashFlowLineItem record as inactive or removes it entirely."
      operationId: "deleteCashFlowLineItemRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CashFlowLineItem"
      summary: "Partially update a CashFlowLineItem record."
      operationId: "updateCashFlowLineItem"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItemUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "cashFlowLineItemKey"
      in: "path"
      description: "The unique identifier for the specific CashFlowLineItem."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criterias:
    get:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Retrieve a list of CashFlowProjectionCriteria records scoped by cashFlowProjectionKey."
      operationId: "listCashFlowProjectionCriteriasByCashFlowProjectionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CashFlowProjectionCriteria records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Create a new CashFlowProjectionCriteria record."
      operationId: "createCashFlowProjectionCriteria"
      requestBody:
        description: "CashFlowProjectionCriteria data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteriaCreate"
        required: true
      responses:
        "201":
          description: "CashFlowProjectionCriteria created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criterias/{cashFlowProjectionCriteriaKey}:
    get:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Retrieve a specific CashFlowProjectionCriteria record."
      operationId: "getCashFlowProjectionCriteriaById"
      responses:
        "200":
          description: "The requested CashFlowProjectionCriteria record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Replace a CashFlowProjectionCriteria record."
      operationId: "replaceCashFlowProjectionCriteria"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteria"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Delete a CashFlowProjectionCriteria Record"
      description: "Marks the CashFlowProjectionCriteria record as inactive or removes\
        \ it entirely."
      operationId: "deleteCashFlowProjectionCriteriaRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CashFlowProjectionCriteria"
      summary: "Partially update a CashFlowProjectionCriteria record."
      operationId: "updateCashFlowProjectionCriteria"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteriaUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "cashFlowProjectionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "cashFlowProjectionCriteriaKey"
      in: "path"
      description: "The unique identifier for the specific CashFlowProjectionCriteria."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas: {}
