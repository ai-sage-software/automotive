---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /journal-entry-transactions:
    get:
      tags:
      - "JournalEntryTransaction"
      summary: "Retrieve a list of all JournalEntryTransaction ..."
      operationId: "listJournalEntryTransactions"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of JournalEntryTransaction records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/JournalEntryTransaction"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "JournalEntryTransaction"
      summary: "Create a new JournalEntryTransaction record."
      operationId: "createJournalEntryTransaction"
      requestBody:
        description: "JournalEntryTransaction data to create the new ..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransactionCreate"
        required: true
      responses:
        "201":
          description: "JournalEntryTransaction created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/journal-entry-transactions/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}:
    get:
      tags:
      - "JournalEntryTransaction"
      summary: "Retrieve a specific JournalEntryTransaction rec..."
      operationId: "getJournalEntryTransactionById"
      responses:
        "200":
          description: "The requested JournalEntryTransaction record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "JournalEntryTransaction"
      summary: "Replace a JournalEntryTransaction record."
      operationId: "replaceJournalEntryTransaction"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransaction"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "JournalEntryTransaction"
      summary: "Delete a JournalEntryTransaction Record"
      description: "Marks the JournalEntryTransaction record as ina..."
      operationId: "deleteJournalEntryTransactionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "JournalEntryTransaction"
      summary: "Partially update a JournalEntryTransaction record."
      operationId: "updateJournalEntryTransaction"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransactionUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the specific JournalEntryTransaction."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/moneys:
    get:
      tags:
      - "Money"
      summary: "Retrieve a list of Money records scoped by jour..."
      operationId: "listMoneysByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Money records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Money"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/moneys/{moneyKey}:
    get:
      tags:
      - "Money"
      summary: "Retrieve a specific Money record."
      operationId: "getMoneyById"
      responses:
        "200":
          description: "The requested Money record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Money"
      summary: "Replace a Money record."
      operationId: "replaceMoney"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Money"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "moneyKey"
      in: "path"
      description: "The unique identifier for the specific Money."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/general-ledger-references:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a list of GeneralLedgerReference recor..."
      operationId: "listGeneralLedgerReferencesByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of GeneralLedgerReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/GeneralLedgerReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "GeneralLedgerReference"
      summary: "Create a new GeneralLedgerReference record."
      operationId: "createGeneralLedgerReference"
      requestBody:
        description: "GeneralLedgerReference data to create the new r..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReferenceCreate"
        required: true
      responses:
        "201":
          description: "GeneralLedgerReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/general-ledger-references/{generalLedgerReferenceKey}:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a specific GeneralLedgerReference record."
      operationId: "getGeneralLedgerReferenceById"
      responses:
        "200":
          description: "The requested GeneralLedgerReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "GeneralLedgerReference"
      summary: "Replace a GeneralLedgerReference record."
      operationId: "replaceGeneralLedgerReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "GeneralLedgerReference"
      summary: "Delete a GeneralLedgerReference Record"
      description: "Marks the GeneralLedgerReference record as inac..."
      operationId: "deleteGeneralLedgerReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "GeneralLedgerReference"
      summary: "Partially update a GeneralLedgerReference record."
      operationId: "updateGeneralLedgerReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "generalLedgerReferenceKey"
      in: "path"
      description: "The unique identifier for the specific GeneralLedgerReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by..."
      operationId: "listIdentifiersByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or remo..."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/effective-periods:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a list of EffectivePeriod records scop..."
      operationId: "listEffectivePeriodsByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of EffectivePeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/EffectivePeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a specific EffectivePeriod record."
      operationId: "getEffectivePeriodById"
      responses:
        "200":
          description: "The requested EffectivePeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "EffectivePeriod"
      summary: "Replace a EffectivePeriod record."
      operationId: "replaceEffectivePeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "effectivePeriodKey"
      in: "path"
      description: "The unique identifier for the specific EffectivePeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/control-account-references:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a list of ControlAccountReference reco..."
      operationId: "listControlAccountReferencesByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ControlAccountReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ControlAccountReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ControlAccountReference"
      summary: "Create a new ControlAccountReference record."
      operationId: "createControlAccountReference"
      requestBody:
        description: "ControlAccountReference data to create the new ..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceCreate"
        required: true
      responses:
        "201":
          description: "ControlAccountReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/control-account-references/{controlAccountReferenceKey}:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a specific ControlAccountReference rec..."
      operationId: "getControlAccountReferenceById"
      responses:
        "200":
          description: "The requested ControlAccountReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ControlAccountReference"
      summary: "Replace a ControlAccountReference record."
      operationId: "replaceControlAccountReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ControlAccountReference"
      summary: "Delete a ControlAccountReference Record"
      description: "Marks the ControlAccountReference record as ina..."
      operationId: "deleteControlAccountReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ControlAccountReference"
      summary: "Partially update a ControlAccountReference record."
      operationId: "updateControlAccountReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "controlAccountReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ControlAccountReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/party-identifiers:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a list of PartyIdentifier records scop..."
      operationId: "listPartyIdentifiersByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartyIdentifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartyIdentifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartyIdentifier"
      summary: "Create a new PartyIdentifier record."
      operationId: "createPartyIdentifier"
      requestBody:
        description: "PartyIdentifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifierCreate"
        required: true
      responses:
        "201":
          description: "PartyIdentifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/party-identifiers/{partyIdentifierKey}:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a specific PartyIdentifier record."
      operationId: "getPartyIdentifierById"
      responses:
        "200":
          description: "The requested PartyIdentifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartyIdentifier"
      summary: "Replace a PartyIdentifier record."
      operationId: "replacePartyIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartyIdentifier"
      summary: "Delete a PartyIdentifier Record"
      description: "Marks the PartyIdentifier record as inactive or..."
      operationId: "deletePartyIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartyIdentifier"
      summary: "Partially update a PartyIdentifier record."
      operationId: "updatePartyIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partyIdentifierKey"
      in: "path"
      description: "The unique identifier for the specific PartyIdentifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/journal-entries:
    get:
      tags:
      - "JournalEntry"
      summary: "Retrieve a list of JournalEntry records scoped ..."
      operationId: "listJournalEntrysByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of JournalEntry records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/JournalEntry"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "JournalEntry"
      summary: "Create a new JournalEntry record."
      operationId: "createJournalEntry"
      requestBody:
        description: "JournalEntry data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryCreate"
        required: true
      responses:
        "201":
          description: "JournalEntry created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/journal-entries/{journalEntryKey}:
    get:
      tags:
      - "JournalEntry"
      summary: "Retrieve a specific JournalEntry record."
      operationId: "getJournalEntryById"
      responses:
        "200":
          description: "The requested JournalEntry record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "JournalEntry"
      summary: "Replace a JournalEntry record."
      operationId: "replaceJournalEntry"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntry"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "JournalEntry"
      summary: "Delete a JournalEntry Record"
      description: "Marks the JournalEntry record as inactive or re..."
      operationId: "deleteJournalEntryRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "JournalEntry"
      summary: "Partially update a JournalEntry record."
      operationId: "updateJournalEntry"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "journalEntryKey"
      in: "path"
      description: "The unique identifier for the specific JournalEntry."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /journal-entry-transactions/{journalEntryTransactionKey}/subsidiary-ledger-references:
    get:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Retrieve a list of SubsidiaryLedgerReference re..."
      operationId: "listSubsidiaryLedgerReferencesByJournalEntryTransactionKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of SubsidiaryLedgerReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/SubsidiaryLedgerReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Create a new SubsidiaryLedgerReference record."
      operationId: "createSubsidiaryLedgerReference"
      requestBody:
        description: "SubsidiaryLedgerReference data to create the ne..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReferenceCreate"
        required: true
      responses:
        "201":
          description: "SubsidiaryLedgerReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /journal-entry-transactions/{journalEntryTransactionKey}/subsidiary-ledger-references/{subsidiaryLedgerReferenceKey}:
    get:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Retrieve a specific SubsidiaryLedgerReference r..."
      operationId: "getSubsidiaryLedgerReferenceById"
      responses:
        "200":
          description: "The requested SubsidiaryLedgerReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Replace a SubsidiaryLedgerReference record."
      operationId: "replaceSubsidiaryLedgerReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Delete a SubsidiaryLedgerReference Record"
      description: "Marks the SubsidiaryLedgerReference record as i..."
      operationId: "deleteSubsidiaryLedgerReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Partially update a SubsidiaryLedgerReference re..."
      operationId: "updateSubsidiaryLedgerReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "subsidiaryLedgerReferenceKey"
      in: "path"
      description: "The unique identifier for the specific SubsidiaryLedgerReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas:
    Codes:
      type: "array"
      items:
        type: "string"
    ControlAccountReference:
      type: "object"
      properties:
        control_account_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        control_account_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        control_account_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        system_codes:
          $ref: "#/components/schemas/Codes"
        control_account:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "External system identifier"
    ControlAccountReferenceCreate:
      required:
      - "control_account_reference_key"
      type: "object"
      description: "ControlAccountReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/ControlAccountReference"
    ControlAccountReferenceUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        context:
          type: "string"
          description: "Context or domain of the identifier"
        control_account:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "ControlAccountReference Update for Patch and Put"
    ControlAccountReferences:
      type: "array"
      items:
        $ref: "#/components/schemas/ControlAccountReference"
    DaysOfWeekTypes:
      type: "string"
      description: "Status of the account"
      enum:
      - "SUN"
      - "MON"
      - "TUES"
      - "WED"
      - "THURS"
      - "FRI"
      - "SAT"
    DurationUOMTypes:
      type: "string"
      description: "types of duration u o ms."
      enum:
      - "MINUTES"
      - "HOURS"
      - "DAYS"
      - "DAY_OF_MONTH"
      - "WEEKS"
      - "BI_WEEKS"
      - "SEMI_MONTHS"
      - "MONTHS"
      - "QUARTERS"
      - "SEMI_YEARS"
      - "YEARS"
      - "MULTI_YEARS"
      - "CUSTOM"
      - "OCCURRENCE"
      - "UNLIMITED"
      - "NONE"
      - "OTHER"
    EffectivePeriod:
      type: "object"
      properties:
        effective_period_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique key identifying the effective per... Pat..."
        active_indicator:
          type: "boolean"
          description: "Indicator if the period is currently act..."
        exclusive_indicator:
          type: "boolean"
          description: "Indicator if the period is exclusive"
        start_date_time:
          type: "string"
          description: "Start date and time of the effective per..."
          format: "date-time"
        complete_date_time:
          type: "string"
          description: "Completion date and time of the effectiv..."
          format: "date-time"
        duration_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        duration_value:
          type: "string"
          description: "Value of the duration"
        recur_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        recur_count:
          type: "string"
          description: "Count of recurrence"
        recur_values:
          $ref: "#/components/schemas/Codes"
        recur_criteria:
          type: "string"
          description: "Criteria defining recurrence"
        recur_indicator:
          type: "boolean"
          description: "Indicator if recurrence is enabled"
        timeslot:
          $ref: "#/components/schemas/TimeSlot"
      description: "Effective Period"
    EntryResourceTypes:
      type: "string"
      description: "types of entry resources."
      enum:
      - "CUSTOM"
      - "PAYROLL_EARNING"
      - "TAX"
      - "FEE"
      - "LENDER"
      - "TRADE_IN"
      - "DISCOUNT"
      - "INCENTIVE"
      - "RESIDUAL"
      - "DEPOSIT"
      - "DUE_ON"
      - "PAYMENT"
      - "PAYABLE"
      - "EXPENSE"
      - "RECEIVABLE"
      - "COST_OF_SALE"
      - "SALE"
      - "INVENTORY"
      - "INCOME"
      - "CONTRACTS_IN_TRANSIT"
      - "DEALER_VALUE"
      - "INSURANCE"
      - "SERVICE"
      - "PRODUCT"
      - "CONTRACT"
    ErrorDetail:
      required:
      - "code"
      - "message"
      - "timestamp"
      type: "object"
      properties:
        code:
          type: "string"
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: "NOT_FOUND_PR"
        message:
          type: "string"
          description: "A human-readable summary providing specific det..."
          example: "Personnel Record with ID PR-1234567 was not found."
        details:
          type: "array"
          description: "Optional array of specific field-level validati..."
          items:
            type: "object"
            properties:
              field:
                type: "string"
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: "payrollId"
              issue:
                type: "string"
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: "must be unique"
        timestamp:
          type: "string"
          description: "The time the error occurred, in ISO 8601 format."
          format: "date-time"
          example: "2025-11-10T10:30:00Z"
      description: "Standard format for all API error responses."
    FinancialFlowStatusTypes:
      type: "string"
      description: "types of financial flow status."
      enum:
      - "DRAFT"
      - "PENDING_APPROVAL"
      - "POSTED"
      - "FAILED"
      - "REFUNDED"
      - "APPROVED"
      - "DECLINED"
      - "REJECTED"
      - "INITIATING"
      - "COMPLETE"
      - "ACTIVE"
      - "INACTIVE"
      - "INVALID"
      - "ABANDON"
      - "CANCELLED"
      - "SALES_HOLD"
      - "PAYMENT_HOLD"
      - "FINANCE_HOLD"
      - "CUSTOMER_HOLD"
    GeneralLedgerReference:
      type: "object"
      properties:
        general_ledger_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        general_ledger_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        general_ledger_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        context:
          type: "string"
          description: "Context or domain of the reference"
        system_codes:
          $ref: "#/components/schemas/Codes"
        general_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "External system reference"
    GeneralLedgerReferenceCreate:
      required:
      - "general_ledger_reference_key"
      type: "object"
      description: "GeneralLedgerReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/GeneralLedgerReference"
    GeneralLedgerReferenceUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        general_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
        context:
          type: "string"
          description: "Context or domain of the reference"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "GeneralLedgerReference Update for Patch and Put"
    GeneralLedgerReferences:
      type: "array"
      items:
        $ref: "#/components/schemas/GeneralLedgerReference"
    Identifier:
      type: "object"
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: "External system identifier"
    IdentifierCreate:
      required:
      - "identifier_key"
      type: "object"
      description: "Identifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
      description: "Identifier Update for Patch and Put"
    Identifiers:
      type: "array"
      items:
        $ref: "#/components/schemas/Identifier"
    JournalEntries:
      type: "array"
      items:
        $ref: "#/components/schemas/JournalEntry"
    JournalEntry:
      type: "object"
      properties:
        journal_entry_id:
          type: "string"
          description: "natural immutable identifier"
        journal_entry_key:
          type: "string"
          description: "natural immutable identifier"
        entry_sequence:
          type: "string"
          description: "entry sequence"
        financial_transaction_key:
          type: "string"
          description: "Unique identifier of the financial trans..."
        control_account_references:
          $ref: "#/components/schemas/ControlAccountReferences"
        general_ledger_references:
          $ref: "#/components/schemas/GeneralLedgerReferences"
        subsidiary_ledger_reference:
          $ref: "#/components/schemas/SubsidiaryLedgerReference"
        desc:
          type: "string"
          description: "A description for the journal entry."
          example: "Sales - Extended Service Contract"
        detail_sequence:
          type: "string"
          description: "A sequence number for batch updates for ..."
        journal_type:
          $ref: "#/components/schemas/JournalTypes"
        org_href:
          type: "string"
          description: "A reference link to the organization ass..."
          format: "href"
          example: "1"
        org_id:
          type: "string"
          description: "The ID of the organization (cost or prof..."
          example: "1"
        party_identifiers:
          $ref: "#/components/schemas/PartyIdentifiers"
        org_type:
          $ref: "#/components/schemas/OrganizationTypes"
        money:
          $ref: "#/components/schemas/Money"
        entry_reference_account_desc:
          type: "string"
          description: "A description of the reference account."
          example: "Cost of Goods"
        reference_account_id:
          type: "string"
          description: "The ID of the reference account."
          example: "54322"
        resource_href:
          type: "string"
          description: "A unique identifier (link) to the root r..."
          format: "href"
        resource_id:
          type: "string"
          description: "The unique identifier of the root resour..."
          example: "543"
        resource_type:
          $ref: "#/components/schemas/EntryResourceTypes"
        gl_segments:
          $ref: "#/components/schemas/Identifiers"
      description: "journal entry for general ledger"
    JournalEntryCreate:
      required:
      - "journal_entry_key"
      type: "object"
      description: "JournalEntry Create for Post"
      allOf:
      - $ref: "#/components/schemas/JournalEntry"
    JournalEntryTransaction:
      type: "object"
      properties:
        journal_entry_transaction_id:
          type: "string"
          description: "A unique system-generated identifier for..."
          example: "gle_700123"
        journal_entry_transaction_key:
          type: "string"
          description: "A unique system-generated identifier for..."
          example: "gle_700123"
        provider_key:
          type: "string"
          description: "Identifier for the payroll system or pro..."
          example: "PAY-PRO-2"
        cycle_id:
          type: "string"
          description: "Identifier for the specific payroll cycl..."
          example: "2025-09-WEEK2"
        entry_date:
          type: "string"
          description: "The date the journal entry was recorded ..."
          format: "date"
          example: "2025-09-30T00:00:00Z"
        total_debit_amount:
          $ref: "#/components/schemas/Money"
        total_credit_amount:
          $ref: "#/components/schemas/Money"
        is_balanced:
          type: "boolean"
          description: "A flag indicating if the total debits eq..."
          example: "true"
        financial_flow_status_type:
          $ref: "#/components/schemas/FinancialFlowStatusTypes"
        journal_entries:
          $ref: "#/components/schemas/JournalEntries"
      description: "Represents the header of a General Ledger (GL) ..."
    JournalEntryTransactionCreate:
      required:
      - "journal_entry_transaction_key"
      type: "object"
      description: "JournalEntryTransaction Create for Post"
      allOf:
      - $ref: "#/components/schemas/JournalEntryTransaction"
    JournalEntryTransactionUpdate:
      type: "object"
      properties:
        financial_flow_status_type:
          $ref: "#/components/schemas/FinancialFlowStatusTypes"
        journal_entries:
          $ref: "#/components/schemas/JournalEntries"
        total_credit_amount:
          $ref: "#/components/schemas/Money"
        total_debit_amount:
          $ref: "#/components/schemas/Money"
        is_balanced:
          type: "boolean"
          description: "A flag indicating if the total debits eq..."
          example: "true"
        entry_date:
          type: "string"
          description: "The date the journal entry was recorded ..."
          format: "date"
          example: "2025-09-30T00:00:00Z"
      description: "JournalEntryTransaction Update for Patch and Put"
    JournalEntryUpdate:
      type: "object"
      properties:
        resource_href:
          type: "string"
          description: "A unique identifier (link) to the root r..."
          format: "href"
        party_identifiers:
          $ref: "#/components/schemas/PartyIdentifiers"
        control_account_references:
          $ref: "#/components/schemas/ControlAccountReferences"
        org_type:
          $ref: "#/components/schemas/OrganizationTypes"
        resource_type:
          $ref: "#/components/schemas/EntryResourceTypes"
        subsidiary_ledger_reference:
          $ref: "#/components/schemas/SubsidiaryLedgerReference"
        detail_sequence:
          type: "string"
          description: "A sequence number for batch updates for ..."
        gl_segments:
          $ref: "#/components/schemas/Identifiers"
        org_href:
          type: "string"
          description: "A reference link to the organization ass..."
          format: "href"
          example: "1"
        money:
          $ref: "#/components/schemas/Money"
        entry_reference_account_desc:
          type: "string"
          description: "A description of the reference account."
          example: "Cost of Goods"
        general_ledger_references:
          $ref: "#/components/schemas/GeneralLedgerReferences"
        entry_sequence:
          type: "string"
          description: "entry sequence"
        journal_type:
          $ref: "#/components/schemas/JournalTypes"
        desc:
          type: "string"
          description: "A description for the journal entry."
          example: "Sales - Extended Service Contract"
      description: "JournalEntry Update for Patch and Put"
    JournalTypes:
      type: "string"
      description: "types of journals."
      enum:
      - "DEBIT"
      - "CREDIT"
    Money:
      type: "object"
      properties:
        money_key:
          type: "string"
          description: "The unique key for the monetary value."
        money_id:
          type: "string"
          description: "The unique ID for the monetary value."
        currency_code:
          type: "string"
          description: "ISO currency code, e.g., 'USD'."
        display_value:
          type: "string"
          description: "The locale-specific display format for t..."
        locale:
          type: "string"
          description: "The locale code (e.g., en-US, fr-FR)."
        value:
          type: "string"
          description: "The monetary value."
      description: "Represents a monetary value, including its curr..."
    OrganizationTypes:
      type: "string"
      description: "types of organizations."
      enum:
      - "CORPORATION"
      - "DIVISION"
      - "FRANCHISE"
      - "GROUP"
      - "STORE"
      - "DEALERSHIP"
      - "BRANCH"
      - "DEPARTMENT"
      - "SECTION"
      - "TEAM"
      - "OTHER"
    PartyIdentifier:
      type: "object"
      properties:
        party_identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_identifier_type:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        party_relationship_types:
          pattern: "^[a-zA-Z0-9_]+$"
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/PartyRelationshipTypes"
        system_codes:
          $ref: "#/components/schemas/Codes"
        party:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
      description: "External system identifier"
    PartyIdentifierCreate:
      required:
      - "party_identifier_key"
      type: "object"
      description: "PartyIdentifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartyIdentifier"
    PartyIdentifierUpdate:
      type: "object"
      properties:
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
        system_codes:
          $ref: "#/components/schemas/Codes"
        context:
          type: "string"
          description: "Context or domain of the identifier"
        party_relationship_types:
          pattern: "^[a-zA-Z0-9_]+$"
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/PartyRelationshipTypes"
        party_identifier_type:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
        party:
          $ref: "#/components/schemas/Identifier"
      description: "PartyIdentifier Update for Patch and Put"
    PartyIdentifiers:
      type: "array"
      items:
        $ref: "#/components/schemas/PartyIdentifier"
    PartyRelationshipTypes:
      type: "string"
      description: "types of party relationships."
      enum:
      - "CORPORATION"
      - "GROUP"
      - "STORE"
      - "BRANCH"
      - "DEPARTMENT"
      - "SECTION"
      - "TEAM"
      - "DEALERSHIP"
      - "THIRD_PARTY"
      - "VENDOR"
      - "SUPPLIER"
      - "PARTNER_ORGANIZATION"
      - "AFFILIATE"
      - "DISTRIBUTOR"
      - "MANUFACTURER"
      - "SERVICE_PROVIDER"
      - "EMPLOYER"
      - "INTERNAL_ORGANIZATION"
      - "COST_CENTER"
      - "FREIGHT_FORWARDER"
      - "BUYER"
      - "REGULATOR"
      - "GOVERNMENT_AGENCY"
      - "NON_PROFIT"
      - "FRANCHISE"
      - "SUBSIDIARY"
      - "DIVISION"
      - "JOINT_VENTURE"
      - "AUDITOR"
      - "ACTOR"
      - "ACCOUNTABLE"
      - "RESPONSIBLE"
      - "CONSULT"
      - "NOTIFY"
      - "EXECUTE"
      - "PART_OF"
      - "MEMBERSHIP"
      - "COMPOSED_IN"
      - "CUSTOMER"
      - "CO_SIGNER"
      - "SPOUSE"
      - "PARENT"
      - "CHILD"
      - "SIBLING"
      - "LEGAL_GUARDIAN"
      - "DEPENDENT"
      - "EMERGENCY_CONTACT"
      - "FAMILY_MEMBER"
      - "FRIEND"
      - "ACQUAINTANCE"
      - "ASSOCIATE"
      - "EX_SPOUSE"
      - "PROFESSIONAL_CONTACT"
      - "MANAGER"
      - "CO_WORKER"
      - "FINANCIAL_ADVISOR"
      - "SELF"
      - "ROMANTIC_PARTNER"
      - "OTHER_FAMILY"
      - "CAREGIVER"
      - "MENTOR"
      - "MENTEE"
      - "HEIR"
      - "TRUSTEE_PERSON"
      - "REFERENCE_PERSONAL"
      - "REFERENCE_PROFESSIONAL"
      - "AGENT"
      - "BENEFICIARY"
      - "GUARANTOR"
      - "EMPLOYEE"
      - "CONTRACTOR"
      - "OWNER"
      - "BUSINESS_PARTNER"
      - "SERVICE_CONSUMER"
      - "POLITICAL_AFFILIATE"
      - "ADVISOR_CONSULTANT"
      - "PROXY"
      - "SHAREHOLDER"
      - "INVESTOR"
      - "CUSTODIAN"
      - "SOLD_TO"
      - "SHIP_TO"
      - "PAYER"
      - "PAYEE"
      - "GIFT_TO"
      - "LOAN_PROVIDER"
      - "CREDIT_CARD_PROVIDER"
      - "LESSEE"
      - "LESSOR"
      - "INSURED"
      - "INSURER"
      - "UNDERWRITER"
      - "RECIPIENT"
      - "TRANSACTION_FROM"
      - "TRANSACTION_TO"
      - "REMOVE"
      - "OTHER"
    SubsidiaryLedgerReference:
      type: "object"
      properties:
        subsidiary_ledger_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier for the Subsidiary Led... Pat..."
        subsidiary_ledger_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key for the Subsidiary Ledger r... Pat..."
        subsidiary_ledger_transaction_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "The key of the referenced Subsidiary Led... Pat..."
        context:
          type: "string"
          description: "Context or domain of the Sub-Ledger tran..."
        system_codes:
          $ref: "#/components/schemas/Codes"
        subsidiary_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the Journal Entry refe..."
          format: "date-time"
      description: "Immutable reference from a Journal Entry back t..."
    SubsidiaryLedgerReferenceCreate:
      required:
      - "subsidiary_ledger_reference_key"
      type: "object"
      description: "SubsidiaryLedgerReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/SubsidiaryLedgerReference"
    SubsidiaryLedgerReferenceUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        context:
          type: "string"
          description: "Context or domain of the Sub-Ledger tran..."
        occurrence_date_time:
          type: "string"
          description: "The date and time the Journal Entry refe..."
          format: "date-time"
        subsidiary_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
      description: "SubsidiaryLedgerReference Update for Patch and Put"
    TimeSlot:
      type: "object"
      properties:
        time_slot_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique business key for the time slot. Pattern:..."
        end_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        end_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: "string"
          description: "End time in HH:mm format. Pattern: ^([01]\\d|2[0..."
          format: "date-time"
        recurring_indicator:
          type: "boolean"
          description: "Indicates if the time slot is recurring."
        recurring_period:
          $ref: "#/components/schemas/EffectivePeriod"
        start_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        start_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: "string"
          description: "Start time in HH:mm format. Pattern: ^([01]\\d|2..."
          format: "date-time"
        timeslot_directive_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/TimeslotDirectiveTypes"
        time_slot_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier for the time slot reco... Pat..."
        week_of_date:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          format: "date"
          items:
            type: "string"
      description: "Range of time for the appointment including sta..."
    TimeslotDirectiveTypes:
      type: "string"
      description: "Represents the directive for a timeslot."
      enum:
      - "AFTER"
      - "ANYTIME"
      - "BEFORE"
      - "BETWEEN"
      - "BLOCK"
      - "UNKNOWN"
  responses:
    NotFoundError:
      description: "The requested resource was not found."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: "Authentication failed. The access token is miss..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: "The authenticated user does not have the necess..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: "The request could not be completed because it c..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: "An unexpected error occurred on the server whil..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
