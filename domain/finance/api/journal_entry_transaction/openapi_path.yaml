---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /journal-entry-transactions:
    get:
      tags:
      - "JournalEntryTransaction"
      summary: "Retrieve a list of all JournalEntryTransaction records."
      operationId: "listJournalEntryTransactions"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of JournalEntryTransaction records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/JournalEntryTransaction"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "JournalEntryTransaction"
      summary: "Create a new JournalEntryTransaction record."
      operationId: "createJournalEntryTransaction"
      requestBody:
        description: "JournalEntryTransaction data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransaction_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "JournalEntryTransaction created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}:
    get:
      tags:
      - "JournalEntryTransaction"
      summary: "Retrieve a specific JournalEntryTransaction record."
      operationId: "getJournalEntryTransactionById"
      responses:
        "200":
          description: "The requested JournalEntryTransaction record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "JournalEntryTransaction"
      summary: "replace a JournalEntryTransaction record."
      operationId: "replaceJournalEntryTransaction"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransaction"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "JournalEntryTransaction"
      summary: "Delete a JournalEntryTransaction Record"
      description: "Marks the JournalEntryTransaction record as inactive or removes\
        \ it entirely."
      operationId: "deleteJournalEntryTransactionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "JournalEntryTransaction"
      summary: "Partially update a JournalEntryTransaction record."
      operationId: "updateJournalEntryTransaction"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntryTransaction_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntryTransaction"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryTransactionKey"
      in: "path"
      description: "The unique identifier for the specific JournalEntryTransaction."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/moneys:
    get:
      tags:
      - "Money"
      summary: "Retrieve a list of all Money records."
      operationId: "listMoneys"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of Money records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/Money"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
  /journal-entry-transactions/{journalEntryTransactionKey}/moneys/{moneyKey}:
    get:
      tags:
      - "Money"
      summary: "Retrieve a specific Money record."
      operationId: "getMoneyById"
      responses:
        "200":
          description: "The requested Money record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Money"
      summary: "replace a Money record."
      operationId: "replaceMoney"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Money"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "moneyKey"
      in: "path"
      description: "The unique identifier for the specific Money."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/general-ledger-references:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a list of all GeneralLedgerReference records."
      operationId: "listGeneralLedgerReferences"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of GeneralLedgerReference records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/GeneralLedgerReference"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "GeneralLedgerReference"
      summary: "Create a new GeneralLedgerReference record."
      operationId: "createGeneralLedgerReference"
      requestBody:
        description: "GeneralLedgerReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReference_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "GeneralLedgerReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/general-ledger-references/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/general-ledger-references/{generalLedgerReferenceKey}:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a specific GeneralLedgerReference record."
      operationId: "getGeneralLedgerReferenceById"
      responses:
        "200":
          description: "The requested GeneralLedgerReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "GeneralLedgerReference"
      summary: "replace a GeneralLedgerReference record."
      operationId: "replaceGeneralLedgerReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReference"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "GeneralLedgerReference"
      summary: "Delete a GeneralLedgerReference Record"
      description: "Marks the GeneralLedgerReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteGeneralLedgerReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "GeneralLedgerReference"
      summary: "Partially update a GeneralLedgerReference record."
      operationId: "updateGeneralLedgerReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReference_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "generalLedgerReferenceKey"
      in: "path"
      description: "The unique identifier for the specific GeneralLedgerReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of all Identifier records."
      operationId: "listIdentifiers"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/Identifier"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/identifiers/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or removes it entirely."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/control-account-references:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a list of all ControlAccountReference records."
      operationId: "listControlAccountReferences"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of ControlAccountReference records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/ControlAccountReference"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ControlAccountReference"
      summary: "Create a new ControlAccountReference record."
      operationId: "createControlAccountReference"
      requestBody:
        description: "ControlAccountReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "ControlAccountReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/control-account-references/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/control-account-references/{controlAccountReferenceKey}:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a specific ControlAccountReference record."
      operationId: "getControlAccountReferenceById"
      responses:
        "200":
          description: "The requested ControlAccountReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ControlAccountReference"
      summary: "replace a ControlAccountReference record."
      operationId: "replaceControlAccountReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ControlAccountReference"
      summary: "Delete a ControlAccountReference Record"
      description: "Marks the ControlAccountReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteControlAccountReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ControlAccountReference"
      summary: "Partially update a ControlAccountReference record."
      operationId: "updateControlAccountReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "controlAccountReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ControlAccountReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/party-identifiers:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a list of all PartyIdentifier records."
      operationId: "listPartyIdentifiers"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of PartyIdentifier records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/PartyIdentifier"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartyIdentifier"
      summary: "Create a new PartyIdentifier record."
      operationId: "createPartyIdentifier"
      requestBody:
        description: "PartyIdentifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "PartyIdentifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/party-identifiers/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/party-identifiers/{partyIdentifierKey}:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a specific PartyIdentifier record."
      operationId: "getPartyIdentifierById"
      responses:
        "200":
          description: "The requested PartyIdentifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartyIdentifier"
      summary: "replace a PartyIdentifier record."
      operationId: "replacePartyIdentifier"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartyIdentifier"
      summary: "Delete a PartyIdentifier Record"
      description: "Marks the PartyIdentifier record as inactive or removes it entirely."
      operationId: "deletePartyIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartyIdentifier"
      summary: "Partially update a PartyIdentifier record."
      operationId: "updatePartyIdentifier"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partyIdentifierKey"
      in: "path"
      description: "The unique identifier for the specific PartyIdentifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/journal-entries:
    get:
      tags:
      - "JournalEntry"
      summary: "Retrieve a list of all JournalEntry records."
      operationId: "listJournalEntrys"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of JournalEntry records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/JournalEntry"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "JournalEntry"
      summary: "Create a new JournalEntry record."
      operationId: "createJournalEntry"
      requestBody:
        description: "JournalEntry data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntry_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "JournalEntry created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/journal-entries/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/journal-entries/{journalEntryKey}:
    get:
      tags:
      - "JournalEntry"
      summary: "Retrieve a specific JournalEntry record."
      operationId: "getJournalEntryById"
      responses:
        "200":
          description: "The requested JournalEntry record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "JournalEntry"
      summary: "replace a JournalEntry record."
      operationId: "replaceJournalEntry"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntry"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "JournalEntry"
      summary: "Delete a JournalEntry Record"
      description: "Marks the JournalEntry record as inactive or removes it entirely."
      operationId: "deleteJournalEntryRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "JournalEntry"
      summary: "Partially update a JournalEntry record."
      operationId: "updateJournalEntry"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalEntry_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "journalEntryKey"
      in: "path"
      description: "The unique identifier for the specific JournalEntry."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /journal-entry-transactions/{journalEntryTransactionKey}/subsidiary-ledger-references:
    get:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Retrieve a list of all SubsidiaryLedgerReference records."
      operationId: "listSubsidiaryLedgerReferences"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return. (For pagination)"
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          exampleSetFlag: false
          default: 50
      - name: "cursor"
        in: "query"
        description: "A token or value representing the starting point for the next\
          \ set of results. (For cursor-based pagination)"
        schema:
          type: "string"
          exampleSetFlag: false
      responses:
        "200":
          description: "A list of SubsidiaryLedgerReference records."
          content:
            application/json:
              schema:
                type: "array"
                exampleSetFlag: false
                items:
                  $ref: "#/components/schemas/SubsidiaryLedgerReference"
                  exampleSetFlag: false
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Create a new SubsidiaryLedgerReference record."
      operationId: "createSubsidiaryLedgerReference"
      requestBody:
        description: "SubsidiaryLedgerReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReference_Create"
              exampleSetFlag: false
        required: true
      responses:
        "201":
          description: "SubsidiaryLedgerReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//journal-entry-transactions/{journalEntryTransactionKey}/subsidiary-ledger-references/PR-1234567"
                exampleSetFlag: true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /journal-entry-transactions/{journalEntryTransactionKey}/subsidiary-ledger-references/{subsidiaryLedgerReferenceKey}:
    get:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Retrieve a specific SubsidiaryLedgerReference record."
      operationId: "getSubsidiaryLedgerReferenceById"
      responses:
        "200":
          description: "The requested SubsidiaryLedgerReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
                exampleSetFlag: false
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "replace a SubsidiaryLedgerReference record."
      operationId: "replaceSubsidiaryLedgerReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReference"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Delete a SubsidiaryLedgerReference Record"
      description: "Marks the SubsidiaryLedgerReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteSubsidiaryLedgerReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "SubsidiaryLedgerReference"
      summary: "Partially update a SubsidiaryLedgerReference record."
      operationId: "updateSubsidiaryLedgerReference"
      requestBody:
        description: "Fields to be updated. Only included fields will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubsidiaryLedgerReference_Update"
              exampleSetFlag: false
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubsidiaryLedgerReference"
                exampleSetFlag: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "subsidiaryLedgerReferenceKey"
      in: "path"
      description: "The unique identifier for the specific SubsidiaryLedgerReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
components:
  schemas: {}
