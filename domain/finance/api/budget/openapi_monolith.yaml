---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /budgets:
    get:
      tags:
      - "Budget"
      summary: "Retrieve a list of all Budget records."
      operationId: "listBudgets"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Budget records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Budget"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Budget"
      summary: "Create a new Budget record."
      operationId: "createBudget"
      requestBody:
        description: "Budget data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BudgetCreate"
        required: true
      responses:
        "201":
          description: "Budget created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/budgets/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Budget"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /budgets/{budgetKey}:
    get:
      tags:
      - "Budget"
      summary: "Retrieve a specific Budget record."
      operationId: "getBudgetById"
      responses:
        "200":
          description: "The requested Budget record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Budget"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Budget"
      summary: "Replace a Budget record."
      operationId: "replaceBudget"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Budget"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Budget"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Budget"
      summary: "Delete a Budget Record"
      description: "Marks the Budget record as inactive or removes ..."
      operationId: "deleteBudgetRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Budget"
      summary: "Partially update a Budget record."
      operationId: "updateBudget"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BudgetUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Budget"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the specific Budget."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/moneys:
    get:
      tags:
      - "Money"
      summary: "Retrieve a list of Money records scoped by budg..."
      operationId: "listMoneysByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Money records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Money"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/moneys/{moneyKey}:
    get:
      tags:
      - "Money"
      summary: "Retrieve a specific Money record."
      operationId: "getMoneyById"
      responses:
        "200":
          description: "The requested Money record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Money"
      summary: "Replace a Money record."
      operationId: "replaceMoney"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Money"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "moneyKey"
      in: "path"
      description: "The unique identifier for the specific Money."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/general-ledger-references:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a list of GeneralLedgerReference recor..."
      operationId: "listGeneralLedgerReferencesByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of GeneralLedgerReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/GeneralLedgerReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "GeneralLedgerReference"
      summary: "Create a new GeneralLedgerReference record."
      operationId: "createGeneralLedgerReference"
      requestBody:
        description: "GeneralLedgerReference data to create the new r..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReferenceCreate"
        required: true
      responses:
        "201":
          description: "GeneralLedgerReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/general-ledger-references/{generalLedgerReferenceKey}:
    get:
      tags:
      - "GeneralLedgerReference"
      summary: "Retrieve a specific GeneralLedgerReference record."
      operationId: "getGeneralLedgerReferenceById"
      responses:
        "200":
          description: "The requested GeneralLedgerReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "GeneralLedgerReference"
      summary: "Replace a GeneralLedgerReference record."
      operationId: "replaceGeneralLedgerReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "GeneralLedgerReference"
      summary: "Delete a GeneralLedgerReference Record"
      description: "Marks the GeneralLedgerReference record as inac..."
      operationId: "deleteGeneralLedgerReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "GeneralLedgerReference"
      summary: "Partially update a GeneralLedgerReference record."
      operationId: "updateGeneralLedgerReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneralLedgerReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneralLedgerReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "generalLedgerReferenceKey"
      in: "path"
      description: "The unique identifier for the specific GeneralLedgerReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by..."
      operationId: "listIdentifiersByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or remo..."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/budget-items:
    get:
      tags:
      - "BudgetItem"
      summary: "Retrieve a list of BudgetItem records scoped by..."
      operationId: "listBudgetItemsByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of BudgetItem records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/BudgetItem"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "BudgetItem"
      summary: "Create a new BudgetItem record."
      operationId: "createBudgetItem"
      requestBody:
        description: "BudgetItem data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BudgetItemCreate"
        required: true
      responses:
        "201":
          description: "BudgetItem created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BudgetItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/budget-items/{budgetItemKey}:
    get:
      tags:
      - "BudgetItem"
      summary: "Retrieve a specific BudgetItem record."
      operationId: "getBudgetItemById"
      responses:
        "200":
          description: "The requested BudgetItem record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BudgetItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "BudgetItem"
      summary: "Replace a BudgetItem record."
      operationId: "replaceBudgetItem"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BudgetItem"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BudgetItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "BudgetItem"
      summary: "Delete a BudgetItem Record"
      description: "Marks the BudgetItem record as inactive or remo..."
      operationId: "deleteBudgetItemRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "BudgetItem"
      summary: "Partially update a BudgetItem record."
      operationId: "updateBudgetItem"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BudgetItemUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BudgetItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "budgetItemKey"
      in: "path"
      description: "The unique identifier for the specific BudgetItem."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/accounting-periods:
    get:
      tags:
      - "AccountingPeriod"
      summary: "Retrieve a list of AccountingPeriod records sco..."
      operationId: "listAccountingPeriodsByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of AccountingPeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/AccountingPeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AccountingPeriod"
      summary: "Create a new AccountingPeriod record."
      operationId: "createAccountingPeriod"
      requestBody:
        description: "AccountingPeriod data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodCreate"
        required: true
      responses:
        "201":
          description: "AccountingPeriod created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/accounting-periods/{accountingPeriodKey}:
    get:
      tags:
      - "AccountingPeriod"
      summary: "Retrieve a specific AccountingPeriod record."
      operationId: "getAccountingPeriodById"
      responses:
        "200":
          description: "The requested AccountingPeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AccountingPeriod"
      summary: "Replace a AccountingPeriod record."
      operationId: "replaceAccountingPeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AccountingPeriod"
      summary: "Delete a AccountingPeriod Record"
      description: "Marks the AccountingPeriod record as inactive o..."
      operationId: "deleteAccountingPeriodRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AccountingPeriod"
      summary: "Partially update a AccountingPeriod record."
      operationId: "updateAccountingPeriod"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "accountingPeriodKey"
      in: "path"
      description: "The unique identifier for the specific AccountingPeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /budgets/{budgetKey}/control-account-references:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a list of ControlAccountReference reco..."
      operationId: "listControlAccountReferencesByBudgetKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ControlAccountReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ControlAccountReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ControlAccountReference"
      summary: "Create a new ControlAccountReference record."
      operationId: "createControlAccountReference"
      requestBody:
        description: "ControlAccountReference data to create the new ..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceCreate"
        required: true
      responses:
        "201":
          description: "ControlAccountReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /budgets/{budgetKey}/control-account-references/{controlAccountReferenceKey}:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a specific ControlAccountReference rec..."
      operationId: "getControlAccountReferenceById"
      responses:
        "200":
          description: "The requested ControlAccountReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ControlAccountReference"
      summary: "Replace a ControlAccountReference record."
      operationId: "replaceControlAccountReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ControlAccountReference"
      summary: "Delete a ControlAccountReference Record"
      description: "Marks the ControlAccountReference record as ina..."
      operationId: "deleteControlAccountReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ControlAccountReference"
      summary: "Partially update a ControlAccountReference record."
      operationId: "updateControlAccountReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "budgetKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "controlAccountReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ControlAccountReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas:
    AccountingPeriod:
      type: "object"
      properties:
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        period_desc:
          maxLength: 255
          type: "string"
          description: "Descriptive name for the accounting peri..."
        accounting_period_id:
          pattern: "^[0-9]{6}$"
          type: "string"
          description: "Business key for the accounting period (... Pat..."
        accounting_period_key:
          pattern: "^[0-9]{6}$"
          type: "string"
          description: "Business key for the accounting period (... Pat..."
        period_name:
          maxLength: 100
          type: "string"
          description: "Display name of the accounting period."
        start_date:
          type: "string"
          description: "The beginning date of the accounting per..."
          format: "date"
        end_date:
          type: "string"
          description: "The ending date of the accounting period..."
          format: "date"
        accounting_period_status_type:
          $ref: "#/components/schemas/AccountingPeriodStatusTypes"
        fiscal_year:
          type: "integer"
          description: "The fiscal year this period belongs to."
          format: "int32"
      description: "Defines a fiscal period, including its unique i..."
    AccountingPeriodCreate:
      required:
      - "accounting_period_key"
      type: "object"
      description: "AccountingPeriod Create for Post"
      allOf:
      - $ref: "#/components/schemas/AccountingPeriod"
    AccountingPeriodStatusTypes:
      type: "string"
      description: "types of accounting period status."
      enum:
      - "OPEN"
      - "ADJUSTMENT"
      - "CLOSED"
      - "FUTURE"
      - "PENDING_CLOSURE"
      - "LOCKED"
      - "OTHER"
    AccountingPeriodUpdate:
      type: "object"
      properties:
        end_date:
          type: "string"
          description: "The ending date of the accounting period..."
          format: "date"
        period_desc:
          maxLength: 255
          type: "string"
          description: "Descriptive name for the accounting peri..."
        fiscal_year:
          type: "integer"
          description: "The fiscal year this period belongs to."
          format: "int32"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        accounting_period_status_type:
          $ref: "#/components/schemas/AccountingPeriodStatusTypes"
        period_name:
          maxLength: 100
          type: "string"
          description: "Display name of the accounting period."
        start_date:
          type: "string"
          description: "The beginning date of the accounting per..."
          format: "date"
      description: "AccountingPeriod Update for Patch and Put"
    Budget:
      type: "object"
      properties:
        budget_key:
          type: "string"
          description: "The unique key for the Budget."
        budget_id:
          type: "string"
          description: "The unique ID for the Budget."
        name:
          type: "string"
          description: "The name given to the budget (e.g., \"202..."
        accounting_period:
          $ref: "#/components/schemas/AccountingPeriod"
        status:
          $ref: "#/components/schemas/BudgetStatusTypes"
        approval_date:
          type: "string"
          description: "The date the budget was approved."
          format: "date"
        budget_items:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/BudgetItem"
      description: "Represents an approved financial budget for a s..."
    BudgetCreate:
      required:
      - "budget_key"
      type: "object"
      description: "Budget Create for Post"
      allOf:
      - $ref: "#/components/schemas/Budget"
    BudgetItem:
      type: "object"
      properties:
        budget_item_key:
          type: "string"
          description: "The unique key for the BudgetItem."
        budget_item_id:
          type: "string"
          description: "The unique ID for the BudgetItem."
        general_ledger_account:
          $ref: "#/components/schemas/GeneralLedgerReference"
        control_account:
          $ref: "#/components/schemas/ControlAccountReference"
        financial_category:
          $ref: "#/components/schemas/FinancialCategoryTypes"
        budgeted_amount:
          $ref: "#/components/schemas/Money"
      description: "Represents a single line item within a financia..."
    BudgetItemCreate:
      required:
      - "budget_item_key"
      type: "object"
      description: "BudgetItem Create for Post"
      allOf:
      - $ref: "#/components/schemas/BudgetItem"
    BudgetItemUpdate:
      type: "object"
      properties:
        budgeted_amount:
          $ref: "#/components/schemas/Money"
        financial_category:
          $ref: "#/components/schemas/FinancialCategoryTypes"
        general_ledger_account:
          $ref: "#/components/schemas/GeneralLedgerReference"
        control_account:
          $ref: "#/components/schemas/ControlAccountReference"
      description: "BudgetItem Update for Patch and Put"
    BudgetStatusTypes:
      type: "string"
      description: "types of budget status."
      enum:
      - "DRAFT"
      - "SUBMITTED"
      - "PENDING_APPROVAL"
      - "APPROVED"
      - "ACTIVE"
      - "REJECTED"
      - "REVISED"
      - "CLOSED"
      - "VOIDED"
      - "EXPIRED"
      - "OTHER"
    BudgetUpdate:
      type: "object"
      properties:
        approval_date:
          type: "string"
          description: "The date the budget was approved."
          format: "date"
        budget_items:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/BudgetItem"
        name:
          type: "string"
          description: "The name given to the budget (e.g., \"202..."
        accounting_period:
          $ref: "#/components/schemas/AccountingPeriod"
        status:
          $ref: "#/components/schemas/BudgetStatusTypes"
      description: "Budget Update for Patch and Put"
    Codes:
      type: "array"
      items:
        type: "string"
    ControlAccountReference:
      type: "object"
      properties:
        control_account_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        control_account_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        control_account_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        system_codes:
          $ref: "#/components/schemas/Codes"
        control_account:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "External system identifier"
    ControlAccountReferenceCreate:
      required:
      - "control_account_reference_key"
      type: "object"
      description: "ControlAccountReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/ControlAccountReference"
    ControlAccountReferenceUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        context:
          type: "string"
          description: "Context or domain of the identifier"
        control_account:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "ControlAccountReference Update for Patch and Put"
    ErrorDetail:
      required:
      - "code"
      - "message"
      - "timestamp"
      type: "object"
      properties:
        code:
          type: "string"
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: "NOT_FOUND_PR"
        message:
          type: "string"
          description: "A human-readable summary providing specific det..."
          example: "Personnel Record with ID PR-1234567 was not found."
        details:
          type: "array"
          description: "Optional array of specific field-level validati..."
          items:
            type: "object"
            properties:
              field:
                type: "string"
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: "payrollId"
              issue:
                type: "string"
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: "must be unique"
        timestamp:
          type: "string"
          description: "The time the error occurred, in ISO 8601 format."
          format: "date-time"
          example: "2025-11-10T10:30:00Z"
      description: "Standard format for all API error responses."
    FinancialCategoryTypes:
      type: "string"
      description: "types of financial categorys."
      enum:
      - "CAPEX"
      - "OPEX"
      - "HARDWARE"
      - "SOFTWARE"
      - "MAINTENANCE"
      - "TRAVEL"
      - "TRAINING"
      - "UTILITIES"
      - "RENT"
      - "SALARY"
      - "REVENUE"
      - "TAX"
      - "OTHER"
    GeneralLedgerReference:
      type: "object"
      properties:
        general_ledger_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        general_ledger_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        general_ledger_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique reference value Pattern: ^[a-zA-Z0-9_]+$..."
        context:
          type: "string"
          description: "Context or domain of the reference"
        system_codes:
          $ref: "#/components/schemas/Codes"
        general_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "External system reference"
    GeneralLedgerReferenceCreate:
      required:
      - "general_ledger_reference_key"
      type: "object"
      description: "GeneralLedgerReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/GeneralLedgerReference"
    GeneralLedgerReferenceUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        general_ledger_identifier:
          $ref: "#/components/schemas/Identifier"
        context:
          type: "string"
          description: "Context or domain of the reference"
        occurrence_date_time:
          type: "string"
          description: "The date and time the event occurred."
          format: "date-time"
      description: "GeneralLedgerReference Update for Patch and Put"
    Identifier:
      type: "object"
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: "External system identifier"
    IdentifierCreate:
      required:
      - "identifier_key"
      type: "object"
      description: "Identifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
      description: "Identifier Update for Patch and Put"
    Identifiers:
      type: "array"
      items:
        $ref: "#/components/schemas/Identifier"
    Money:
      type: "object"
      properties:
        money_key:
          type: "string"
          description: "The unique key for the monetary value."
        money_id:
          type: "string"
          description: "The unique ID for the monetary value."
        currency_code:
          type: "string"
          description: "ISO currency code, e.g., 'USD'."
        display_value:
          type: "string"
          description: "The locale-specific display format for t..."
        locale:
          type: "string"
          description: "The locale code (e.g., en-US, fr-FR)."
        value:
          type: "string"
          description: "The monetary value."
      description: "Represents a monetary value, including its curr..."
  responses:
    NotFoundError:
      description: "The requested resource was not found."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: "Authentication failed. The access token is miss..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: "The authenticated user does not have the necess..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: "The request could not be completed because it c..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: "An unexpected error occurred on the server whil..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
