openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: This API provides a standardized interface for ...
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
security:
- industry-oauth:
  - standard:read
- member-api-key: []
- {}
paths:
  /receipts:
    get:
      tags:
      - Receipt
      summary: Retrieve a list of Receipt entities.
      operationId: listReceipt
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Receipt entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Receipt"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Receipt
      summary: Create a new Receipt entity.
      operationId: createReceipt
      requestBody:
        description: Receipt data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptCreate"
        required: true
      responses:
        "201":
          description: Receipt created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /receipts/PR-1234567
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /receipts/{receiptKey}:
    get:
      tags:
      - Receipt
      summary: Retrieve a specific Receipt entity.
      operationId: getReceiptById
      responses:
        "200":
          description: The requested Receipt entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - Receipt
      summary: Replace a Receipt entity.
      operationId: replaceReceipt
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Receipt"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - Receipt
      summary: Delete a Receipt entity
      description: Marks the Receipt entity as inactive or removes...
      operationId: deleteReceiptEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - Receipt
      summary: Partially update a Receipt entity.
      operationId: updateReceipt
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ReceiptUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the specific Receipt.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/shipment-references:
    get:
      tags:
      - ShipmentReference
      summary: Retrieve a list of ShipmentReference entities s...
      operationId: listShipmentReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of ShipmentReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ShipmentReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - ShipmentReference
      summary: Create a new ShipmentReference entity.
      operationId: createShipmentReference
      requestBody:
        description: ShipmentReference data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceCreate"
        required: true
      responses:
        "201":
          description: ShipmentReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/shipment-references/{shipmentReferenceKey}:
    get:
      tags:
      - ShipmentReference
      summary: Retrieve a specific ShipmentReference entity.
      operationId: getShipmentReferenceById
      responses:
        "200":
          description: The requested ShipmentReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - ShipmentReference
      summary: Replace a ShipmentReference entity.
      operationId: replaceShipmentReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - ShipmentReference
      summary: Delete a ShipmentReference entity
      description: Marks the ShipmentReference entity as inactive ...
      operationId: deleteShipmentReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - ShipmentReference
      summary: Partially update a ShipmentReference entity.
      operationId: updateShipmentReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: shipmentReferenceKey
      in: path
      description: The unique identifier for the specific ShipmentReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/unit-of-measures:
    get:
      tags:
      - UnitOfMeasure
      summary: Retrieve a list of UnitOfMeasure entities scope...
      operationId: listUnitOfMeasureByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of UnitOfMeasure entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/UnitOfMeasure"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/unit-of-measures/{unitOfMeasureKey}:
    get:
      tags:
      - UnitOfMeasure
      summary: Retrieve a specific UnitOfMeasure entity.
      operationId: getUnitOfMeasureById
      responses:
        "200":
          description: The requested UnitOfMeasure entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - UnitOfMeasure
      summary: Replace a UnitOfMeasure entity.
      operationId: replaceUnitOfMeasure
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: unitOfMeasureKey
      in: path
      description: The unique identifier for the specific UnitOfMeasure.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/positions:
    get:
      tags:
      - Position
      summary: Retrieve a list of Position entities scoped by ...
      operationId: listPositionByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Position entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Position"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Position
      summary: Create a new Position entity.
      operationId: createPosition
      requestBody:
        description: Position data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionCreate"
        required: true
      responses:
        "201":
          description: Position created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/positions/{positionKey}:
    get:
      tags:
      - Position
      summary: Retrieve a specific Position entity.
      operationId: getPositionById
      responses:
        "200":
          description: The requested Position entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - Position
      summary: Replace a Position entity.
      operationId: replacePosition
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - Position
      summary: Delete a Position entity
      description: Marks the Position entity as inactive or remove...
      operationId: deletePositionEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - Position
      summary: Partially update a Position entity.
      operationId: updatePosition
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PositionUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: positionKey
      in: path
      description: The unique identifier for the specific Position.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/time-slots:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a list of TimeSlot entities scoped by ...
      operationId: listTimeSlotByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of TimeSlot entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TimeSlot"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/time-slots/{timeSlotKey}:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a specific TimeSlot entity.
      operationId: getTimeSlotById
      responses:
        "200":
          description: The requested TimeSlot entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - TimeSlot
      summary: Replace a TimeSlot entity.
      operationId: replaceTimeSlot
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TimeSlot"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: timeSlotKey
      in: path
      description: The unique identifier for the specific TimeSlot.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/address-references:
    get:
      tags:
      - AddressReference
      summary: Retrieve a list of AddressReference entities sc...
      operationId: listAddressReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of AddressReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/AddressReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - AddressReference
      summary: Create a new AddressReference entity.
      operationId: createAddressReference
      requestBody:
        description: AddressReference data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReferenceCreate"
        required: true
      responses:
        "201":
          description: AddressReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/address-references/{addressReferenceKey}:
    get:
      tags:
      - AddressReference
      summary: Retrieve a specific AddressReference entity.
      operationId: getAddressReferenceById
      responses:
        "200":
          description: The requested AddressReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - AddressReference
      summary: Replace a AddressReference entity.
      operationId: replaceAddressReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - AddressReference
      summary: Delete a AddressReference entity
      description: Marks the AddressReference entity as inactive o...
      operationId: deleteAddressReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - AddressReference
      summary: Partially update a AddressReference entity.
      operationId: updateAddressReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AddressReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: addressReferenceKey
      in: path
      description: The unique identifier for the specific AddressReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/party-references:
    get:
      tags:
      - PartyReference
      summary: Retrieve a list of PartyReference entities scop...
      operationId: listPartyReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PartyReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PartyReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PartyReference
      summary: Create a new PartyReference entity.
      operationId: createPartyReference
      requestBody:
        description: PartyReference data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyReferenceCreate"
        required: true
      responses:
        "201":
          description: PartyReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/party-references/{partyReferenceKey}:
    get:
      tags:
      - PartyReference
      summary: Retrieve a specific PartyReference entity.
      operationId: getPartyReferenceById
      responses:
        "200":
          description: The requested PartyReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - PartyReference
      summary: Replace a PartyReference entity.
      operationId: replacePartyReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - PartyReference
      summary: Delete a PartyReference entity
      description: Marks the PartyReference entity as inactive or ...
      operationId: deletePartyReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - PartyReference
      summary: Partially update a PartyReference entity.
      operationId: updatePartyReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PartyReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/PartyReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: partyReferenceKey
      in: path
      description: The unique identifier for the specific PartyReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/staff-assignment-references:
    get:
      tags:
      - StaffAssignmentReference
      summary: Retrieve a list of StaffAssignmentReference ent...
      operationId: listStaffAssignmentReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of StaffAssignmentReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/StaffAssignmentReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - StaffAssignmentReference
      summary: Create a new StaffAssignmentReference entity.
      operationId: createStaffAssignmentReference
      requestBody:
        description: StaffAssignmentReference data to create the new...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceCreate"
        required: true
      responses:
        "201":
          description: StaffAssignmentReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/staff-assignment-references/{staffAssignmentReferenceKey}:
    get:
      tags:
      - StaffAssignmentReference
      summary: Retrieve a specific StaffAssignmentReference en...
      operationId: getStaffAssignmentReferenceById
      responses:
        "200":
          description: The requested StaffAssignmentReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - StaffAssignmentReference
      summary: Replace a StaffAssignmentReference entity.
      operationId: replaceStaffAssignmentReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - StaffAssignmentReference
      summary: Delete a StaffAssignmentReference entity
      description: Marks the StaffAssignmentReference entity as in...
      operationId: deleteStaffAssignmentReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - StaffAssignmentReference
      summary: Partially update a StaffAssignmentReference ent...
      operationId: updateStaffAssignmentReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: staffAssignmentReferenceKey
      in: path
      description: The unique identifier for the specific StaffAssignmentReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/shipment-events:
    get:
      tags:
      - ShipmentEvent
      summary: Retrieve a list of ShipmentEvent entities scope...
      operationId: listShipmentEventByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of ShipmentEvent entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ShipmentEvent"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - ShipmentEvent
      summary: Create a new ShipmentEvent entity.
      operationId: createShipmentEvent
      requestBody:
        description: ShipmentEvent data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEventCreate"
        required: true
      responses:
        "201":
          description: ShipmentEvent created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/shipment-events/{shipmentEventKey}:
    get:
      tags:
      - ShipmentEvent
      summary: Retrieve a specific ShipmentEvent entity.
      operationId: getShipmentEventById
      responses:
        "200":
          description: The requested ShipmentEvent entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - ShipmentEvent
      summary: Replace a ShipmentEvent entity.
      operationId: replaceShipmentEvent
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEvent"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - ShipmentEvent
      summary: Delete a ShipmentEvent entity
      description: Marks the ShipmentEvent entity as inactive or r...
      operationId: deleteShipmentEventEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - ShipmentEvent
      summary: Partially update a ShipmentEvent entity.
      operationId: updateShipmentEvent
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ShipmentEventUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: shipmentEventKey
      in: path
      description: The unique identifier for the specific ShipmentEvent.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/identifiers:
    get:
      tags:
      - Identifier
      summary: Retrieve a list of Identifier entities scoped b...
      operationId: listIdentifierByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Identifier entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Identifier"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Identifier
      summary: Create a new Identifier entity.
      operationId: createIdentifier
      requestBody:
        description: Identifier data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: Identifier created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/identifiers/{identifierKey}:
    get:
      tags:
      - Identifier
      summary: Retrieve a specific Identifier entity.
      operationId: getIdentifierById
      responses:
        "200":
          description: The requested Identifier entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - Identifier
      summary: Replace a Identifier entity.
      operationId: replaceIdentifier
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - Identifier
      summary: Delete a Identifier entity
      description: Marks the Identifier entity as inactive or remo...
      operationId: deleteIdentifierEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - Identifier
      summary: Partially update a Identifier entity.
      operationId: updateIdentifier
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: identifierKey
      in: path
      description: The unique identifier for the specific Identifier.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/prices:
    get:
      tags:
      - Price
      summary: Retrieve a list of Price entities scoped by rec...
      operationId: listPriceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Price entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Price"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Price
      summary: Create a new Price entity.
      operationId: createPrice
      requestBody:
        description: Price data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceCreate"
        required: true
      responses:
        "201":
          description: Price created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/prices/{priceKey}:
    get:
      tags:
      - Price
      summary: Retrieve a specific Price entity.
      operationId: getPriceById
      responses:
        "200":
          description: The requested Price entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - Price
      summary: Replace a Price entity.
      operationId: replacePrice
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Price"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - Price
      summary: Delete a Price entity
      description: Marks the Price entity as inactive or removes i...
      operationId: deletePriceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - Price
      summary: Partially update a Price entity.
      operationId: updatePrice
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PriceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: priceKey
      in: path
      description: The unique identifier for the specific Price.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/purchase-order-references:
    get:
      tags:
      - PurchaseOrderReference
      summary: Retrieve a list of PurchaseOrderReference entit...
      operationId: listPurchaseOrderReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PurchaseOrderReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PurchaseOrderReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PurchaseOrderReference
      summary: Create a new PurchaseOrderReference entity.
      operationId: createPurchaseOrderReference
      requestBody:
        description: PurchaseOrderReference data to create the new r...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReferenceCreate"
        required: true
      responses:
        "201":
          description: PurchaseOrderReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/purchase-order-references/{purchaseOrderReferenceKey}:
    get:
      tags:
      - PurchaseOrderReference
      summary: Retrieve a specific PurchaseOrderReference entity.
      operationId: getPurchaseOrderReferenceById
      responses:
        "200":
          description: The requested PurchaseOrderReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - PurchaseOrderReference
      summary: Replace a PurchaseOrderReference entity.
      operationId: replacePurchaseOrderReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - PurchaseOrderReference
      summary: Delete a PurchaseOrderReference entity
      description: Marks the PurchaseOrderReference entity as inac...
      operationId: deletePurchaseOrderReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - PurchaseOrderReference
      summary: Partially update a PurchaseOrderReference entity.
      operationId: updatePurchaseOrderReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: purchaseOrderReferenceKey
      in: path
      description: The unique identifier for the specific PurchaseOrderReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/receipt-line-items:
    get:
      tags:
      - ReceiptLineItem
      summary: Retrieve a list of ReceiptLineItem entities sco...
      operationId: listReceiptLineItemByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of ReceiptLineItem entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReceiptLineItem"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - ReceiptLineItem
      summary: Create a new ReceiptLineItem entity.
      operationId: createReceiptLineItem
      requestBody:
        description: ReceiptLineItem data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItemCreate"
        required: true
      responses:
        "201":
          description: ReceiptLineItem created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/receipt-line-items/{receiptLineItemKey}:
    get:
      tags:
      - ReceiptLineItem
      summary: Retrieve a specific ReceiptLineItem entity.
      operationId: getReceiptLineItemById
      responses:
        "200":
          description: The requested ReceiptLineItem entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - ReceiptLineItem
      summary: Replace a ReceiptLineItem entity.
      operationId: replaceReceiptLineItem
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItem"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - ReceiptLineItem
      summary: Delete a ReceiptLineItem entity
      description: Marks the ReceiptLineItem entity as inactive or...
      operationId: deleteReceiptLineItemEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - ReceiptLineItem
      summary: Partially update a ReceiptLineItem entity.
      operationId: updateReceiptLineItem
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItemUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: receiptLineItemKey
      in: path
      description: The unique identifier for the specific ReceiptLineItem.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/part-references:
    get:
      tags:
      - PartReference
      summary: Retrieve a list of PartReference entities scope...
      operationId: listPartReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PartReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PartReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PartReference
      summary: Create a new PartReference entity.
      operationId: createPartReference
      requestBody:
        description: PartReference data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReferenceCreate"
        required: true
      responses:
        "201":
          description: PartReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/part-references/{partReferenceKey}:
    get:
      tags:
      - PartReference
      summary: Retrieve a specific PartReference entity.
      operationId: getPartReferenceById
      responses:
        "200":
          description: The requested PartReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - PartReference
      summary: Replace a PartReference entity.
      operationId: replacePartReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - PartReference
      summary: Delete a PartReference entity
      description: Marks the PartReference entity as inactive or r...
      operationId: deletePartReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - PartReference
      summary: Partially update a PartReference entity.
      operationId: updatePartReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PartReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: partReferenceKey
      in: path
      description: The unique identifier for the specific PartReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/effective-periods:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a list of EffectivePeriod entities sco...
      operationId: listEffectivePeriodByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of EffectivePeriod entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EffectivePeriod"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a specific EffectivePeriod entity.
      operationId: getEffectivePeriodById
      responses:
        "200":
          description: The requested EffectivePeriod entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - EffectivePeriod
      summary: Replace a EffectivePeriod entity.
      operationId: replaceEffectivePeriod
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: effectivePeriodKey
      in: path
      description: The unique identifier for the specific EffectivePeriod.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/lifecycle-events:
    get:
      tags:
      - LifecycleEvent
      summary: Retrieve a list of LifecycleEvent entities scop...
      operationId: listLifecycleEventByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of LifecycleEvent entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/LifecycleEvent"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - LifecycleEvent
      summary: Create a new LifecycleEvent entity.
      operationId: createLifecycleEvent
      requestBody:
        description: LifecycleEvent data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifecycleEventCreate"
        required: true
      responses:
        "201":
          description: LifecycleEvent created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/lifecycle-events/{lifecycleEventKey}:
    get:
      tags:
      - LifecycleEvent
      summary: Retrieve a specific LifecycleEvent entity.
      operationId: getLifecycleEventById
      responses:
        "200":
          description: The requested LifecycleEvent entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - LifecycleEvent
      summary: Replace a LifecycleEvent entity.
      operationId: replaceLifecycleEvent
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifecycleEvent"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - LifecycleEvent
      summary: Delete a LifecycleEvent entity
      description: Marks the LifecycleEvent entity as inactive or ...
      operationId: deleteLifecycleEventEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - LifecycleEvent
      summary: Partially update a LifecycleEvent entity.
      operationId: updateLifecycleEvent
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/LifecycleEventUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: lifecycleEventKey
      in: path
      description: The unique identifier for the specific LifecycleEvent.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/warehouse-references:
    get:
      tags:
      - WarehouseReference
      summary: Retrieve a list of WarehouseReference entities ...
      operationId: listWarehouseReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of WarehouseReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/WarehouseReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - WarehouseReference
      summary: Create a new WarehouseReference entity.
      operationId: createWarehouseReference
      requestBody:
        description: WarehouseReference data to create the new resou...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReferenceCreate"
        required: true
      responses:
        "201":
          description: WarehouseReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/warehouse-references/{warehouseReferenceKey}:
    get:
      tags:
      - WarehouseReference
      summary: Retrieve a specific WarehouseReference entity.
      operationId: getWarehouseReferenceById
      responses:
        "200":
          description: The requested WarehouseReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - WarehouseReference
      summary: Replace a WarehouseReference entity.
      operationId: replaceWarehouseReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - WarehouseReference
      summary: Delete a WarehouseReference entity
      description: Marks the WarehouseReference entity as inactive...
      operationId: deleteWarehouseReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - WarehouseReference
      summary: Partially update a WarehouseReference entity.
      operationId: updateWarehouseReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/WarehouseReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: warehouseReferenceKey
      in: path
      description: The unique identifier for the specific WarehouseReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/{receiptKey}/financial-track-references:
    get:
      tags:
      - FinancialTrackReference
      summary: Retrieve a list of FinancialTrackReference enti...
      operationId: listFinancialTrackReferenceByReceiptKey
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of FinancialTrackReference entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FinancialTrackReference"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - FinancialTrackReference
      summary: Create a new FinancialTrackReference entity.
      operationId: createFinancialTrackReference
      requestBody:
        description: FinancialTrackReference data to create the new ...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceCreate"
        required: true
      responses:
        "201":
          description: FinancialTrackReference created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /receipts/{receiptKey}/financial-track-references/{financialTrackReferenceKey}:
    get:
      tags:
      - FinancialTrackReference
      summary: Retrieve a specific FinancialTrackReference ent...
      operationId: getFinancialTrackReferenceById
      responses:
        "200":
          description: The requested FinancialTrackReference entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - FinancialTrackReference
      summary: Replace a FinancialTrackReference entity.
      operationId: replaceFinancialTrackReference
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReference"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - FinancialTrackReference
      summary: Delete a FinancialTrackReference entity
      description: Marks the FinancialTrackReference entity as ina...
      operationId: deleteFinancialTrackReferenceEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - FinancialTrackReference
      summary: Partially update a FinancialTrackReference entity.
      operationId: updateFinancialTrackReference
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: receiptKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: financialTrackReferenceKey
      in: path
      description: The unique identifier for the specific FinancialTrackReference.
      required: true
      schema:
        type: string
        example: PR-1234567
  /receipts/health:
    get:
      summary: Health Check
      description: Public endpoint to verify service availability.
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                required:
                - status
                - version
                type: object
                properties:
                  status:
                    type: string
                    example: up
                    enum:
                    - up
                    - down
                    - maintenance
                  version:
                    type: string
                    description: The version of the ontology implemented by this service.
                    example: 1.0.0
                  timestamp:
                    type: string
                    description: Server time in ISO-8601 format.
                    format: date-time
                    example: 2025-12-21T23:00:00Z
      security:
      - {}
  /receipts/batch:
    post:
      summary: Bulk create receipts
      operationId: createReceiptBatch
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  maxItems: 100
                  type: array
                  items:
                    $ref: "#/components/schemas/ReceiptCreate"
      responses:
        "200":
          description: Batch processing complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  succeeded:
                    type: array
                    items:
                      $ref: "#/components/schemas/Receipt"
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        index:
                          type: integer
                          format: int32
                        error:
                          $ref: "#/components/schemas/ErrorDetail"
components:
  schemas:
    AddressReference:
      type: object
      properties:
        address_reference_key:
          type: string
          description: A unique key for the address reference.
        address_reference_id:
          type: string
          description: A unique key for the address reference.
        address_key:
          type: string
          description: The unique value of the address key.
        address_reference_value:
          type: string
          description: The unique value of the address referenc...
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/AddressTypes"
        address_line1:
          maxLength: 100
          minLength: 1
          type: string
          description: "Primary address line (Street, PO Box, et..."
        address_line2:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line3:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line4:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line5:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        city:
          maxLength: 100
          minLength: 1
          type: string
          description: Name of the city.
        country_code:
          maxLength: 100
          minLength: 2
          type: string
          description: Full name of the country.
        district:
          maxLength: 100
          type: string
          description: Name of the district.
        label:
          maxLength: 50
          type: string
          description: Label or display name for the address.
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: string
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
        state_province:
          maxLength: 50
          type: string
          description: State or equivalent subdivision.
      description: "A reference to an address, including its unique..."
    AddressReferenceCreate:
      required:
      - address_reference_key
      type: object
      description: AddressReference Create for Post
      allOf:
      - $ref: "#/components/schemas/AddressReference"
    AddressReferenceUpdate:
      type: object
      properties:
        address_reference_value:
          type: string
          description: The unique value of the address referenc...
        city:
          maxLength: 100
          minLength: 1
          type: string
          description: Name of the city.
        state_province:
          maxLength: 50
          type: string
          description: State or equivalent subdivision.
        label:
          maxLength: 50
          type: string
          description: Label or display name for the address.
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/AddressTypes"
        address_line5:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        country_code:
          maxLength: 100
          minLength: 2
          type: string
          description: Full name of the country.
        address_line4:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line3:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line2:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line1:
          maxLength: 100
          minLength: 1
          type: string
          description: "Primary address line (Street, PO Box, et..."
        district:
          maxLength: 100
          type: string
          description: Name of the district.
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: string
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
      description: AddressReference Update for Patch and Put
    AddressTypes:
      type: string
      description: types of address.
      enum:
      - BILLING
      - SHIPPING
      - MAILING
      - POSTAL
      - HOME
      - RESIDENCE
      - WORK
      - BUSINESS
      - LEGAL
      - SERVICE
      - DEALER
      - CONTRACT
      - AR_ADDRESS
      - RECIPIENT
      - DELIVERY
      - DESTINATION
      - ORIGINAL
      - CURRENT
      - PREVIOUS
      - TEMPORARY
      - VACATION
      - GARAGE
      - EMERGENCY
      - REBATE
      - SERVICE_CONTRACT
      - INSURANCE
      - PRODUCT
      - ARCHIVE
      - SOLD_BY
      - SHIPPED_BY
      - OTHER
    Codes:
      type: array
      items:
        type: string
    DaysOfWeekTypes:
      type: string
      description: Status of the account
      enum:
      - SUN
      - MON
      - TUES
      - WED
      - THURS
      - FRI
      - SAT
    DurationUOMTypes:
      type: string
      description: types of duration u o ms.
      enum:
      - MINUTES
      - HOURS
      - DAYS
      - DAY_OF_MONTH
      - WEEKS
      - BI_WEEKS
      - SEMI_MONTHS
      - MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - CUSTOM
      - OCCURRENCE
      - UNLIMITED
      - NONE
      - OTHER
    EffectivePeriod:
      type: object
      properties:
        effective_period_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key identifying the effective per... Pat...
        active_indicator:
          type: boolean
          description: Indicator if the period is currently act...
        exclusive_indicator:
          type: boolean
          description: Indicator if the period is exclusive
        start_date_time:
          type: string
          description: Start date and time of the effective per...
          format: date-time
        complete_date_time:
          type: string
          description: Completion date and time of the effectiv...
          format: date-time
        duration_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        duration_value:
          type: string
          description: Value of the duration
        recur_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        recur_count:
          type: string
          description: Count of recurrence
        recur_values:
          $ref: "#/components/schemas/Codes"
        recur_criteria:
          type: string
          description: Criteria defining recurrence
        recur_indicator:
          type: boolean
          description: Indicator if recurrence is enabled
        timeslot:
          $ref: "#/components/schemas/TimeSlot"
      description: Effective Period
    ErrorDetail:
      required:
      - code
      - message
      - timestamp
      type: object
      properties:
        code:
          type: string
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: NOT_FOUND_PR
        message:
          type: string
          description: A human-readable summary providing specific det...
          example: Personnel Record with ID PR-1234567 was not found.
        details:
          type: array
          description: Optional array of specific field-level validati...
          items:
            type: object
            properties:
              field:
                type: string
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: payrollId
              issue:
                type: string
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: must be unique
        timestamp:
          type: string
          description: "The time the error occurred, in ISO 8601 format."
          format: date-time
          example: 2025-11-10T10:30:00Z
      description: Standard format for all API error responses.
    FinancialTrackReference:
      type: object
      properties:
        track_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier value for the financia... Pat...
        track_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the financial track... Pat...
        track_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: The unique key identifying the reference... Pat...
        context:
          type: string
          description: Context or domain of the financial track...
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the financial track en...
          format: date-time
      description: Reference to a Financial Track Aggregate Root (...
    FinancialTrackReferenceCreate:
      required:
      - financial_track_reference_key
      type: object
      description: FinancialTrackReference Create for Post
      allOf:
      - $ref: "#/components/schemas/FinancialTrackReference"
    FinancialTrackReferenceUpdate:
      type: object
      properties:
        identifier:
          $ref: "#/components/schemas/Identifier"
        context:
          type: string
          description: Context or domain of the financial track...
        occurrence_date_time:
          type: string
          description: The date and time the financial track en...
          format: date-time
      description: FinancialTrackReference Update for Patch and Put
    FinancialTrackReferences:
      type: array
      items:
        $ref: "#/components/schemas/FinancialTrackReference"
    Identifier:
      type: object
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: string
          description: Context or domain of the identifier
        value:
          type: string
          description: The monetary or hours-based value of the...
        detail:
          type: string
          description: The monetary or hours-based value of the...
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: External system identifier
    IdentifierCreate:
      required:
      - identifier_key
      type: object
      description: Identifier Create for Post
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        context:
          type: string
          description: Context or domain of the identifier
        detail:
          type: string
          description: The monetary or hours-based value of the...
        value:
          type: string
          description: The monetary or hours-based value of the...
      description: Identifier Update for Patch and Put
    Identifiers:
      type: array
      items:
        $ref: "#/components/schemas/Identifier"
    LifecycleEvent:
      type: object
      properties:
        lifecycle_event_id:
          type: string
          description: The unique ID for the lifecycle event.
          example: EVT-99887766
        lifecycle_event_key:
          type: string
          description: The unique external key or business iden...
          example: LIFECYCLE-879234
        actor_href:
          type: string
          description: The href of the actor who initiated the ...
          format: href
        actor_id:
          type: string
          description: The unique identifier of the actor who i...
          example: USR-24681357
        actor_name:
          type: string
          description: The name of the actor who initiated the ...
          example: John Doe
        lifecycle_event_type:
          $ref: "#/components/schemas/LifecycleEventTypes"
        occurrence_date:
          type: string
          description: The date and time the event occurred.
          format: date
        resource_href:
          type: string
          description: The href of the resource associated with...
          format: href
        resource_identifier:
          type: string
          description: The unique identifier of the resource as...
          example: RSC-88776655
        detail_name:
          type: string
          description: The name of the event detail.
          example: Order ID
        detail_desc:
          type: string
          description: A description of the event detail.
          example: The unique ID of the order being processed.
        detail_value:
          type: string
          description: The value of the event detail.
          example: ORD-44556677
        detail_key:
          type: string
          description: The unique key for the event detail.
          example: ORDER_ID
        detail_code:
          type: string
          description: The code for the event detail.
          example: ORDER_STATUS_CHANGED
        resource_types:
          $ref: "#/components/schemas/ResourceTypes"
      description: Lifecycle Event
    LifecycleEventCreate:
      required:
      - lifecycle_event_key
      type: object
      description: LifecycleEvent Create for Post
      allOf:
      - $ref: "#/components/schemas/LifecycleEvent"
    LifecycleEventTypes:
      type: string
      description: types of lifecycle events.
      enum:
      - CREATED
      - UPDATED
      - DELETE_HARD
      - DELETE_SOFT
      - ACCESS_ALLOWED
      - ACCESS_DENIED
      - SUBMITTED
      - CLOSED
      - CANCELLED
      - DEAD
      - INITIALIZED
      - INITIALIZING
      - PROCESSING
      - PROCESSED
      - FAILED
      - VALIDATED
      - QUALIFIED
    LifecycleEventUpdate:
      type: object
      properties:
        occurrence_date:
          type: string
          description: The date and time the event occurred.
          format: date
        resource_href:
          type: string
          description: The href of the resource associated with...
          format: href
        detail_name:
          type: string
          description: The name of the event detail.
          example: Order ID
        lifecycle_event_type:
          $ref: "#/components/schemas/LifecycleEventTypes"
        resource_identifier:
          type: string
          description: The unique identifier of the resource as...
          example: RSC-88776655
        actor_name:
          type: string
          description: The name of the actor who initiated the ...
          example: John Doe
        detail_desc:
          type: string
          description: A description of the event detail.
          example: The unique ID of the order being processed.
        detail_value:
          type: string
          description: The value of the event detail.
          example: ORD-44556677
        detail_code:
          type: string
          description: The code for the event detail.
          example: ORDER_STATUS_CHANGED
        actor_href:
          type: string
          description: The href of the actor who initiated the ...
          format: href
        resource_types:
          $ref: "#/components/schemas/ResourceTypes"
      description: LifecycleEvent Update for Patch and Put
    LifecycleEvents:
      type: array
      items:
        $ref: "#/components/schemas/LifecycleEvent"
    OrderTypes:
      type: string
      description: types of orders.
      enum:
      - STANDARD
      - SALES
      - PURCHASE
      - RUSH
      - BACK_ORDER
      - SPECIAL
      - SUBSCRIPTION
      - SERVICE
      - MAINTENANCE
      - WARRANTY
      - SAMPLE
      - INTERNAL_TRANSFER
      - CONSIGNMENT
      - TRIAL
      - RETURN
    PartReference:
      type: object
      properties:
        part_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        part_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        name:
          type: string
          description: Product Name.
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        part_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        part_master_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: string
          description: Context or domain of the identifier
        product_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ProductTypes"
        system_codes:
          $ref: "#/components/schemas/Codes"
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
      description: External system identifier
    PartReferenceCreate:
      required:
      - part_reference_key
      type: object
      description: PartReference Create for Post
      allOf:
      - $ref: "#/components/schemas/PartReference"
    PartReferenceUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        product_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ProductTypes"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        name:
          type: string
          description: Product Name.
        context:
          type: string
          description: Context or domain of the identifier
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
      description: PartReference Update for Patch and Put
    PartReferences:
      type: array
      items:
        $ref: "#/components/schemas/PartReference"
    PartyReference:
      type: object
      properties:
        party_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_reference_type:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: string
          description: Context or domain of the identifier
        party_relationship_types:
          pattern: "^[a-zA-Z0-9_]+$"
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PartyRelationshipTypes"
        system_codes:
          $ref: "#/components/schemas/Codes"
        party:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
      description: External system identifier
    PartyReferenceCreate:
      required:
      - party_reference_key
      type: object
      description: PartyReference Create for Post
      allOf:
      - $ref: "#/components/schemas/PartyReference"
    PartyReferenceUpdate:
      type: object
      properties:
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
        system_codes:
          $ref: "#/components/schemas/Codes"
        context:
          type: string
          description: Context or domain of the identifier
        party_reference_type:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        party_relationship_types:
          pattern: "^[a-zA-Z0-9_]+$"
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PartyRelationshipTypes"
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        party:
          $ref: "#/components/schemas/Identifier"
      description: PartyReference Update for Patch and Put
    PartyRelationshipTypes:
      type: string
      description: types of party relationships.
      enum:
      - CORPORATION
      - GROUP
      - STORE
      - BRANCH
      - DEPARTMENT
      - SECTION
      - TEAM
      - DEALERSHIP
      - THIRD_PARTY
      - VENDOR
      - SUPPLIER
      - PARTNER_ORGANIZATION
      - AFFILIATE
      - DISTRIBUTOR
      - MANUFACTURER
      - SERVICE_PROVIDER
      - EMPLOYER
      - INTERNAL_ORGANIZATION
      - COST_CENTER
      - FREIGHT_FORWARDER
      - BUYER
      - REGULATOR
      - GOVERNMENT_AGENCY
      - NON_PROFIT
      - FRANCHISE
      - SUBSIDIARY
      - DIVISION
      - JOINT_VENTURE
      - AUDITOR
      - ACTOR
      - ACCOUNTABLE
      - RESPONSIBLE
      - CONSULT
      - NOTIFY
      - EXECUTE
      - PART_OF
      - MEMBERSHIP
      - COMPOSED_IN
      - CUSTOMER
      - CO_SIGNER
      - SPOUSE
      - PARENT
      - CHILD
      - SIBLING
      - LEGAL_GUARDIAN
      - DEPENDENT
      - EMERGENCY_CONTACT
      - FAMILY_MEMBER
      - FRIEND
      - ACQUAINTANCE
      - ASSOCIATE
      - EX_SPOUSE
      - PROFESSIONAL_CONTACT
      - MANAGER
      - CO_WORKER
      - FINANCIAL_ADVISOR
      - SELF
      - ROMANTIC_PARTNER
      - OTHER_FAMILY
      - CAREGIVER
      - MENTOR
      - MENTEE
      - HEIR
      - TRUSTEE_PERSON
      - REFERENCE_PERSONAL
      - REFERENCE_PROFESSIONAL
      - AGENT
      - BENEFICIARY
      - GUARANTOR
      - EMPLOYEE
      - CONTRACTOR
      - OWNER
      - BUSINESS_PARTNER
      - SERVICE_CONSUMER
      - POLITICAL_AFFILIATE
      - ADVISOR_CONSULTANT
      - PROXY
      - SHAREHOLDER
      - INVESTOR
      - CUSTODIAN
      - SOLD_TO
      - SHIP_TO
      - PAYER
      - PAYEE
      - GIFT_TO
      - LOAN_PROVIDER
      - CREDIT_CARD_PROVIDER
      - LESSEE
      - LESSOR
      - INSURED
      - INSURER
      - UNDERWRITER
      - RECIPIENT
      - TRANSACTION_FROM
      - TRANSACTION_TO
      - REMOVE
      - OTHER
    Position:
      type: object
      properties:
        position_id:
          type: string
          description: Unique identifier for this position
          example: pos-001
        position_key:
          type: string
          description: Business key for this position record
          example: POS-ATL-WEST
        latitude:
          type: string
          description: Latitude of the location
          example: "33.7490"
        longitude:
          type: string
          description: Longitude of the location
          example: "-84.3880"
        altitude:
          type: string
          description: Altitude of the location
          example: "320"
        bearing:
          type: string
          description: Bearing direction (compass)
          example: NW
        postal_code:
          type: string
          description: Postal code of the location
          example: "30303"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
      description: "position meta-data that includes postal code, a..."
    PositionCreate:
      required:
      - position_key
      type: object
      description: Position Create for Post
      allOf:
      - $ref: "#/components/schemas/Position"
    PositionUpdate:
      type: object
      properties:
        altitude:
          type: string
          description: Altitude of the location
          example: "320"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        bearing:
          type: string
          description: Bearing direction (compass)
          example: NW
        latitude:
          type: string
          description: Latitude of the location
          example: "33.7490"
        postal_code:
          type: string
          description: Postal code of the location
          example: "30303"
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        longitude:
          type: string
          description: Longitude of the location
          example: "-84.3880"
      description: Position Update for Patch and Put
    Price:
      type: object
      properties:
        display_value:
          type: string
          description: Human-readable display value for the pri...
        locale:
          type: string
          description: Locale used for formatting display value...
        value:
          type: string
          description: The numeric value of the price (before t...
        tax_value:
          type: string
          description: The calculated amount of tax applied to ...
        taxable_amount:
          type: string
          description: The portion of the price subject to tax.
        non_taxable_amount:
          type: string
          description: The portion of the price not subject to ...
        total_amount:
          type: string
          description: The total final price including all comp...
        total_cost_amount:
          type: string
          description: The total cost to the provider/seller.
        unit_cost_amount:
          type: string
          description: The unit cost to the provider/seller.
        price_description:
          type: string
          description: "A descriptive title for the price (e.g.,..."
        price_key:
          type: string
          description: Natural key or business identifier for t...
        tax_included_indicator:
          type: boolean
          description: Indicates whether tax is included in the...
        currency_code:
          type: string
          description: "ISO 4217 currency code, e.g., 'USD', 'EU..."
        price_id:
          type: string
          description: Unique system identifier for the price r...
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
        price_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PriceTypes"
      description: "Price that includes display value, currency, lo..."
    PriceCreate:
      required:
      - price_key
      type: object
      description: Price Create for Post
      allOf:
      - $ref: "#/components/schemas/Price"
    PriceTypes:
      type: string
      description: types of prices.
      enum:
      - METERED
      - SEAT
      - FLAT
      - ITEMIZED
      - BUNDLE
      - QUANTITY
      - PER_UNIT
      - PERCENT
      - VOLUME
      - USAGE
      - STAIR_STEP
      - SUBSCRIPTION
      - CUSTOM
      - STANDARD
      - LICENSED
      - TIER_VOLUME
      - TIER_GRADUATED
      - ACTUAL
      - LIST
      - RETAIL
      - MSRP
      - WHOLESALE
      - DEALER
      - TRADE
      - CORE
      - LANDED
      - PROMOTIONAL
      - DISCOUNT
      - CONTRACT
      - SALE
      - CUSTOMER
      - AFFILIATE
      - ONLINE
      - STORE
      - REGION
      - LOCATION
      - ESTIMATED
      - NET
      - PURCHASE
      - MONEY
      - TAXABLE
      - NONTAXABLE
      - TOTAL
      - UNIT
      - TOTAL_COST
      - UNIT_COST
      - TAX
      - OTHER
    PriceUpdate:
      type: object
      properties:
        display_value:
          type: string
          description: Human-readable display value for the pri...
        unit_cost_amount:
          type: string
          description: The unit cost to the provider/seller.
        locale:
          type: string
          description: Locale used for formatting display value...
        currency_code:
          type: string
          description: "ISO 4217 currency code, e.g., 'USD', 'EU..."
        duration:
          $ref: "#/components/schemas/EffectivePeriod"
        tax_value:
          type: string
          description: The calculated amount of tax applied to ...
        total_amount:
          type: string
          description: The total final price including all comp...
        total_cost_amount:
          type: string
          description: The total cost to the provider/seller.
        price_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PriceTypes"
        tax_included_indicator:
          type: boolean
          description: Indicates whether tax is included in the...
        price_description:
          type: string
          description: "A descriptive title for the price (e.g.,..."
        non_taxable_amount:
          type: string
          description: The portion of the price not subject to ...
        value:
          type: string
          description: The numeric value of the price (before t...
        taxable_amount:
          type: string
          description: The portion of the price subject to tax.
      description: Price Update for Patch and Put
    ProductTypes:
      type: string
      description: types of products.
      enum:
      - VEHICLE
      - INFORMATION
      - DURABLE_GOODS
      - SOFTWARE
      - APP
      - INSURANCE
      - FINANCE
      - SERVICE
      - PART
      - ACCESSORY
      - F_N_I_PRODUCT
      - SERVICE_PACKAGE
      - DEALER_PRODUCT
      - EQUIPMENT
      - PACKAGE_GOODS
      - SERVICE_PLAN
      - WARRANTY_PLAN
      - SUBSCRIPTION
      - OTHER
    PurchaseOrderReference:
      type: object
      properties:
        order_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier value for the purchase... Pat...
        order_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the purchase order ... Pat...
        purchase_order_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: The unique key identifying the reference... Pat...
        context:
          type: string
          description: Context or domain of the purchase order ...
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the purchase order was...
          format: date-time
      description: Reference to a Purchase Order Aggregate Root.
    PurchaseOrderReferenceCreate:
      required:
      - purchase_order_reference_key
      type: object
      description: PurchaseOrderReference Create for Post
      allOf:
      - $ref: "#/components/schemas/PurchaseOrderReference"
    PurchaseOrderReferenceUpdate:
      type: object
      properties:
        identifier:
          $ref: "#/components/schemas/Identifier"
        context:
          type: string
          description: Context or domain of the purchase order ...
        occurrence_date_time:
          type: string
          description: The date and time the purchase order was...
          format: date-time
      description: PurchaseOrderReference Update for Patch and Put
    Receipt:
      type: object
      properties:
        receipt_key:
          maxLength: 50
          minLength: 1
          type: string
          description: A unique key for the receipt record.
          example: RCPT-100045
        receipt_id:
          maxLength: 50
          minLength: 1
          type: string
          description: A unique identifier for the receipt.
          example: RCPT-100045
        purchase_order_reference:
          $ref: "#/components/schemas/PurchaseOrderReference"
        supplier_reference:
          $ref: "#/components/schemas/PartyReference"
        receiving_staff_reference:
          $ref: "#/components/schemas/StaffAssignmentReference"
        receipt_status_type:
          $ref: "#/components/schemas/ReceiptStatusTypes"
        is_fully_received:
          type: boolean
          description: Indicates if all expected items for this...
          example: "true"
        currency:
          type: string
          description: The currency code for the entire purchas...
          example: USD
        delivery_location:
          $ref: "#/components/schemas/AddressReference"
        total_quantity:
          type: string
          description: The total quantity of all parts received...
          example: "25"
        total_value:
          $ref: "#/components/schemas/Price"
        warehouse_reference:
          $ref: "#/components/schemas/WarehouseReference"
        notes:
          maxLength: 500
          type: string
          description: Any notes related to the receipt.
          example: Damaged packaging observed on arrival.
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        lifecycle_events:
          $ref: "#/components/schemas/LifecycleEvents"
        shipment_events:
          $ref: "#/components/schemas/ShipmentEvents"
        shipments:
          $ref: "#/components/schemas/ShipmentReferences"
        lineitems:
          $ref: "#/components/schemas/ReceiptLineItems"
        status:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the reference recor... Pat...
      description: Represents a single goods receipt for items rec...
    ReceiptCreate:
      required:
      - receipt_key
      type: object
      description: Receipt Create for Post
      allOf:
      - $ref: "#/components/schemas/Receipt"
    ReceiptLineItem:
      type: object
      properties:
        receipt_line_item_id:
          maxLength: 50
          minLength: 1
          type: string
          description: Unique identifier for the receipt.
          example: RCPT-100045
        receipt_line_item_key:
          maxLength: 50
          minLength: 1
          type: string
          description: Unique identifier for the receipt.
          example: RCPT-100045
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        financial_tracks:
          $ref: "#/components/schemas/FinancialTrackReferences"
        lifecycle_events:
          $ref: "#/components/schemas/LifecycleEvents"
        received_parts:
          $ref: "#/components/schemas/PartReferences"
        delivered_quantity:
          type: string
          description: Total quantity of all parts received in ...
          example: "25"
        notes:
          maxLength: 500
          type: string
          description: Any notes related to the receipt.
          example: Damaged packaging observed on arrival.
        receiving_staff_reference:
          $ref: "#/components/schemas/StaffAssignmentReference"
        supplier_reference:
          $ref: "#/components/schemas/PartyReference"
        receipt_status_type:
          $ref: "#/components/schemas/ReceiptStatusTypes"
        events:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ShipmentEvent"
        order_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/OrderTypes"
        journal_entry_key:
          type: string
          description: A link to the corresponding financial jo...
        status:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the reference recor... Pat...
      description: receipt.line.item.desc
    ReceiptLineItemCreate:
      required:
      - receipt_line_item_key
      type: object
      description: ReceiptLineItem Create for Post
      allOf:
      - $ref: "#/components/schemas/ReceiptLineItem"
    ReceiptLineItemUpdate:
      type: object
      properties:
        notes:
          maxLength: 500
          type: string
          description: Any notes related to the receipt.
          example: Damaged packaging observed on arrival.
        receiving_staff_reference:
          $ref: "#/components/schemas/StaffAssignmentReference"
        received_parts:
          $ref: "#/components/schemas/PartReferences"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        order_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/OrderTypes"
        financial_tracks:
          $ref: "#/components/schemas/FinancialTrackReferences"
        lifecycle_events:
          $ref: "#/components/schemas/LifecycleEvents"
        supplier_reference:
          $ref: "#/components/schemas/PartyReference"
        receipt_status_type:
          $ref: "#/components/schemas/ReceiptStatusTypes"
        events:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ShipmentEvent"
        delivered_quantity:
          type: string
          description: Total quantity of all parts received in ...
          example: "25"
        status:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the reference recor... Pat...
      description: ReceiptLineItem Update for Patch and Put
    ReceiptLineItems:
      type: array
      items:
        $ref: "#/components/schemas/ReceiptLineItem"
    ReceiptStatusTypes:
      type: string
      description: types of receipt status.
      enum:
      - DRAFT
      - PENDING_APPROVAL
      - CANCELLED
      - CLOSED
      - PARTIALLY_RECEIVED
      - FULLY_RECEIVED
      - PARTIALLY_SHIPPED
      - SHIPPED
      - DELIVERED
      - ON_HOLD
      - SALES_HOLD
      - PAYMENT_HOLD
      - FINANCE_HOLD
      - CUSTOMER_HOLD
      - INTERNAL_HOLD
    ReceiptUpdate:
      type: object
      properties:
        total_value:
          $ref: "#/components/schemas/Price"
        notes:
          maxLength: 500
          type: string
          description: Any notes related to the receipt.
          example: Damaged packaging observed on arrival.
        receiving_staff_reference:
          $ref: "#/components/schemas/StaffAssignmentReference"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        supplier_reference:
          $ref: "#/components/schemas/PartyReference"
        delivery_location:
          $ref: "#/components/schemas/AddressReference"
        shipments:
          $ref: "#/components/schemas/ShipmentReferences"
        warehouse_reference:
          $ref: "#/components/schemas/WarehouseReference"
        lineitems:
          $ref: "#/components/schemas/ReceiptLineItems"
        purchase_order_reference:
          $ref: "#/components/schemas/PurchaseOrderReference"
        total_quantity:
          type: string
          description: The total quantity of all parts received...
          example: "25"
        lifecycle_events:
          $ref: "#/components/schemas/LifecycleEvents"
        currency:
          type: string
          description: The currency code for the entire purchas...
          example: USD
        shipment_events:
          $ref: "#/components/schemas/ShipmentEvents"
        receipt_status_type:
          $ref: "#/components/schemas/ReceiptStatusTypes"
        is_fully_received:
          type: boolean
          description: Indicates if all expected items for this...
          example: "true"
        status:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the reference recor... Pat...
      description: Receipt Update for Patch and Put
    ResourceTypes:
      type: string
      description: types of resources.
      enum:
      - SYSTEM
      - WORKFLOW
      - ACTIVITY
      - FUNCTION
      - PROCESS
      - INCIDENT
      - PARTY
      - CUSTOMER
      - APPLICANT
      - VENDOR
      - DEALER_STAFF
      - DRIVER
      - ADDRESS
      - BILL_ADDRESS
      - BILL_CONTACT
      - COMM_CHANNEL
      - ACCOUNT
      - VEHICLE
      - TRADE_IN
      - PRODUCT
      - MANUFACTURER
      - LEAD
      - OPPORTUNITY
      - DEAL
      - FINANCE
      - INVOICE
      - LINEITEM
      - EXPENSE
      - DISCOUNT
      - TAX
      - FEE
      - INCENTIVE
      - DEALERSHIP
      - APPOINTMENT
      - OTHER
    ShipmentEvent:
      type: object
      properties:
        shipment_event_id:
          type: string
          description: Unique identifier for this specific part...
          example: pii_abc123xyz
        shipment_event_key:
          type: string
          description: A unique key for the parts invoice line ...
          example: PARTINVITEM-2023-001-A
        location:
          $ref: "#/components/schemas/Position"
        description:
          type: string
          description: A description of the event.
        occurrence_date_time:
          type: string
          description: The date and time the event physically o...
          format: date-time
        effective_date_time:
          type: string
          description: The date and time the event's status or ...
          format: date-time
        shipment_state_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ShipmentStateTypes"
      description: not nullable
    ShipmentEventCreate:
      required:
      - shipment_event_key
      type: object
      description: ShipmentEvent Create for Post
      allOf:
      - $ref: "#/components/schemas/ShipmentEvent"
    ShipmentEventUpdate:
      type: object
      properties:
        effective_date_time:
          type: string
          description: The date and time the event's status or ...
          format: date-time
        description:
          type: string
          description: A description of the event.
        location:
          $ref: "#/components/schemas/Position"
        shipment_state_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/ShipmentStateTypes"
        occurrence_date_time:
          type: string
          description: The date and time the event physically o...
          format: date-time
      description: ShipmentEvent Update for Patch and Put
    ShipmentEvents:
      type: array
      items:
        $ref: "#/components/schemas/ShipmentEvent"
    ShipmentReference:
      type: object
      properties:
        shipment_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        shipment_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        shipment_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        context:
          type: string
          description: Context or domain of the identifier
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        delivery_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        unit_of_measures:
          $ref: "#/components/schemas/UnitOfMeasures"
      description: External system identifier
    ShipmentReferenceCreate:
      required:
      - shipment_reference_key
      type: object
      description: ShipmentReference Create for Post
      allOf:
      - $ref: "#/components/schemas/ShipmentReference"
    ShipmentReferenceUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier:
          $ref: "#/components/schemas/Identifier"
        delivery_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        context:
          type: string
          description: Context or domain of the identifier
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        unit_of_measures:
          $ref: "#/components/schemas/UnitOfMeasures"
      description: ShipmentReference Update for Patch and Put
    ShipmentReferences:
      type: array
      items:
        $ref: "#/components/schemas/ShipmentReference"
    ShipmentStateTypes:
      type: string
      description: types of shipment states.
      enum:
      - OPEN
      - ACTIVE
      - ESTIMATED_SHIP
      - ACTUAL_SHIP
      - UPDATING
      - ESTIMATED_DELIVERY
      - ACTUAL_DELIVERY
      - TRACKED
      - CONFIRM
      - PICKED_UP
      - DELIVERED
      - MANIFEST_CREATED
      - CLOSED
      - RETURN_INITIATED
      - RETURNED_TO_SENDER
      - LOST_IN_TRANSIT
      - RETURNED_TO_WAREHOUSE
    StaffAssignmentReference:
      type: object
      properties:
        staff_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier value for the referenc... Pat...
        staff_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the reference recor... Pat...
        staff_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: The unique key identifying the reference... Pat...
        context:
          type: string
          description: Context or domain of the assignment (e.g...
        assignment_type:
          type: string
          description: The role or type of the assignment (e.g....
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the staff member was a...
          format: date-time
      description: Reference to a Staff (Party) Aggregate Root and...
    StaffAssignmentReferenceCreate:
      required:
      - staff_assignment_reference_key
      type: object
      description: StaffAssignmentReference Create for Post
      allOf:
      - $ref: "#/components/schemas/StaffAssignmentReference"
    StaffAssignmentReferenceUpdate:
      type: object
      properties:
        identifier:
          $ref: "#/components/schemas/Identifier"
        assignment_type:
          type: string
          description: The role or type of the assignment (e.g....
        context:
          type: string
          description: Context or domain of the assignment (e.g...
        occurrence_date_time:
          type: string
          description: The date and time the staff member was a...
          format: date-time
      description: StaffAssignmentReference Update for Patch and Put
    TimeSlot:
      type: object
      properties:
        time_slot_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique business key for the time slot. Pattern:...
        end_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        end_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "End time in HH:mm format. Pattern: ^([01]\\d|2[0..."
          format: date-time
        recurring_indicator:
          type: boolean
          description: Indicates if the time slot is recurring.
        recurring_period:
          $ref: "#/components/schemas/EffectivePeriod"
        start_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        start_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "Start time in HH:mm format. Pattern: ^([01]\\d|2..."
          format: date-time
        timeslot_directive_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeslotDirectiveTypes"
        time_slot_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the time slot reco... Pat...
        week_of_date:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          format: date
          items:
            type: string
      description: Range of time for the appointment including sta...
    TimeslotDirectiveTypes:
      type: string
      description: Represents the directive for a timeslot.
      enum:
      - AFTER
      - ANYTIME
      - BEFORE
      - BETWEEN
      - BLOCK
      - UNKNOWN
    UOMQuantityCategoryTypes:
      type: string
      description: types of u o m quantity categorys.
      enum:
      - COUNT
      - WEIGHT
      - VOLUME
      - TIME
      - NET_WEIGHT
      - NET_VOLUME
      - PACKAGE_QUANTITY
      - MIN_ORDER_QUANTITY
      - GROSS_WEIGHT
      - GROSS_VOLUME
      - SHIPPING_WEIGHT
      - DIMENSIONAL_WEIGHT
      - MATERIAL
      - ELECTRICAL_RATING
      - DEFAULT_LEAD_TIME
      - MAX_LEAD_TIME
      - DIMENSION_UNIT
      - MIN_LENGTH
      - MAX_LENGTH
      - MIN_WIDTH
      - MAX_WIDTH
      - MIN_HEIGHT
      - MAX_HEIGHT
      - MIN_WEIGHT
      - MAX_WEIGHT
      - MAX_ORDER_QUANTITY
      - MIN_ORDER_AVAILABLE
      - DIMENSIONS
      - SHIPPING_DIM_LENGTH
      - SHIPPING_DIM_WIDTH
      - SHIPPING_DIM_HEIGHT
      - SHIPPING_DIM_GROSS_WEIGHT
      - SHIPPING_DIM_NET_WEIGHT
      - SHELF_LIFE_HOURS
      - EXPIRATION_DATE
      - MIN_PAYMENT_PERCENT
      - MIN_PAYMENT_AMOUNT
    UnitOfMeasure:
      type: object
      properties:
        unit_of_measure_key:
          maxLength: 100
          minLength: 1
          pattern: "^[a-zA-Z0-9_.-]+$"
          type: string
          description: "Unit of Measure Value Pattern: ^[a-zA-Z0-9_.-]+..."
        unit_name:
          maxLength: 50
          minLength: 1
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unit of Measure Pattern: ^[a-zA-Z0-9_]+$ (Alpha..."
        uom_value:
          maxLength: 100
          minLength: 1
          pattern: "^[a-zA-Z0-9_.-]+$"
          type: string
          description: "Unit of Measure Value Pattern: ^[a-zA-Z0-9_.-]+..."
        uom_quantity_category_type:
          $ref: "#/components/schemas/UOMQuantityCategoryTypes"
      description: value price with unit of measure
    UnitOfMeasures:
      type: array
      items:
        $ref: "#/components/schemas/UnitOfMeasure"
    WarehouseReference:
      type: object
      properties:
        location_reference_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier value for the location... Pat...
        location_reference_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key value for the location refere... Pat...
        warehouse_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: The unique key identifying the reference... Pat...
        context:
          type: string
          description: "Context or type of the location (e.g., '..."
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the item was associate...
          format: date-time
      description: Reference to a Warehouse or Inventory Location ...
    WarehouseReferenceCreate:
      required:
      - warehouse_reference_key
      type: object
      description: WarehouseReference Create for Post
      allOf:
      - $ref: "#/components/schemas/WarehouseReference"
    WarehouseReferenceUpdate:
      type: object
      properties:
        identifier:
          $ref: "#/components/schemas/Identifier"
        context:
          type: string
          description: "Context or type of the location (e.g., '..."
        occurrence_date_time:
          type: string
          description: The date and time the item was associate...
          format: date-time
      description: WarehouseReference Update for Patch and Put
  responses:
    NotFoundError:
      description: The requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: Authentication failed. The access token is miss...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: The authenticated user does not have the necess...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: The request could not be completed because it c...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: An unexpected error occurred on the server whil...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
  securitySchemes:
    industry-oauth:
      type: oauth2
      description: Centralized OAuth2 service for all industry members
      flows:
        clientCredentials:
          tokenUrl: https://auth.example.com/token
          scopes:
            standard:read: Read access
    member-api-key:
      type: apiKey
      name: X-API-KEY
      in: header
    bearer-auth:
      type: http
      description: JWT token obtained from authentication service
      scheme: bearer
      bearerFormat: JWT
