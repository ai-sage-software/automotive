---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /price-plans:
    get:
      tags:
      - "PricePlan"
      summary: "Retrieve a list of all PricePlan records."
      operationId: "listPricePlans"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PricePlan records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PricePlan"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PricePlan"
      summary: "Create a new PricePlan record."
      operationId: "createPricePlan"
      requestBody:
        description: "PricePlan data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlan_Create"
        required: true
      responses:
        "201":
          description: "PricePlan created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/price-plans/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlan"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /price-plans/{pricePlanKey}:
    get:
      tags:
      - "PricePlan"
      summary: "Retrieve a specific PricePlan record."
      operationId: "getPricePlanById"
      responses:
        "200":
          description: "The requested PricePlan record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlan"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PricePlan"
      summary: "Replace a PricePlan record."
      operationId: "replacePricePlan"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlan"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlan"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PricePlan"
      summary: "Delete a PricePlan Record"
      description: "Marks the PricePlan record as inactive or removes it entirely."
      operationId: "deletePricePlanRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PricePlan"
      summary: "Partially update a PricePlan record."
      operationId: "updatePricePlan"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlan_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlan"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the specific PricePlan."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/product-schedule-references:
    get:
      tags:
      - "ProductScheduleReference"
      summary: "Retrieve a list of ProductScheduleReference records scoped by pricePlanKey."
      operationId: "listProductScheduleReferencesByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ProductScheduleReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ProductScheduleReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ProductScheduleReference"
      summary: "Create a new ProductScheduleReference record."
      operationId: "createProductScheduleReference"
      requestBody:
        description: "ProductScheduleReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductScheduleReference_Create"
        required: true
      responses:
        "201":
          description: "ProductScheduleReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/product-schedule-references/{productScheduleReferenceKey}:
    get:
      tags:
      - "ProductScheduleReference"
      summary: "Retrieve a specific ProductScheduleReference record."
      operationId: "getProductScheduleReferenceById"
      responses:
        "200":
          description: "The requested ProductScheduleReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductScheduleReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ProductScheduleReference"
      summary: "Replace a ProductScheduleReference record."
      operationId: "replaceProductScheduleReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductScheduleReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ProductScheduleReference"
      summary: "Delete a ProductScheduleReference Record"
      description: "Marks the ProductScheduleReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteProductScheduleReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ProductScheduleReference"
      summary: "Partially update a ProductScheduleReference record."
      operationId: "updateProductScheduleReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductScheduleReference_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "productScheduleReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ProductScheduleReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by pricePlanKey."
      operationId: "listIdentifiersByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Create"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or removes it entirely."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/geographic-boundary-references:
    get:
      tags:
      - "GeographicBoundaryReference"
      summary: "Retrieve a list of GeographicBoundaryReference records scoped by pricePlanKey."
      operationId: "listGeographicBoundaryReferencesByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of GeographicBoundaryReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/GeographicBoundaryReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "GeographicBoundaryReference"
      summary: "Create a new GeographicBoundaryReference record."
      operationId: "createGeographicBoundaryReference"
      requestBody:
        description: "GeographicBoundaryReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeographicBoundaryReference_Create"
        required: true
      responses:
        "201":
          description: "GeographicBoundaryReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundaryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/geographic-boundary-references/{geographicBoundaryReferenceKey}:
    get:
      tags:
      - "GeographicBoundaryReference"
      summary: "Retrieve a specific GeographicBoundaryReference record."
      operationId: "getGeographicBoundaryReferenceById"
      responses:
        "200":
          description: "The requested GeographicBoundaryReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundaryReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "GeographicBoundaryReference"
      summary: "Replace a GeographicBoundaryReference record."
      operationId: "replaceGeographicBoundaryReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeographicBoundaryReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundaryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "GeographicBoundaryReference"
      summary: "Delete a GeographicBoundaryReference Record"
      description: "Marks the GeographicBoundaryReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteGeographicBoundaryReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "GeographicBoundaryReference"
      summary: "Partially update a GeographicBoundaryReference record."
      operationId: "updateGeographicBoundaryReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeographicBoundaryReference_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundaryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "geographicBoundaryReferenceKey"
      in: "path"
      description: "The unique identifier for the specific GeographicBoundaryReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/price-sensitivities:
    get:
      tags:
      - "PriceSensitivity"
      summary: "Retrieve a list of PriceSensitivity records scoped by pricePlanKey."
      operationId: "listPriceSensitivitysByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PriceSensitivity records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PriceSensitivity"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PriceSensitivity"
      summary: "Create a new PriceSensitivity record."
      operationId: "createPriceSensitivity"
      requestBody:
        description: "PriceSensitivity data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceSensitivity_Create"
        required: true
      responses:
        "201":
          description: "PriceSensitivity created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceSensitivity"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/price-sensitivities/{priceSensitivityKey}:
    get:
      tags:
      - "PriceSensitivity"
      summary: "Retrieve a specific PriceSensitivity record."
      operationId: "getPriceSensitivityById"
      responses:
        "200":
          description: "The requested PriceSensitivity record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceSensitivity"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PriceSensitivity"
      summary: "Replace a PriceSensitivity record."
      operationId: "replacePriceSensitivity"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceSensitivity"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceSensitivity"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PriceSensitivity"
      summary: "Delete a PriceSensitivity Record"
      description: "Marks the PriceSensitivity record as inactive or removes it entirely."
      operationId: "deletePriceSensitivityRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PriceSensitivity"
      summary: "Partially update a PriceSensitivity record."
      operationId: "updatePriceSensitivity"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceSensitivity_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceSensitivity"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "priceSensitivityKey"
      in: "path"
      description: "The unique identifier for the specific PriceSensitivity."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/effective-periods:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a list of EffectivePeriod records scoped by pricePlanKey."
      operationId: "listEffectivePeriodsByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of EffectivePeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/EffectivePeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a specific EffectivePeriod record."
      operationId: "getEffectivePeriodById"
      responses:
        "200":
          description: "The requested EffectivePeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "EffectivePeriod"
      summary: "Replace a EffectivePeriod record."
      operationId: "replaceEffectivePeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "effectivePeriodKey"
      in: "path"
      description: "The unique identifier for the specific EffectivePeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/party-identifiers:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a list of PartyIdentifier records scoped by pricePlanKey."
      operationId: "listPartyIdentifiersByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartyIdentifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartyIdentifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartyIdentifier"
      summary: "Create a new PartyIdentifier record."
      operationId: "createPartyIdentifier"
      requestBody:
        description: "PartyIdentifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier_Create"
        required: true
      responses:
        "201":
          description: "PartyIdentifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/party-identifiers/{partyIdentifierKey}:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a specific PartyIdentifier record."
      operationId: "getPartyIdentifierById"
      responses:
        "200":
          description: "The requested PartyIdentifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartyIdentifier"
      summary: "Replace a PartyIdentifier record."
      operationId: "replacePartyIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartyIdentifier"
      summary: "Delete a PartyIdentifier Record"
      description: "Marks the PartyIdentifier record as inactive or removes it entirely."
      operationId: "deletePartyIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartyIdentifier"
      summary: "Partially update a PartyIdentifier record."
      operationId: "updatePartyIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partyIdentifierKey"
      in: "path"
      description: "The unique identifier for the specific PartyIdentifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /price-plans/{pricePlanKey}/financial-split-references:
    get:
      tags:
      - "FinancialSplitReference"
      summary: "Retrieve a list of FinancialSplitReference records scoped by pricePlanKey."
      operationId: "listFinancialSplitReferencesByPricePlanKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialSplitReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialSplitReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialSplitReference"
      summary: "Create a new FinancialSplitReference record."
      operationId: "createFinancialSplitReference"
      requestBody:
        description: "FinancialSplitReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplitReference_Create"
        required: true
      responses:
        "201":
          description: "FinancialSplitReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplitReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /price-plans/{pricePlanKey}/financial-split-references/{financialSplitReferenceKey}:
    get:
      tags:
      - "FinancialSplitReference"
      summary: "Retrieve a specific FinancialSplitReference record."
      operationId: "getFinancialSplitReferenceById"
      responses:
        "200":
          description: "The requested FinancialSplitReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplitReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialSplitReference"
      summary: "Replace a FinancialSplitReference record."
      operationId: "replaceFinancialSplitReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplitReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplitReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialSplitReference"
      summary: "Delete a FinancialSplitReference Record"
      description: "Marks the FinancialSplitReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteFinancialSplitReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialSplitReference"
      summary: "Partially update a FinancialSplitReference record."
      operationId: "updateFinancialSplitReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplitReference_Update"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplitReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "pricePlanKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialSplitReferenceKey"
      in: "path"
      description: "The unique identifier for the specific FinancialSplitReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas: {}
