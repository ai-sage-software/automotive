openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: A standardized interface for Automotive Retail ...
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
servers:
- url: /v1_0_0
  description: server
security:
- industry-oauth:
  - standard:read
- member-api-key: []
- {}
paths:
  /payroll-stubs:
    get:
      tags:
      - PayrollStub
      summary: Retrieve a list of PayrollStub entities.
      operationId: listPayrollStub
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of PayrollStub entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PayrollStub"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PayrollStub
      summary: Create a new PayrollStub entity.
      operationId: createPayrollStub
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: PayrollStub data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollStubCreate"
        required: true
      responses:
        "201":
          description: PayrollStub created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /payroll-stubs/KEY78H
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStub"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
  /payroll-stubs/{payrollStubKey}:
    get:
      tags:
      - PayrollStub
      summary: Retrieve a specific PayrollStub entity.
      operationId: getPayrollStubById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested PayrollStub entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStubResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PayrollStub
      summary: Replace a PayrollStub entity.
      operationId: replacePayrollStub
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollStub"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStub"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PayrollStub
      summary: Delete a PayrollStub entity
      description: Marks the PayrollStub entity as inactive or rem...
      operationId: deletePayrollStubEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PayrollStub
      summary: Partially update a PayrollStub entity.
      operationId: updatePayrollStub
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PayrollStubUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollStub"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the specific PayrollStub.
      required: true
      schema:
        type: string
        example: KEY78f
  /payroll-stubs/{payrollStubKey}/tax-reporting-contexts:
    get:
      tags:
      - TaxReportingContext
      summary: Retrieve a list of TaxReportingContext entities...
      operationId: listTaxReportingContextByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of TaxReportingContext entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaxReportingContext"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - TaxReportingContext
      summary: Create a new TaxReportingContext entity.
      operationId: createTaxReportingContext
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: TaxReportingContext data to create the new reso...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaxReportingContextCreate"
        required: true
      responses:
        "201":
          description: TaxReportingContext created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxReportingContext"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/tax-reporting-contexts/{taxReportingContextKey}:
    get:
      tags:
      - TaxReportingContext
      summary: Retrieve a specific TaxReportingContext entity.
      operationId: getTaxReportingContextById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested TaxReportingContext entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxReportingContext"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - TaxReportingContext
      summary: Replace a TaxReportingContext entity.
      operationId: replaceTaxReportingContext
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaxReportingContext"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxReportingContext"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - TaxReportingContext
      summary: Delete a TaxReportingContext entity
      description: Marks the TaxReportingContext entity as inactiv...
      operationId: deleteTaxReportingContextEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - TaxReportingContext
      summary: Partially update a TaxReportingContext entity.
      operationId: updateTaxReportingContext
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/TaxReportingContextUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxReportingContext"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: taxReportingContextKey
      in: path
      description: The unique identifier for the specific TaxReportingContext.
      required: true
      schema:
        type: string
        example: KEY78fo
  /payroll-stubs/{payrollStubKey}/moneys:
    get:
      tags:
      - Money
      summary: Retrieve a list of Money entities scoped by pay...
      operationId: listMoneyByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of Money entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Money"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/moneys/{moneyKey}:
    get:
      tags:
      - Money
      summary: Retrieve a specific Money entity.
      operationId: getMoneyById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested Money entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: moneyKey
      in: path
      description: The unique identifier for the specific Money.
      required: true
      schema:
        type: string
        example: KEY78fo9
  /payroll-stubs/{payrollStubKey}/identifiers:
    get:
      tags:
      - Identifier
      summary: Retrieve a list of Identifier entities scoped b...
      operationId: listIdentifierByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of Identifier entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Identifier"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Identifier
      summary: Create a new Identifier entity.
      operationId: createIdentifier
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Identifier data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: Identifier created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/identifiers/{identifierKey}:
    get:
      tags:
      - Identifier
      summary: Retrieve a specific Identifier entity.
      operationId: getIdentifierById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested Identifier entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Identifier
      summary: Replace a Identifier entity.
      operationId: replaceIdentifier
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Identifier
      summary: Delete a Identifier entity
      description: Marks the Identifier entity as inactive or remo...
      operationId: deleteIdentifierEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Identifier
      summary: Partially update a Identifier entity.
      operationId: updateIdentifier
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: identifierKey
      in: path
      description: The unique identifier for the specific Identifier.
      required: true
      schema:
        type: string
        example: KEY78fo9J
  /payroll-stubs/{payrollStubKey}/effective-periods:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a list of EffectivePeriod entities sco...
      operationId: listEffectivePeriodByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of EffectivePeriod entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EffectivePeriod"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a specific EffectivePeriod entity.
      operationId: getEffectivePeriodById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested EffectivePeriod entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: effectivePeriodKey
      in: path
      description: The unique identifier for the specific EffectivePeriod.
      required: true
      schema:
        type: string
        example: KEY78fo9Jx
  /payroll-stubs/{payrollStubKey}/payroll-components:
    get:
      tags:
      - PayrollComponent
      summary: Retrieve a list of PayrollComponent entities sc...
      operationId: listPayrollComponentByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of PayrollComponent entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PayrollComponent"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PayrollComponent
      summary: Create a new PayrollComponent entity.
      operationId: createPayrollComponent
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: PayrollComponent data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollComponentCreate"
        required: true
      responses:
        "201":
          description: PayrollComponent created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/payroll-components/{payrollComponentKey}:
    get:
      tags:
      - PayrollComponent
      summary: Retrieve a specific PayrollComponent entity.
      operationId: getPayrollComponentById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested PayrollComponent entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PayrollComponent
      summary: Replace a PayrollComponent entity.
      operationId: replacePayrollComponent
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollComponent"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PayrollComponent
      summary: Delete a PayrollComponent entity
      description: Marks the PayrollComponent entity as inactive o...
      operationId: deletePayrollComponentEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PayrollComponent
      summary: Partially update a PayrollComponent entity.
      operationId: updatePayrollComponent
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PayrollComponentUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: payrollComponentKey
      in: path
      description: The unique identifier for the specific PayrollComponent.
      required: true
      schema:
        type: string
        example: KEY78fo9Jx5
  /payroll-stubs/{payrollStubKey}/time-slots:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a list of TimeSlot entities scoped by ...
      operationId: listTimeSlotByPayrollStubKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of TimeSlot entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TimeSlot"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /payroll-stubs/{payrollStubKey}/time-slots/{timeSlotKey}:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a specific TimeSlot entity.
      operationId: getTimeSlotById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested TimeSlot entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: payrollStubKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: timeSlotKey
      in: path
      description: The unique identifier for the specific TimeSlot.
      required: true
      schema:
        type: string
        example: KEY78fo9Jx5A
  /payroll-stubs/health:
    get:
      summary: Health Check
      description: Public endpoint to verify service availability.
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: Service is healthy
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - status
                - version
                type: object
                properties:
                  status:
                    type: string
                    example: up
                    enum:
                    - up
                    - down
                    - maintenance
                  version:
                    type: string
                    description: The version of the ontology implemented by this service.
                    example: 1.0.0
                  timestamp:
                    type: string
                    description: Server time in ISO-8601 format.
                    format: date-time
                    example: 2025-12-21T23:00:00Z
      security:
      - {}
  /payroll-stubs/batch:
    post:
      summary: Bulk create payroll-stubs
      operationId: createPayrollStubBatch
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  maxItems: 100
                  type: array
                  items:
                    $ref: "#/components/schemas/PayrollStubCreate"
      responses:
        "200":
          description: Batch processing complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  succeeded:
                    type: array
                    items:
                      $ref: "#/components/schemas/PayrollStub"
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        index:
                          type: integer
                          format: int32
                        error:
                          $ref: "#/components/schemas/ErrorDetail"
  /payroll-stubs/{payrollStubKey}/commands:
    post:
      summary: Execute a business workflow on PayrollStub
      description: Single entry point for all PayrollStub state-changing intents.
      operationId: executePayrollStubCommand
      parameters:
      - name: payrollStubKey
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Command intent and parameters
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: "The intent of the command (e.g., RescheduleSlot, CancelInvoice)"
                workflow_state:
                  type: string
                  description: "The intent of the command (e.g., CREATED, DELETED)"
                  enum:
                  - CREATED
                  - DELETED
                context:
                  type: object
                  additionalProperties: true
                  description: The payload required for this specific command type
                data:
                  $ref: "#/components/schemas/PayrollStub"
      responses:
        "200":
          description: Commands processing complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: The execution status of the command
                    example: SUCCESS
                    enum:
                    - SUCCESS
                    - FAILED
                    - PENDING
                    - PROCESSING
                    - VALIDATION_ERROR
                    - PARTIAL_SUCCESS
                    - REQUIRES_ACTION
                    - MITIGATION_APPLIED
                  message:
                    type: string
                    description: A human-readable summary or error detail
                    example: "Invoice #6870d848 has been successfully cancelled."
                  commandId:
                    type: string
                    description: The tracking ID for this specific command execution
                    format: identifier
                  code:
                    type: string
                    description: detailed code for workflow business processing
                    example: 90bad0b3
                  retry_after:
                    type: string
                    description: "For PENDING or PROCESSING statuses, retry_after\
                      \ (integer, seconds) "
                  retryable:
                    type: boolean
                    description: "If true, the agent can attempt this command again\
                      \ immediately"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
  /payroll-stubs/{payrollStubKey}/commands/{commandId}:
    get:
      summary: Poll PayrollStub command status
      description: Check the progress or result of a previously submitted command.
      operationId: getPayrollStubCommandStatus
      parameters:
      - name: payrollStubKey
        in: path
        required: true
        schema:
          type: string
          format: string
      - name: commandId
        in: path
        required: true
        schema:
          type: string
          format: string
      responses:
        "200":
          description: Current status of the command
          content:
            application/json:
              schema:
                type: object
                properties:
                  commandId:
                    type: string
                    description: The unique identifier for the command being tracked
                    format: uuid
                  status:
                    type: string
                    description: The current lifecycle state of the command
                    enum:
                    - PENDING
                    - PROCESSING
                    - SUCCESS
                    - FAILED
                    - REQUIRES_ACTION
                  progress:
                    maximum: 100
                    minimum: 0
                    type: integer
                    description: Percentage of completion (0-100)
                    format: int32
                  message:
                    type: string
                    description: Status update or error message
                  result:
                    type: object
                    additionalProperties: true
                    description: The final output of the command (only populated on
                      SUCCESS)
                  errors:
                    type: array
                    items:
                      $ref: "#/components/schemas/ErrorDetail"
  /payroll-stubs/capabilities:
    get:
      summary: Discover PayrollStub State Machine
      operationId: getPayrollStubCapabilities
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  aggregate:
                    type: string
                    example: PayrollStub
                  version:
                    type: string
                    example: 1.2.0
                  commands:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                          example: CancelInvoice
                        description:
                          type: string
                          example: Voids the invoice and releases held funds.
                        from_states:
                          type: array
                          example:
                          - CREATED
                          - ACTIVE
                          items:
                            type: string
                        to_state:
                          type: string
                          example: DELETED
                        parameters_ref:
                          type: string
                          example: "#/components/schemas/PayrollStub"
components:
  schemas:
    Codes:
      type: array
      items:
        type: string
    DaysOfWeekTypes:
      type: string
      description: types of days of weeks.
      enum:
      - SUN
      - MON
      - TUES
      - WED
      - THURS
      - FRI
      - SAT
    DurationUOMTypes:
      type: string
      description: types of duration u o ms.
      enum:
      - MINUTES
      - HOURS
      - DAYS
      - DAY_OF_MONTH
      - WEEKS
      - BI_WEEKS
      - SEMI_MONTHS
      - MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - CUSTOM
      - OCCURRENCE
      - UNLIMITED
      - NONE
      - OTHER
    EffectivePeriod:
      type: object
      properties:
        effective_period_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key identifying the effective per... Pat...
        active_indicator:
          type: boolean
          description: Indicator if the period is currently act...
        exclusive_indicator:
          type: boolean
          description: Indicator if the period is exclusive
        start_date_time:
          type: string
          description: Start date and time of the effective per...
          format: date-time
        complete_date_time:
          type: string
          description: Completion date and time of the effectiv...
          format: date-time
        duration_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        duration_value:
          type: string
          description: Value of the duration
        recur_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        recur_count:
          type: string
          description: Count of recurrence
        recur_values:
          $ref: "#/components/schemas/Codes"
        recur_criteria:
          type: string
          description: Criteria defining recurrence
        recur_indicator:
          type: boolean
          description: Indicator if recurrence is enabled
        timeslot:
          $ref: "#/components/schemas/TimeSlot"
      description: Effective Period
    ErrorDetail:
      required:
      - category
      - code
      - message
      - timestamp
      - traceId
      type: object
      properties:
        code:
          type: string
          description: "A machine-readable, unique system error code (e..."
          example: NOT_FOUND_PR
        message:
          type: string
          description: A developer-friendly explanation of the error.
          example: Personnel Record with ID PR-1234567 was not found.
        category:
          type: string
          description: Classifies the error to help clients determine ...
          example: DATA_INTEGRITY
          enum:
          - VALIDATION
          - AUTHENTICATION
          - AUTHORIZATION
          - INFRASTRUCTURE
          - BUSINESS_LOGIC
          - DATA_INTEGRITY
        timestamp:
          type: string
          description: The time the error occurred (ISO 8601).
          format: date-time
          example: 2025-11-10T10:30:00Z
        traceId:
          type: string
          description: "A unique identifier for this specific request, ..."
          example: 57c31110-859a-11ec-a8a3-0242ac120002
        docUrl:
          type: string
          description: A URL leading to the documentation page for thi...
          format: uri
          example: https://api.docs.com/errors/NOT_FOUND_PR
        details:
          type: array
          description: Field-level validation errors or granular diagn...
          items:
            type: object
            properties:
              field:
                type: string
                description: The JSON path or field name that caused the error.
                example: payrollId
              issue:
                type: string
                description: The specific validation rule that failed.
                example: must be unique
              rejectedValue:
                type: string
                description: The value that was sent and rejected (only if non-sensitive).
                example: "12345"
      description: Standardized error response for the entire API ...
    Identifier:
      type: object
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: string
          description: Context or domain of the identifier
        value:
          type: string
          description: The monetary or hours-based value of the...
        detail:
          type: string
          description: The monetary or hours-based value of the...
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: External system identifier
    IdentifierCreate:
      required:
      - identifier_key
      type: object
      description: Identifier Create for Post
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        context:
          type: string
          description: Context or domain of the identifier
        detail:
          type: string
          description: The monetary or hours-based value of the...
        value:
          type: string
          description: The monetary or hours-based value of the...
      description: Identifier Update for Patch and Put
    Money:
      type: object
      properties:
        money_key:
          type: string
          description: The unique key for the monetary value.
        money_id:
          type: string
          description: The unique ID for the monetary value.
        currency_code:
          type: string
          description: "ISO currency code, e.g., 'USD'."
        display_value:
          type: string
          description: The locale-specific display format for t...
        locale:
          type: string
          description: "The locale code (e.g., en-US, fr-FR)."
        value:
          type: string
          description: The monetary value.
      description: "Represents a monetary value, including its curr..."
    PayerTypes:
      type: string
      description: types of payers.
      enum:
      - AGGREGATE
      - EMPLOYER
      - EMPLOYEE
      - AGGREGATE_EMPLOYER
      - AGGREGATE_EMPLOYEE
    PayrollAggregateTypes:
      type: string
      description: types of payroll aggregates.
      enum:
      - ITEM
      - GROSS_PAY
      - NET_PAY
      - TOTAL_DEDUCTIONS
      - TOTAL_TAXES
    PayrollComponent:
      type: object
      properties:
        payroll_component_id:
          type: string
          description: A unique identifier for the payroll even...
        payroll_componen_key:
          type: string
          description: A unique identifier for the payroll even...
        staff_member_id:
          type: string
          description: The unique ID of the staff member associ...
        payroll_cycle_key:
          type: string
          description: The unique ID of the staff member associ...
        employing_entity_id:
          type: string
          description: The unique ID of the staff member associ...
        payroll_event_type:
          $ref: "#/components/schemas/PayrollEventTypes"
        payroll_event_uomtype:
          $ref: "#/components/schemas/UOMPayrollTypes"
        payer_type:
          $ref: "#/components/schemas/PayerTypes"
        event_code:
          type: string
          description: The code for the payroll event.
        event_description:
          type: string
          description: A description of the payroll event.
        amount:
          $ref: "#/components/schemas/Money"
        rate:
          type: string
          description: The rate of pay for the payroll event.
        hours_worked:
          type: string
          description: The number of hours worked for the payro...
        ytd_amount:
          $ref: "#/components/schemas/Money"
        event_reference:
          type: string
          description: Event Reference for this event
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
        payroll_aggregate_type:
          $ref: "#/components/schemas/PayrollAggregateTypes"
        payroll_event_category_type:
          $ref: "#/components/schemas/PayrollEventCategoryTypes"
      description: Represents a single event that affects a staff ...
    PayrollComponentCreate:
      required:
      - payroll_component_key
      type: object
      description: PayrollComponent Create for Post
      allOf:
      - $ref: "#/components/schemas/PayrollComponent"
    PayrollComponentUpdate:
      type: object
      properties:
        amount:
          $ref: "#/components/schemas/Money"
        payer_type:
          $ref: "#/components/schemas/PayerTypes"
        event_description:
          type: string
          description: A description of the payroll event.
        event_reference:
          type: string
          description: Event Reference for this event
        payroll_aggregate_type:
          $ref: "#/components/schemas/PayrollAggregateTypes"
        payroll_event_category_type:
          $ref: "#/components/schemas/PayrollEventCategoryTypes"
        rate:
          type: string
          description: The rate of pay for the payroll event.
        event_code:
          type: string
          description: The code for the payroll event.
        ytd_amount:
          $ref: "#/components/schemas/Money"
        hours_worked:
          type: string
          description: The number of hours worked for the payro...
        payroll_event_uomtype:
          $ref: "#/components/schemas/UOMPayrollTypes"
        payroll_event_type:
          $ref: "#/components/schemas/PayrollEventTypes"
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
      description: PayrollComponent Update for Patch and Put
    PayrollComponents:
      type: array
      items:
        $ref: "#/components/schemas/PayrollComponent"
    PayrollCycleFrequencyTypes:
      type: string
      description: types of payroll cycle frequencys.
      enum:
      - ANNUAL
      - SEMI_ANNUAL
      - QUARTERLY
      - BI_MONTHLY
      - MONTHLY
      - SEMI_MONTHLY
      - BI_WEEKLY
      - WEEKLY
      - DAILY
      - OFF_CYCLE
      - ON_DEMAND
    PayrollEventCategoryTypes:
      type: string
      description: types of payroll event categorys.
      enum:
      - PAY_CYCLE
      - PAY_STUB
      - PAY_RESULTS
    PayrollEventTypes:
      type: string
      description: types of payroll events.
      enum:
      - PAYMENT_EVENT
      - HOURS_EVENT
      - OVERTIME_EVENT
      - COMMISSION_EVENT
      - BONUS_EVENT
      - EXPENSE_PAYMENT_EVENT
      - REIMBURSEMENT_EVENT
      - PRETAX_BENEFIT_EVENT
      - RETIREMENT_PLAN_EVENT
      - POSTTAX_DEDUCTION_EVENT
      - GARNISHMENT_EVENT
      - FEE_EVENT
      - TAX_EVENT
      - PRETAX_DEDUCTION_EVENT
      - DEDUCTION_EVENT
      - CYCLE_PROCESSED
      - UNKNOWN
    PayrollStub:
      type: object
      properties:
        pay_stub_id:
          type: string
          description: A unique identifier for the pay stub doc...
        pay_stub_key:
          type: string
          description: A unique identifier for the pay stub doc...
        tax_reporting_context:
          $ref: "#/components/schemas/TaxReportingContext"
        pay_period:
          $ref: "#/components/schemas/EffectivePeriod"
        pay_date_time:
          type: string
          description: The date and time the payment was issued...
          format: date-time
        identifiers:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/Identifier"
        payroll_items:
          $ref: "#/components/schemas/PayrollComponents"
        payroll_cycle_frequency_type:
          $ref: "#/components/schemas/PayrollCycleFrequencyTypes"
        pay_summary_id:
          type: string
          description: A unique identifier for the pay summary ...
        pay_summary_key:
          type: string
          description: A unique identifier for the pay summary ...
        payroll_run_id:
          type: string
          description: A unique identifier for the specific bat...
      description: Represents a single document issued to an emplo...
    PayrollStubCreate:
      required:
      - payroll_stub_key
      type: object
      description: PayrollStub Create for Post
      allOf:
      - $ref: "#/components/schemas/PayrollStub"
    PayrollStubUpdate:
      type: object
      properties:
        pay_date_time:
          type: string
          description: The date and time the payment was issued...
          format: date-time
        payroll_items:
          $ref: "#/components/schemas/PayrollComponents"
        tax_reporting_context:
          $ref: "#/components/schemas/TaxReportingContext"
        identifiers:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/Identifier"
        pay_period:
          $ref: "#/components/schemas/EffectivePeriod"
        payroll_cycle_frequency_type:
          $ref: "#/components/schemas/PayrollCycleFrequencyTypes"
      description: PayrollStub Update for Patch and Put
    TaxReportingContext:
      type: object
      properties:
        tax_reporting_context_id:
          type: string
          description: The Employer Identification Number (EIN)...
        tax_reporting_context_key:
          type: string
          description: The Employer Identification Number (EIN)...
        employer_ein:
          type: string
          description: The Employer Identification Number (EIN)...
        employee_ssn_last_four:
          type: string
          description: The last four digits of the employee's S...
        employing_legal_entity_id:
          type: string
          description: The internal ID of the legal entity resp...
        state_employer_id:
          type: string
          description: The state-specific employer ID for tax r...
          example: 987-654
      description: "Provides the necessary legal identifiers (EIN, ..."
    TaxReportingContextCreate:
      required:
      - tax_reporting_context_key
      type: object
      description: TaxReportingContext Create for Post
      allOf:
      - $ref: "#/components/schemas/TaxReportingContext"
    TaxReportingContextUpdate:
      type: object
      properties:
        employer_ein:
          type: string
          description: The Employer Identification Number (EIN)...
        employee_ssn_last_four:
          type: string
          description: The last four digits of the employee's S...
      description: TaxReportingContext Update for Patch and Put
    TimeSlot:
      type: object
      properties:
        time_slot_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique business key for the time slot. Pattern:...
        end_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        end_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "End time in HH:mm format. Pattern: ^([01]\\d|2[0..."
          format: date-time
        recurring_indicator:
          type: boolean
          description: Indicates if the time slot is recurring.
        recurring_period:
          $ref: "#/components/schemas/EffectivePeriod"
        start_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        start_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "Start time in HH:mm format. Pattern: ^([01]\\d|2..."
          format: date-time
        timeslot_directive_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeslotDirectiveTypes"
        time_slot_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the time slot reco... Pat...
        week_of_date:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          format: date
          items:
            type: string
      description: Range of time for the appointment including sta...
    TimeslotDirectiveTypes:
      type: string
      description: types of timeslot directives.
      enum:
      - AFTER
      - ANYTIME
      - BEFORE
      - BETWEEN
      - BLOCK
      - UNKNOWN
    UOMPayrollTypes:
      type: string
      description: Undocumented Enum
      enum:
      - HOURS
      - MINUTES
      - DAYS
      - WEEKS
      - MONTHS
      - NANOSECOND
      - MICROSECOND
      - MILLISECOND
      - SECOND
      - MINUTE
      - HOUR
      - DAY
      - WEEK
      - MONTH
      - YEAR
      - DECADE
      - CENTURY
      - MILLENNIUM
      - PERCENTAGE_OF_HOUR
      - DURATION
      - DAY_OF_MONTH
      - BI_WEEKS
      - SEMI_MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - OCCURRENCE
      - UNLIMITED
      - NONE
      - CUSTOM
      - OTHER
      - MONETARY
      - UNIT
      - PERCENTAGE
      - UNKNOWN
    CommandOption:
      type: object
      properties:
        type:
          type: string
          description: The command name to send to the /commands endpoint
          example: Approve
        description:
          type: string
          description: Guidance for the agent on when to use this command
          example: Moves the invoice to ACTIVE state and triggers the payment gateway.
        target_state:
          type: string
          description: The state the entity will transition to upon success
          example: ACTIVE
    PayrollStubWorkflowMetadata:
      type: object
      properties:
        current_state:
          type: string
          description: The current lifecycle phase of this entity.
          enum:
          - CREATED
          - ACTIVE
          - DELETED
          - ARCHIVED
        allowed_commands:
          type: array
          description: Intents the agent can execute on this specific instance right
            now.
          items:
            $ref: "#/components/schemas/CommandOption"
    PayrollStubResponse:
      allOf:
      - $ref: "#/components/schemas/PayrollStub"
      - $ref: "#/components/schemas/PayrollStubWorkflowMetadata"
  responses:
    ForbiddenError:
      description: The authenticated user does not have the necess...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: The request could not be completed because it c...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: An unexpected error occurred on the server whil...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    PreconditionFailedError:
      description: The request was not processed because the 'If-M...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    NotFoundError:
      description: The requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: Authentication failed. The access token is miss...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
  parameters:
    StandardsVersionHeader:
      name: standards-version
      in: header
      required: true
      schema:
        pattern: ^\d+(\.\d+)*$
        type: string
        example: "2026.1"
  headers:
    StandardsVersionResponse:
      description: Standards Version
      schema:
        pattern: ^\d+(\.\d+)*$
        type: string
        example: "2026.1"
  securitySchemes:
    industry-oauth:
      type: oauth2
      description: Centralized OAuth2 service for all industry members
      flows:
        clientCredentials:
          tokenUrl: https://auth.example.com/token
          scopes:
            standard:read: Read access
    member-api-key:
      type: apiKey
      name: X-API-KEY
      in: header
    bearer-auth:
      type: http
      description: JWT token obtained from authentication service
      scheme: bearer
      bearerFormat: JWT
