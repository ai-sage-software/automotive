openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: A standardized interface for Automotive Retail ...
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
security:
- industry-oauth:
  - standard:read
- member-api-key: []
- {}
paths:
  /staff-members:
    get:
      tags:
      - StaffMember
      summary: Retrieve a list of StaffMember entities.
      operationId: listStaffMember
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of StaffMember entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/StaffMember"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - StaffMember
      summary: Create a new StaffMember entity.
      operationId: createStaffMember
      requestBody:
        description: StaffMember data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffMemberCreate"
        required: true
      responses:
        "201":
          description: StaffMember created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /staff-members/KEY78K
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
  /staff-members/{staffMemberKey}:
    get:
      tags:
      - StaffMember
      summary: Retrieve a specific StaffMember entity.
      operationId: getStaffMemberById
      responses:
        "200":
          description: The requested StaffMember entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMemberResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - StaffMember
      summary: Replace a StaffMember entity.
      operationId: replaceStaffMember
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffMember"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - StaffMember
      summary: Delete a StaffMember entity
      description: Marks the StaffMember entity as inactive or rem...
      operationId: deleteStaffMemberEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - StaffMember
      summary: Partially update a StaffMember entity.
      operationId: updateStaffMember
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/StaffMemberUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffMember"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the specific StaffMember.
      required: true
      schema:
        type: string
        example: KEY78G
  /staff-members/{staffMemberKey}/person-names:
    get:
      tags:
      - PersonName
      summary: Retrieve a list of PersonName entities scoped b...
      operationId: listPersonNameByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PersonName entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PersonName"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PersonName
      summary: Create a new PersonName entity.
      operationId: createPersonName
      requestBody:
        description: PersonName data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonNameCreate"
        required: true
      responses:
        "201":
          description: PersonName created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/person-names/{personNameKey}:
    get:
      tags:
      - PersonName
      summary: Retrieve a specific PersonName entity.
      operationId: getPersonNameById
      responses:
        "200":
          description: The requested PersonName entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PersonName
      summary: Replace a PersonName entity.
      operationId: replacePersonName
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonName"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PersonName
      summary: Delete a PersonName entity
      description: Marks the PersonName entity as inactive or remo...
      operationId: deletePersonNameEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PersonName
      summary: Partially update a PersonName entity.
      operationId: updatePersonName
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PersonNameUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: personNameKey
      in: path
      description: The unique identifier for the specific PersonName.
      required: true
      schema:
        type: string
        example: KEY78GD
  /staff-members/{staffMemberKey}/payroll-rates:
    get:
      tags:
      - PayrollRate
      summary: Retrieve a list of PayrollRate entities scoped ...
      operationId: listPayrollRateByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PayrollRate entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PayrollRate"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PayrollRate
      summary: Create a new PayrollRate entity.
      operationId: createPayrollRate
      requestBody:
        description: PayrollRate data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollRateCreate"
        required: true
      responses:
        "201":
          description: PayrollRate created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRate"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/payroll-rates/{payrollRateKey}:
    get:
      tags:
      - PayrollRate
      summary: Retrieve a specific PayrollRate entity.
      operationId: getPayrollRateById
      responses:
        "200":
          description: The requested PayrollRate entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRate"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PayrollRate
      summary: Replace a PayrollRate entity.
      operationId: replacePayrollRate
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PayrollRate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRate"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PayrollRate
      summary: Delete a PayrollRate entity
      description: Marks the PayrollRate entity as inactive or rem...
      operationId: deletePayrollRateEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PayrollRate
      summary: Partially update a PayrollRate entity.
      operationId: updatePayrollRate
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PayrollRateUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayrollRate"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: payrollRateKey
      in: path
      description: The unique identifier for the specific PayrollRate.
      required: true
      schema:
        type: string
        example: KEY78GDk
  /staff-members/{staffMemberKey}/addresses:
    get:
      tags:
      - Address
      summary: Retrieve a list of Address entities scoped by s...
      operationId: listAddressByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Address entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Address"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Address
      summary: Create a new Address entity.
      operationId: createAddress
      requestBody:
        description: Address data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressCreate"
        required: true
      responses:
        "201":
          description: Address created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/addresses/{addressKey}:
    get:
      tags:
      - Address
      summary: Retrieve a specific Address entity.
      operationId: getAddressById
      responses:
        "200":
          description: The requested Address entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Address
      summary: Replace a Address entity.
      operationId: replaceAddress
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Address"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Address
      summary: Delete a Address entity
      description: Marks the Address entity as inactive or removes...
      operationId: deleteAddressEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Address
      summary: Partially update a Address entity.
      operationId: updateAddress
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AddressUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: addressKey
      in: path
      description: The unique identifier for the specific Address.
      required: true
      schema:
        type: string
        example: KEY78GDk7
  /staff-members/{staffMemberKey}/positions:
    get:
      tags:
      - Position
      summary: Retrieve a list of Position entities scoped by ...
      operationId: listPositionByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Position entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Position"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Position
      summary: Create a new Position entity.
      operationId: createPosition
      requestBody:
        description: Position data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionCreate"
        required: true
      responses:
        "201":
          description: Position created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/positions/{positionKey}:
    get:
      tags:
      - Position
      summary: Retrieve a specific Position entity.
      operationId: getPositionById
      responses:
        "200":
          description: The requested Position entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Position
      summary: Replace a Position entity.
      operationId: replacePosition
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Position
      summary: Delete a Position entity
      description: Marks the Position entity as inactive or remove...
      operationId: deletePositionEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Position
      summary: Partially update a Position entity.
      operationId: updatePosition
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PositionUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: positionKey
      in: path
      description: The unique identifier for the specific Position.
      required: true
      schema:
        type: string
        example: KEY78GDk71
  /staff-members/{staffMemberKey}/privacy-events:
    get:
      tags:
      - PrivacyEvent
      summary: Retrieve a list of PrivacyEvent entities scoped...
      operationId: listPrivacyEventByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PrivacyEvent entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PrivacyEvent"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PrivacyEvent
      summary: Create a new PrivacyEvent entity.
      operationId: createPrivacyEvent
      requestBody:
        description: PrivacyEvent data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyEventCreate"
        required: true
      responses:
        "201":
          description: PrivacyEvent created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/privacy-events/{privacyEventKey}:
    get:
      tags:
      - PrivacyEvent
      summary: Retrieve a specific PrivacyEvent entity.
      operationId: getPrivacyEventById
      responses:
        "200":
          description: The requested PrivacyEvent entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PrivacyEvent
      summary: Replace a PrivacyEvent entity.
      operationId: replacePrivacyEvent
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyEvent"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PrivacyEvent
      summary: Delete a PrivacyEvent entity
      description: Marks the PrivacyEvent entity as inactive or re...
      operationId: deletePrivacyEventEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PrivacyEvent
      summary: Partially update a PrivacyEvent entity.
      operationId: updatePrivacyEvent
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PrivacyEventUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: privacyEventKey
      in: path
      description: The unique identifier for the specific PrivacyEvent.
      required: true
      schema:
        type: string
        example: KEY78GDk71Z
  /staff-members/{staffMemberKey}/time-slots:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a list of TimeSlot entities scoped by ...
      operationId: listTimeSlotByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of TimeSlot entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TimeSlot"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/time-slots/{timeSlotKey}:
    get:
      tags:
      - TimeSlot
      summary: Retrieve a specific TimeSlot entity.
      operationId: getTimeSlotById
      responses:
        "200":
          description: The requested TimeSlot entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: timeSlotKey
      in: path
      description: The unique identifier for the specific TimeSlot.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm
  /staff-members/{staffMemberKey}/control-accounts:
    get:
      tags:
      - ControlAccount
      summary: Retrieve a list of ControlAccount entities scop...
      operationId: listControlAccountByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of ControlAccount entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ControlAccount"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - ControlAccount
      summary: Create a new ControlAccount entity.
      operationId: createControlAccount
      requestBody:
        description: ControlAccount data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountCreate"
        required: true
      responses:
        "201":
          description: ControlAccount created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccount"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/control-accounts/{controlAccountKey}:
    get:
      tags:
      - ControlAccount
      summary: Retrieve a specific ControlAccount entity.
      operationId: getControlAccountById
      responses:
        "200":
          description: The requested ControlAccount entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccount"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - ControlAccount
      summary: Replace a ControlAccount entity.
      operationId: replaceControlAccount
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccount"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccount"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - ControlAccount
      summary: Delete a ControlAccount entity
      description: Marks the ControlAccount entity as inactive or ...
      operationId: deleteControlAccountEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - ControlAccount
      summary: Partially update a ControlAccount entity.
      operationId: updateControlAccount
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/ControlAccountUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccount"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: controlAccountKey
      in: path
      description: The unique identifier for the specific ControlAccount.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2
  /staff-members/{staffMemberKey}/communication-channels:
    get:
      tags:
      - CommunicationChannel
      summary: Retrieve a list of CommunicationChannel entitie...
      operationId: listCommunicationChannelByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of CommunicationChannel entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CommunicationChannel"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - CommunicationChannel
      summary: Create a new CommunicationChannel entity.
      operationId: createCommunicationChannel
      requestBody:
        description: CommunicationChannel data to create the new res...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommunicationChannelCreate"
        required: true
      responses:
        "201":
          description: CommunicationChannel created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationChannel"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/communication-channels/{communicationChannelKey}:
    get:
      tags:
      - CommunicationChannel
      summary: Retrieve a specific CommunicationChannel entity.
      operationId: getCommunicationChannelById
      responses:
        "200":
          description: The requested CommunicationChannel entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationChannel"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - CommunicationChannel
      summary: Replace a CommunicationChannel entity.
      operationId: replaceCommunicationChannel
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommunicationChannel"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationChannel"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - CommunicationChannel
      summary: Delete a CommunicationChannel entity
      description: Marks the CommunicationChannel entity as inacti...
      operationId: deleteCommunicationChannelEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - CommunicationChannel
      summary: Partially update a CommunicationChannel entity.
      operationId: updateCommunicationChannel
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/CommunicationChannelUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommunicationChannel"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: communicationChannelKey
      in: path
      description: The unique identifier for the specific CommunicationChannel.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2Y
  /staff-members/{staffMemberKey}/address-locales:
    get:
      tags:
      - AddressLocale
      summary: Retrieve a list of AddressLocale entities scope...
      operationId: listAddressLocaleByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of AddressLocale entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/AddressLocale"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - AddressLocale
      summary: Create a new AddressLocale entity.
      operationId: createAddressLocale
      requestBody:
        description: AddressLocale data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressLocaleCreate"
        required: true
      responses:
        "201":
          description: AddressLocale created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/address-locales/{addressLocaleKey}:
    get:
      tags:
      - AddressLocale
      summary: Retrieve a specific AddressLocale entity.
      operationId: getAddressLocaleById
      responses:
        "200":
          description: The requested AddressLocale entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - AddressLocale
      summary: Replace a AddressLocale entity.
      operationId: replaceAddressLocale
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressLocale"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - AddressLocale
      summary: Delete a AddressLocale entity
      description: Marks the AddressLocale entity as inactive or r...
      operationId: deleteAddressLocaleEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - AddressLocale
      summary: Partially update a AddressLocale entity.
      operationId: updateAddressLocale
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AddressLocaleUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: addressLocaleKey
      in: path
      description: The unique identifier for the specific AddressLocale.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YW
  /staff-members/{staffMemberKey}/authorizations:
    get:
      tags:
      - Authorization
      summary: Retrieve a list of Authorization entities scope...
      operationId: listAuthorizationByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Authorization entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Authorization"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Authorization
      summary: Create a new Authorization entity.
      operationId: createAuthorization
      requestBody:
        description: Authorization data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthorizationCreate"
        required: true
      responses:
        "201":
          description: Authorization created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/authorizations/{authorizationKey}:
    get:
      tags:
      - Authorization
      summary: Retrieve a specific Authorization entity.
      operationId: getAuthorizationById
      responses:
        "200":
          description: The requested Authorization entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Authorization
      summary: Replace a Authorization entity.
      operationId: replaceAuthorization
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Authorization"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Authorization
      summary: Delete a Authorization entity
      description: Marks the Authorization entity as inactive or r...
      operationId: deleteAuthorizationEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Authorization
      summary: Partially update a Authorization entity.
      operationId: updateAuthorization
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AuthorizationUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: authorizationKey
      in: path
      description: The unique identifier for the specific Authorization.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWc
  /staff-members/{staffMemberKey}/moneys:
    get:
      tags:
      - Money
      summary: Retrieve a list of Money entities scoped by sta...
      operationId: listMoneyByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Money entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Money"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/moneys/{moneyKey}:
    get:
      tags:
      - Money
      summary: Retrieve a specific Money entity.
      operationId: getMoneyById
      responses:
        "200":
          description: The requested Money entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: moneyKey
      in: path
      description: The unique identifier for the specific Money.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWcz
  /staff-members/{staffMemberKey}/departments:
    get:
      tags:
      - Department
      summary: Retrieve a list of Department entities scoped b...
      operationId: listDepartmentByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Department entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Department"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Department
      summary: Create a new Department entity.
      operationId: createDepartment
      requestBody:
        description: Department data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DepartmentCreate"
        required: true
      responses:
        "201":
          description: Department created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/departments/{departmentKey}:
    get:
      tags:
      - Department
      summary: Retrieve a specific Department entity.
      operationId: getDepartmentById
      responses:
        "200":
          description: The requested Department entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Department
      summary: Replace a Department entity.
      operationId: replaceDepartment
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Department"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Department
      summary: Delete a Department entity
      description: Marks the Department entity as inactive or remo...
      operationId: deleteDepartmentEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Department
      summary: Partially update a Department entity.
      operationId: updateDepartment
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/DepartmentUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Department"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: departmentKey
      in: path
      description: The unique identifier for the specific Department.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczM
  /staff-members/{staffMemberKey}/metric-name-values:
    get:
      tags:
      - MetricNameValue
      summary: Retrieve a list of MetricNameValue entities sco...
      operationId: listMetricNameValueByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of MetricNameValue entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MetricNameValue"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - MetricNameValue
      summary: Create a new MetricNameValue entity.
      operationId: createMetricNameValue
      requestBody:
        description: MetricNameValue data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetricNameValueCreate"
        required: true
      responses:
        "201":
          description: MetricNameValue created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricNameValue"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/metric-name-values/{metricNameValueKey}:
    get:
      tags:
      - MetricNameValue
      summary: Retrieve a specific MetricNameValue entity.
      operationId: getMetricNameValueById
      responses:
        "200":
          description: The requested MetricNameValue entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricNameValue"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - MetricNameValue
      summary: Replace a MetricNameValue entity.
      operationId: replaceMetricNameValue
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetricNameValue"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricNameValue"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - MetricNameValue
      summary: Delete a MetricNameValue entity
      description: Marks the MetricNameValue entity as inactive or...
      operationId: deleteMetricNameValueEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - MetricNameValue
      summary: Partially update a MetricNameValue entity.
      operationId: updateMetricNameValue
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/MetricNameValueUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricNameValue"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: metricNameValueKey
      in: path
      description: The unique identifier for the specific MetricNameValue.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMs
  /staff-members/{staffMemberKey}/identifiers:
    get:
      tags:
      - Identifier
      summary: Retrieve a list of Identifier entities scoped b...
      operationId: listIdentifierByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Identifier entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Identifier"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Identifier
      summary: Create a new Identifier entity.
      operationId: createIdentifier
      requestBody:
        description: Identifier data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: Identifier created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/identifiers/{identifierKey}:
    get:
      tags:
      - Identifier
      summary: Retrieve a specific Identifier entity.
      operationId: getIdentifierById
      responses:
        "200":
          description: The requested Identifier entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Identifier
      summary: Replace a Identifier entity.
      operationId: replaceIdentifier
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Identifier
      summary: Delete a Identifier entity
      description: Marks the Identifier entity as inactive or remo...
      operationId: deleteIdentifierEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Identifier
      summary: Partially update a Identifier entity.
      operationId: updateIdentifier
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: identifierKey
      in: path
      description: The unique identifier for the specific Identifier.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMsS
  /staff-members/{staffMemberKey}/effective-periods:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a list of EffectivePeriod entities sco...
      operationId: listEffectivePeriodByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of EffectivePeriod entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EffectivePeriod"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - EffectivePeriod
      summary: Retrieve a specific EffectivePeriod entity.
      operationId: getEffectivePeriodById
      responses:
        "200":
          description: The requested EffectivePeriod entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: effectivePeriodKey
      in: path
      description: The unique identifier for the specific EffectivePeriod.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMsSO
  /staff-members/{staffMemberKey}/privacy-items:
    get:
      tags:
      - PrivacyItem
      summary: Retrieve a list of PrivacyItem entities scoped ...
      operationId: listPrivacyItemByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of PrivacyItem entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/PrivacyItem"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - PrivacyItem
      summary: Create a new PrivacyItem entity.
      operationId: createPrivacyItem
      requestBody:
        description: PrivacyItem data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyItemCreate"
        required: true
      responses:
        "201":
          description: PrivacyItem created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/privacy-items/{privacyItemKey}:
    get:
      tags:
      - PrivacyItem
      summary: Retrieve a specific PrivacyItem entity.
      operationId: getPrivacyItemById
      responses:
        "200":
          description: The requested PrivacyItem entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - PrivacyItem
      summary: Replace a PrivacyItem entity.
      operationId: replacePrivacyItem
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PrivacyItem"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - PrivacyItem
      summary: Delete a PrivacyItem entity
      description: Marks the PrivacyItem entity as inactive or rem...
      operationId: deletePrivacyItemEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - PrivacyItem
      summary: Partially update a PrivacyItem entity.
      operationId: updatePrivacyItem
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PrivacyItemUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrivacyItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: privacyItemKey
      in: path
      description: The unique identifier for the specific PrivacyItem.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMsSOA
  /staff-members/{staffMemberKey}/textual-details:
    get:
      tags:
      - TextualDetail
      summary: Retrieve a list of TextualDetail entities scope...
      operationId: listTextualDetailByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of TextualDetail entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/TextualDetail"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/textual-details/{textualDetailKey}:
    get:
      tags:
      - TextualDetail
      summary: Retrieve a specific TextualDetail entity.
      operationId: getTextualDetailById
      responses:
        "200":
          description: The requested TextualDetail entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: textualDetailKey
      in: path
      description: The unique identifier for the specific TextualDetail.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMsSOAF
  /staff-members/{staffMemberKey}/people:
    get:
      tags:
      - Person
      summary: Retrieve a list of Person entities scoped by st...
      operationId: listPersonByStaffMemberKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of Person entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Person"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Person
      summary: Create a new Person entity.
      operationId: createPerson
      requestBody:
        description: Person data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonCreate"
        required: true
      responses:
        "201":
          description: Person created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /part-search-results/PS-12345/bill-of-materials/BOM-001
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /staff-members/{staffMemberKey}/people/{personKey}:
    get:
      tags:
      - Person
      summary: Retrieve a specific Person entity.
      operationId: getPersonById
      responses:
        "200":
          description: The requested Person entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Person
      summary: Replace a Person entity.
      operationId: replacePerson
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Person"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Person
      summary: Delete a Person entity
      description: Marks the Person entity as inactive or removes ...
      operationId: deletePersonEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Person
      summary: Partially update a Person entity.
      operationId: updatePerson
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/PersonUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: staffMemberKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: personKey
      in: path
      description: The unique identifier for the specific Person.
      required: true
      schema:
        type: string
        example: KEY78GDk71Zm2YWczMsSOAFB
  /staff-members/health:
    get:
      summary: Health Check
      description: Public endpoint to verify service availability.
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                required:
                - status
                - version
                type: object
                properties:
                  status:
                    type: string
                    example: up
                    enum:
                    - up
                    - down
                    - maintenance
                  version:
                    type: string
                    description: The version of the ontology implemented by this service.
                    example: 1.0.0
                  timestamp:
                    type: string
                    description: Server time in ISO-8601 format.
                    format: date-time
                    example: 2025-12-21T23:00:00Z
      security:
      - {}
  /staff-members/batch:
    post:
      summary: Bulk create staff-members
      operationId: createStaffMemberBatch
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  maxItems: 100
                  type: array
                  items:
                    $ref: "#/components/schemas/StaffMemberCreate"
      responses:
        "200":
          description: Batch processing complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  succeeded:
                    type: array
                    items:
                      $ref: "#/components/schemas/StaffMember"
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        index:
                          type: integer
                          format: int32
                        error:
                          $ref: "#/components/schemas/ErrorDetail"
  /staff-members/{staffMemberKey}/commands:
    post:
      summary: Execute a business workflow on StaffMember
      description: Single entry point for all StaffMember state-changing intents.
      operationId: executeStaffMemberCommand
      parameters:
      - name: staffMemberKey
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Command intent and parameters
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: "The intent of the command (e.g., RescheduleSlot, CancelInvoice)"
                workflow_state:
                  type: string
                  description: "The intent of the command (e.g., CREATED, DELETED)"
                  enum:
                  - CREATED
                  - DELETED
                context:
                  type: object
                  additionalProperties: true
                  description: The payload required for this specific command type
                data:
                  $ref: "#/components/schemas/StaffMember"
      responses:
        "200":
          description: Commands processing complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: The execution status of the command
                    example: SUCCESS
                    enum:
                    - SUCCESS
                    - FAILED
                    - PENDING
                    - PROCESSING
                    - VALIDATION_ERROR
                    - PARTIAL_SUCCESS
                    - REQUIRES_ACTION
                    - MITIGATION_APPLIED
                  message:
                    type: string
                    description: A human-readable summary or error detail
                    example: "Invoice #9c29ad5a has been successfully cancelled."
                  commandId:
                    type: string
                    description: The tracking ID for this specific command execution
                    format: identifier
                  code:
                    type: string
                    description: detailed code for workflow business processing
                    example: cfacd925
                  retry_after:
                    type: string
                    description: "For PENDING or PROCESSING statuses, retry_after\
                      \ (integer, seconds) "
                  retryable:
                    type: boolean
                    description: "If true, the agent can attempt this command again\
                      \ immediately"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
  /staff-members/{staffMemberKey}/commands/{commandId}:
    get:
      summary: Poll StaffMember command status
      description: Check the progress or result of a previously submitted command.
      operationId: getStaffMemberCommandStatus
      parameters:
      - name: staffMemberKey
        in: path
        required: true
        schema:
          type: string
          format: string
      - name: commandId
        in: path
        required: true
        schema:
          type: string
          format: string
      responses:
        "200":
          description: Current status of the command
          content:
            application/json:
              schema:
                type: object
                properties:
                  commandId:
                    type: string
                    description: The unique identifier for the command being tracked
                    format: uuid
                  status:
                    type: string
                    description: The current lifecycle state of the command
                    enum:
                    - PENDING
                    - PROCESSING
                    - SUCCESS
                    - FAILED
                    - REQUIRES_ACTION
                  progress:
                    maximum: 100
                    minimum: 0
                    type: integer
                    description: Percentage of completion (0-100)
                    format: int32
                  message:
                    type: string
                    description: Status update or error message
                  result:
                    type: object
                    additionalProperties: true
                    description: The final output of the command (only populated on
                      SUCCESS)
                  errors:
                    type: array
                    items:
                      $ref: "#/components/schemas/ErrorDetail"
  /staff-members/capabilities:
    get:
      summary: Discover StaffMember State Machine
      operationId: getStaffMemberCapabilities
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  aggregate:
                    type: string
                    example: StaffMember
                  version:
                    type: string
                    example: 1.2.0
                  commands:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                          example: CancelInvoice
                        description:
                          type: string
                          example: Voids the invoice and releases held funds.
                        from_states:
                          type: array
                          example:
                          - CREATED
                          - ACTIVE
                          items:
                            type: string
                        to_state:
                          type: string
                          example: DELETED
                        parameters_ref:
                          type: string
                          example: "#/components/schemas/StaffMember"
components:
  schemas:
    Address:
      type: object
      properties:
        address_locales:
          $ref: "#/components/schemas/AddressLocales"
        priority:
          maxLength: 10
          minLength: 1
          type: string
          description: Priority indicator for address usage (e....
        city_code:
          maxLength: 20
          minLength: 1
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized city code. Pattern: ^[a-zA-Z0-9_]+..."
        address_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique business key for the address. Pattern: ^..."
        address_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the address record... Pat...
        address_input:
          maxLength: 255
          minLength: 1
          type: string
          description: Raw input provided for address processin...
        address_line1:
          maxLength: 100
          minLength: 1
          type: string
          description: "Primary address line (Street, PO Box, et..."
        address_line2:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line3:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_line4:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_line5:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/AddressTypes"
        authorizations:
          $ref: "#/components/schemas/Authorizations"
        city:
          maxLength: 100
          minLength: 1
          type: string
          description: Name of the city.
        city_subdivision:
          maxLength: 100
          type: string
          description: "Subdivision within the city (e.g., neigh..."
        country:
          maxLength: 100
          minLength: 2
          type: string
          description: Full name of the country.
        country_code:
          maxLength: 2
          minLength: 2
          pattern: "^[A-Z]{2}$"
          type: string
          description: "ISO 3166-1 Alpha-2 country code. Pattern: ^[A-Z..."
        county:
          maxLength: 100
          type: string
          description: Name of the county.
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        label:
          maxLength: 50
          type: string
          description: Label or display name for the address.
        position:
          $ref: "#/components/schemas/Position"
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: string
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
        postal_code_ext:
          maxLength: 10
          type: string
          description: "Extended postal code (e.g., ZIP+4)."
        preferred_indicator:
          type: boolean
          description: Flag indicating if this address is the p...
        region:
          maxLength: 50
          type: string
          description: "Administrative region (e.g., province, s..."
        state:
          maxLength: 50
          type: string
          description: State or equivalent subdivision.
        locale:
          maxLength: 10
          minLength: 2
          pattern: "^[a-z]{2}(-[A-Z]{2})?$"
          type: string
          description: Locale code associated with this address... Pat...
      description: Address represents a physical or mailing locati...
    AddressCreate:
      required:
      - address_key
      type: object
      description: Address Create for Post
      allOf:
      - $ref: "#/components/schemas/Address"
    AddressLocale:
      type: object
      properties:
        address_locale_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Business key for the address locale. Pattern: ^..."
        address_locale_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the address locale... Pat...
        region_code:
          maxLength: 20
          type: string
          description: Code representing the region for the loc...
        region_name:
          maxLength: 100
          type: string
          description: Name of the region for the locale.
        region_aliases:
          $ref: "#/components/schemas/Codes"
        location_type:
          $ref: "#/components/schemas/LocationTypes"
      description: External system address locale including region...
    AddressLocaleCreate:
      required:
      - address_locale_key
      type: object
      description: AddressLocale Create for Post
      allOf:
      - $ref: "#/components/schemas/AddressLocale"
    AddressLocaleUpdate:
      type: object
      properties:
        region_name:
          maxLength: 100
          type: string
          description: Name of the region for the locale.
        region_aliases:
          $ref: "#/components/schemas/Codes"
        location_type:
          $ref: "#/components/schemas/LocationTypes"
        region_code:
          maxLength: 20
          type: string
          description: Code representing the region for the loc...
      description: AddressLocale Update for Patch and Put
    AddressLocales:
      type: array
      items:
        $ref: "#/components/schemas/AddressLocale"
    AddressTypes:
      type: string
      description: types of address.
      enum:
      - BILLING
      - SHIPPING
      - MAILING
      - POSTAL
      - HOME
      - RESIDENCE
      - WORK
      - BUSINESS
      - LEGAL
      - SERVICE
      - DEALER
      - CONTRACT
      - AR_ADDRESS
      - RECIPIENT
      - DELIVERY
      - DESTINATION
      - ORIGINAL
      - CURRENT
      - PREVIOUS
      - TEMPORARY
      - VACATION
      - GARAGE
      - EMERGENCY
      - REBATE
      - SERVICE_CONTRACT
      - INSURANCE
      - PRODUCT
      - ARCHIVE
      - SOLD_BY
      - SHIPPED_BY
      - OTHER
    AddressUpdate:
      type: object
      properties:
        address_input:
          maxLength: 255
          minLength: 1
          type: string
          description: Raw input provided for address processin...
        country:
          maxLength: 100
          minLength: 2
          type: string
          description: Full name of the country.
        city:
          maxLength: 100
          minLength: 1
          type: string
          description: Name of the city.
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        authorizations:
          $ref: "#/components/schemas/Authorizations"
        county:
          maxLength: 100
          type: string
          description: Name of the county.
        city_code:
          maxLength: 20
          minLength: 1
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized city code. Pattern: ^[a-zA-Z0-9_]+..."
        locale:
          maxLength: 10
          minLength: 2
          pattern: "^[a-z]{2}(-[A-Z]{2})?$"
          type: string
          description: Locale code associated with this address... Pat...
        address_locales:
          $ref: "#/components/schemas/AddressLocales"
        address_line5:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_line4:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_line3:
          maxLength: 100
          type: string
          description: Additional address line information.
        address_line2:
          maxLength: 100
          type: string
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line1:
          maxLength: 100
          minLength: 1
          type: string
          description: "Primary address line (Street, PO Box, et..."
        postal_code_ext:
          maxLength: 10
          type: string
          description: "Extended postal code (e.g., ZIP+4)."
        state:
          maxLength: 50
          type: string
          description: State or equivalent subdivision.
        preferred_indicator:
          type: boolean
          description: Flag indicating if this address is the p...
        city_subdivision:
          maxLength: 100
          type: string
          description: "Subdivision within the city (e.g., neigh..."
        label:
          maxLength: 50
          type: string
          description: Label or display name for the address.
        priority:
          maxLength: 10
          minLength: 1
          type: string
          description: Priority indicator for address usage (e....
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/AddressTypes"
        country_code:
          maxLength: 2
          minLength: 2
          pattern: "^[A-Z]{2}$"
          type: string
          description: "ISO 3166-1 Alpha-2 country code. Pattern: ^[A-Z..."
        position:
          $ref: "#/components/schemas/Position"
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: string
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
        region:
          maxLength: 50
          type: string
          description: "Administrative region (e.g., province, s..."
      description: Address Update for Patch and Put
    Addresses:
      type: array
      items:
        $ref: "#/components/schemas/Address"
    Authorization:
      type: object
      properties:
        update_date_time:
          type: string
          description: Timestamp of the last update to this aut...
          format: date-time
        authorized_indicator:
          type: boolean
          description: Indicates whether the authorization is c...
        authorization_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Business key uniquely identifying the au... Pat...
      description: Establish authorization level
    AuthorizationCreate:
      required:
      - authorization_key
      type: object
      description: Authorization Create for Post
      allOf:
      - $ref: "#/components/schemas/Authorization"
    AuthorizationUpdate:
      type: object
      properties:
        authorized_indicator:
          type: boolean
          description: Indicates whether the authorization is c...
        update_date_time:
          type: string
          description: Timestamp of the last update to this aut...
          format: date-time
      description: Authorization Update for Patch and Put
    Authorizations:
      type: array
      items:
        $ref: "#/components/schemas/Authorization"
    Codes:
      type: array
      items:
        type: string
    CommunicationChannel:
      type: object
      properties:
        communication_channel_key:
          type: string
          description: The unique key for the communication cha...
        communication_channel_id:
          type: string
          description: The unique ID for the communication chan...
        communication_channel_type:
          $ref: "#/components/schemas/CommunicationChannelTypes"
        value:
          type: string
          description: The value of the communication channel (...
        value_extension:
          type: string
          description: An optional extension for the communicat...
        label:
          type: string
          description: A label for the communication channel (e...
        country:
          type: string
          description: The country associated with the communic...
        preferred_indicator:
          type: boolean
          description: A boolean indicator of whether this is t...
        preferences:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeSlot"
        authorizations:
          $ref: "#/components/schemas/Authorizations"
      description: "Represents a communication channel, such as an ..."
    CommunicationChannelCreate:
      required:
      - communication_channel_key
      type: object
      description: CommunicationChannel Create for Post
      allOf:
      - $ref: "#/components/schemas/CommunicationChannel"
    CommunicationChannelTypes:
      type: string
      description: types of communication channels.
      enum:
      - PHONE
      - CELL
      - SMS
      - VOICE
      - VIDEO
      - EMAIL
      - APP
      - SOCIAL_MEDIA
      - URL
      - BROADCAST
      - DIRECT_MAIL
      - PDA
      - OTHER
    CommunicationChannelUpdate:
      type: object
      properties:
        country:
          type: string
          description: The country associated with the communic...
        communication_channel_type:
          $ref: "#/components/schemas/CommunicationChannelTypes"
        preferences:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeSlot"
        authorizations:
          $ref: "#/components/schemas/Authorizations"
        label:
          type: string
          description: A label for the communication channel (e...
        value:
          type: string
          description: The value of the communication channel (...
        value_extension:
          type: string
          description: An optional extension for the communicat...
        preferred_indicator:
          type: boolean
          description: A boolean indicator of whether this is t...
      description: CommunicationChannel Update for Patch and Put
    CommunicationChannels:
      type: array
      items:
        $ref: "#/components/schemas/CommunicationChannel"
    ControlAccount:
      type: object
      properties:
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        control_account_desc:
          maxLength: 255
          type: string
          description: Description of the control account.
        control_account_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the control accoun... Pat...
        control_account_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Business key for the control account. Pattern: ..."
        control_account_name:
          maxLength: 100
          type: string
          description: Display name of the control account.
        control_account_type:
          $ref: "#/components/schemas/ControlAccountTypes"
        control_type_id:
          type: string
          description: Identifier for the control type of the a...
        bank_account_id:
          type: string
          description: Associated bank account identifier.
        is_counted:
          type: boolean
          description: Indicates if the account is a counted ac...
        prefix:
          type: string
          description: The prefix of the account number.
        control_account_role_type:
          $ref: "#/components/schemas/ControlAccountRoleTypes"
        control_account_status_types:
          $ref: "#/components/schemas/ControlAccountStatusTypes"
        financial_category_type:
          $ref: "#/components/schemas/FinancialCategoryTypes"
      description: "Journal entry for general ledger, capturing con..."
    ControlAccountCreate:
      required:
      - control_account_key
      type: object
      description: ControlAccount Create for Post
      allOf:
      - $ref: "#/components/schemas/ControlAccount"
    ControlAccountRoleTypes:
      type: string
      description: types of control account roles.
      enum:
      - MASTER
      - SUB
      - SPLIT
      - STANDARD
      - OTHER
    ControlAccountStatusTypes:
      type: string
      description: types of control account status.
      enum:
      - INITIALIZE
      - CREATED
      - ACTIVATE
      - ACTIVE
      - SUSPEND_FOR_REVIEW
      - REACTIVATE
      - SUSPENDED
      - PERMANENTLY_CLOSED
      - RETIRE
      - TERMINATED
    ControlAccountTypes:
      type: string
      description: types of control accounts.
      enum:
      - ASSETS
      - FIXED_ASSETS
      - CONTRA_ASSET
      - LIABILITY
      - LONG_TERM_LIABILITY
      - EQUITY
      - CLEARING
      - PROVISION
      - ROUNDING
      - REVENUE
      - OTHER_INCOME
      - COGS
      - EXPENSE
      - DEPRECIATION
      - TAX
      - DISCOUNT
      - REBATE
      - FEE
      - SERVICE
      - CREDIT
      - OTHER
      - DEALER
      - VEHICLE
      - ACCESSORIES
      - OPTION
      - CUSTOMER
      - VENDOR
      - EMPLOYEE
      - PRODUCT
      - REPAIR_ORDER
      - PARTS_SALES_ORDER
      - PACKAGE
      - REFERENCE
    ControlAccountUpdate:
      type: object
      properties:
        control_account_role_type:
          $ref: "#/components/schemas/ControlAccountRoleTypes"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        prefix:
          type: string
          description: The prefix of the account number.
        control_account_desc:
          maxLength: 255
          type: string
          description: Description of the control account.
        control_account_type:
          $ref: "#/components/schemas/ControlAccountTypes"
        control_account_status_types:
          $ref: "#/components/schemas/ControlAccountStatusTypes"
        is_counted:
          type: boolean
          description: Indicates if the account is a counted ac...
        control_account_name:
          maxLength: 100
          type: string
          description: Display name of the control account.
        financial_category_type:
          $ref: "#/components/schemas/FinancialCategoryTypes"
      description: ControlAccount Update for Patch and Put
    ControlAccounts:
      type: array
      items:
        $ref: "#/components/schemas/ControlAccount"
    DailyHour:
      type: object
      properties:
        daily_hour_key:
          type: string
          description: A unique key for the daily hour record
        start_day:
          type: string
          description: The starting day of the week for the hou...
        end_day:
          type: string
          description: The ending day of the week for the hours
        opening_time:
          type: string
          description: The opening time for the given day
          format: date-time
        closing_time:
          type: string
          description: The closing time for the given day
          format: date-time
        week_starting_date:
          type: string
          description: The starting date of the week
          format: date
        week_ending_date:
          type: string
          description: The ending date of the week
          format: date
      description: Daily Hours that the department is open
    DailyHourCreate:
      required:
      - daily_hour_key
      type: object
      description: DailyHour Create for Post
      allOf:
      - $ref: "#/components/schemas/DailyHour"
    DailyHourUpdate:
      type: object
      properties:
        end_day:
          type: string
          description: The ending day of the week for the hours
        week_starting_date:
          type: string
          description: The starting date of the week
          format: date
        week_ending_date:
          type: string
          description: The ending date of the week
          format: date
        start_day:
          type: string
          description: The starting day of the week for the hou...
        closing_time:
          type: string
          description: The closing time for the given day
          format: date-time
        opening_time:
          type: string
          description: The opening time for the given day
          format: date-time
      description: DailyHour Update for Patch and Put
    DaysOfWeekTypes:
      type: string
      description: types of days of weeks.
      enum:
      - SUN
      - MON
      - TUES
      - WED
      - THURS
      - FRI
      - SAT
    Department:
      type: object
      properties:
        department_key:
          type: string
          description: A unique key for the department.
        department_id:
          type: string
          description: The unique ID for the department.
        department_codes:
          $ref: "#/components/schemas/Codes"
        department_name:
          type: string
          description: The name of the department.
        department_function:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        department_contact:
          type: string
          description: The contact name for the department.
        comments:
          type: string
          description: General comments about the department.
        special_remarks_description:
          type: string
          description: A description of special remarks about t...
        contact_methods_href:
          type: string
          description: A hyperlink to the department's contact ...
          format: href
        departments_href:
          type: string
          description: A hyperlink to related departments.
          format: href
        daily_hours:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/DailyHour"
        links:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/Link"
        department_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/DepartmentTypes"
        control_accounts:
          $ref: "#/components/schemas/ControlAccounts"
      description: Dealership Department
    DepartmentCreate:
      required:
      - department_key
      type: object
      description: Department Create for Post
      allOf:
      - $ref: "#/components/schemas/Department"
    DepartmentTypes:
      type: string
      description: types of departments.
      enum:
      - REGULAR_INSPECTION
      - PDI_INSPECTION
      - QUICK_LUBE
      - ACCESSORY_INSTALL
      - FIELD_ACTION
      - SALES
      - SERVICE
      - FLEET
      - F_I
      - USED_VEHICLES
      - BODY_SHOP
    DepartmentUpdate:
      type: object
      properties:
        departments_href:
          type: string
          description: A hyperlink to related departments.
          format: href
        department_function:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        comments:
          type: string
          description: General comments about the department.
        control_accounts:
          $ref: "#/components/schemas/ControlAccounts"
        department_name:
          type: string
          description: The name of the department.
        special_remarks_description:
          type: string
          description: A description of special remarks about t...
        links:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/Link"
        department_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/DepartmentTypes"
        department_codes:
          $ref: "#/components/schemas/Codes"
        department_contact:
          type: string
          description: The contact name for the department.
        contact_methods_href:
          type: string
          description: A hyperlink to the department's contact ...
          format: href
        daily_hours:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/DailyHour"
      description: Department Update for Patch and Put
    DurationUOMTypes:
      type: string
      description: types of duration u o ms.
      enum:
      - MINUTES
      - HOURS
      - DAYS
      - DAY_OF_MONTH
      - WEEKS
      - BI_WEEKS
      - SEMI_MONTHS
      - MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - CUSTOM
      - OCCURRENCE
      - UNLIMITED
      - NONE
      - OTHER
    EffectivePeriod:
      type: object
      properties:
        effective_period_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique key identifying the effective per... Pat...
        active_indicator:
          type: boolean
          description: Indicator if the period is currently act...
        exclusive_indicator:
          type: boolean
          description: Indicator if the period is exclusive
        start_date_time:
          type: string
          description: Start date and time of the effective per...
          format: date-time
        complete_date_time:
          type: string
          description: Completion date and time of the effectiv...
          format: date-time
        duration_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        duration_value:
          type: string
          description: Value of the duration
        recur_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        recur_count:
          type: string
          description: Count of recurrence
        recur_values:
          $ref: "#/components/schemas/Codes"
        recur_criteria:
          type: string
          description: Criteria defining recurrence
        recur_indicator:
          type: boolean
          description: Indicator if recurrence is enabled
        timeslot:
          $ref: "#/components/schemas/TimeSlot"
      description: Effective Period
    EffectivePeriodCreate:
      required:
      - effective_period_key
      type: object
      description: EffectivePeriod Create for Post
      allOf:
      - $ref: "#/components/schemas/EffectivePeriod"
    EffectivePeriodUpdate:
      type: object
      properties:
        recur_criteria:
          type: string
          description: Criteria defining recurrence
        recur_count:
          type: string
          description: Count of recurrence
        exclusive_indicator:
          type: boolean
          description: Indicator if the period is exclusive
        recur_indicator:
          type: boolean
          description: Indicator if recurrence is enabled
        timeslot:
          $ref: "#/components/schemas/TimeSlot"
        duration_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        complete_date_time:
          type: string
          description: Completion date and time of the effectiv...
          format: date-time
        recur_values:
          $ref: "#/components/schemas/Codes"
        start_date_time:
          type: string
          description: Start date and time of the effective per...
          format: date-time
        recur_uom_type:
          $ref: "#/components/schemas/DurationUOMTypes"
        active_indicator:
          type: boolean
          description: Indicator if the period is currently act...
        duration_value:
          type: string
          description: Value of the duration
      description: EffectivePeriod Update for Patch and Put
    ErrorDetail:
      required:
      - category
      - code
      - message
      - timestamp
      - traceId
      type: object
      properties:
        code:
          type: string
          description: "A machine-readable, unique system error code (e..."
          example: NOT_FOUND_PR
        message:
          type: string
          description: A developer-friendly explanation of the error.
          example: Personnel Record with ID PR-1234567 was not found.
        category:
          type: string
          description: Classifies the error to help clients determine ...
          example: DATA_INTEGRITY
          enum:
          - VALIDATION
          - AUTHENTICATION
          - AUTHORIZATION
          - INFRASTRUCTURE
          - BUSINESS_LOGIC
          - DATA_INTEGRITY
        timestamp:
          type: string
          description: The time the error occurred (ISO 8601).
          format: date-time
          example: 2025-11-10T10:30:00Z
        traceId:
          type: string
          description: "A unique identifier for this specific request, ..."
          example: 57c31110-859a-11ec-a8a3-0242ac120002
        docUrl:
          type: string
          description: A URL leading to the documentation page for thi...
          format: uri
          example: https://api.docs.com/errors/NOT_FOUND_PR
        details:
          type: array
          description: Field-level validation errors or granular diagn...
          items:
            type: object
            properties:
              field:
                type: string
                description: The JSON path or field name that caused the error.
                example: payrollId
              issue:
                type: string
                description: The specific validation rule that failed.
                example: must be unique
              rejectedValue:
                type: string
                description: The value that was sent and rejected (only if non-sensitive).
                example: "12345"
      description: Standardized error response for the entire API ...
    FinancialCategoryTypes:
      type: string
      description: types of financial categorys.
      enum:
      - CAPEX
      - OPEX
      - HARDWARE
      - SOFTWARE
      - MAINTENANCE
      - TRAVEL
      - TRAINING
      - UTILITIES
      - RENT
      - SALARY
      - REVENUE
      - TAX
      - OTHER
    GenderTypes:
      type: string
      description: types of genders.
      enum:
      - MALE
      - FEMALE
      - UNKNOWN
      - TRANS_MALE
      - TRANS_FEMALE
      - NA
    Identifier:
      type: object
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: string
          description: Context or domain of the identifier
        value:
          type: string
          description: The monetary or hours-based value of the...
        detail:
          type: string
          description: The monetary or hours-based value of the...
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: External system identifier
    IdentifierCreate:
      required:
      - identifier_key
      type: object
      description: Identifier Create for Post
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        context:
          type: string
          description: Context or domain of the identifier
        detail:
          type: string
          description: The monetary or hours-based value of the...
        value:
          type: string
          description: The monetary or hours-based value of the...
      description: Identifier Update for Patch and Put
    Identifiers:
      type: array
      items:
        $ref: "#/components/schemas/Identifier"
    Link:
      type: object
      properties:
        link_key:
          type: string
          description: The unique key for the link.
        href:
          type: string
          description: The URL of the related resource.
        method:
          type: string
          description: The HTTP method to be used to access the...
        rel:
          type: string
          description: "The link relation, which describes the r..."
        title:
          type: string
          description: A human-readable title for the link.
      description: Represents a Hypermedia as the Engine of Applic...
    LinkCreate:
      required:
      - link_key
      type: object
      description: Link Create for Post
      allOf:
      - $ref: "#/components/schemas/Link"
    LinkUpdate:
      type: object
      properties:
        method:
          type: string
          description: The HTTP method to be used to access the...
        rel:
          type: string
          description: "The link relation, which describes the r..."
        href:
          type: string
          description: The URL of the related resource.
        title:
          type: string
          description: A human-readable title for the link.
      description: Link Update for Patch and Put
    LocationTypes:
      type: string
      description: types of locations.
      enum:
      - BOROUGH
      - PARISH
      - REGION
      - BOUNDARY
      - PROVINCE
      - VILLAGE
      - STATE
      - CITY
      - COUNTRY
      - LOCALITY
      - POSTAL
      - STREET
      - SUB_ADMIN_AREA
      - SUB_LOCALITY
      - FEDERAL
      - COUNTY
      - TELEMATICS
      - ADDRESS
      - NO_PREFERENCE
      - GPS
      - WAREHOUSE
      - DISTRICT
    MaritalStatusTypes:
      type: string
      description: types of marital status.
      enum:
      - SINGLE
      - MARRIED
      - COMMON_LAW
      - SEPARATED
      - DIVORCED
      - WIDOWED
      - ANNULLED
      - OTHER
      - UNKNOWN
    Memo:
      type: array
      items:
        type: string
    MetricNameValue:
      type: object
      properties:
        metric_name_value_key:
          type: string
          description: A unique key for the metric name value r...
        metric_name_value_id:
          type: string
          description: The unique ID for the metric name value.
        header_sequence:
          type: string
          description: The header sequence for the metric value...
        uom:
          $ref: "#/components/schemas/NarrativeUomTypes"
        value:
          type: string
          description: The value of the metric.
        value_category:
          type: string
          description: The category of the metric value.
        value_description:
          type: string
          description: A description of the metric value.
        value_name:
          type: string
          description: The name of the metric value.
        codes:
          $ref: "#/components/schemas/Codes"
      description: A metric value with a name and unit of measure.
    MetricNameValueCreate:
      required:
      - metric_name_value_key
      type: object
      description: MetricNameValue Create for Post
      allOf:
      - $ref: "#/components/schemas/MetricNameValue"
    MetricNameValueUpdate:
      type: object
      properties:
        header_sequence:
          type: string
          description: The header sequence for the metric value...
        codes:
          $ref: "#/components/schemas/Codes"
        uom:
          $ref: "#/components/schemas/NarrativeUomTypes"
        value_category:
          type: string
          description: The category of the metric value.
        value_description:
          type: string
          description: A description of the metric value.
        value:
          type: string
          description: The value of the metric.
        value_name:
          type: string
          description: The name of the metric value.
      description: MetricNameValue Update for Patch and Put
    MetricNameValues:
      type: array
      items:
        $ref: "#/components/schemas/MetricNameValue"
    Money:
      type: object
      properties:
        money_key:
          type: string
          description: The unique key for the monetary value.
        money_id:
          type: string
          description: The unique ID for the monetary value.
        currency_code:
          type: string
          description: "ISO currency code, e.g., 'USD'."
        display_value:
          type: string
          description: The locale-specific display format for t...
        locale:
          type: string
          description: "The locale code (e.g., en-US, fr-FR)."
        value:
          type: string
          description: The monetary value.
      description: "Represents a monetary value, including its curr..."
    MoneyCreate:
      required:
      - money_key
      type: object
      description: Money Create for Post
      allOf:
      - $ref: "#/components/schemas/Money"
    MoneyUpdate:
      type: object
      properties:
        display_value:
          type: string
          description: The locale-specific display format for t...
        locale:
          type: string
          description: "The locale code (e.g., en-US, fr-FR)."
        value:
          type: string
          description: The monetary value.
        currency_code:
          type: string
          description: "ISO currency code, e.g., 'USD'."
      description: Money Update for Patch and Put
    NarrativeUomTypes:
      type: string
      description: types of narrative uoms.
      enum:
      - BAR
      - KPA
      - PSI
      - OZ
      - LB
      - KG
      - GRAMS
      - TONS
      - LITER
      - GALLON
      - FLUID_OUNCE
      - CUBIC_METER
      - KM
      - MILES
      - MI
      - INCH
      - FEET
      - YARD
      - FATHOM
      - NAUTICAL
      - METER
      - CENTIMETER
      - MILLIMETER
      - DURATION
      - SECONDS
      - MINUTES
      - HOURS
      - DAYS
      - WEEKS
      - DAY_OF_MONTH
      - BI_WEEKS
      - SEMI_MONTHS
      - MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - DATE_TIME
      - DATE
      - TIME
      - CELSIUS
      - FAHRENHEIT
      - KELVIN
      - RANKINE
      - VOLT
      - AMPERE
      - OHM
      - WATT
      - KILOWATT_HOUR
      - RPM
      - G_FORCE
      - DEGREES
      - OCCURRENCE
      - GROUP
      - CODE
      - CURRENCY
      - POINTS
      - CUSTOM
      - UNLIMITED
      - NONE
      - LABEL
      - PERCENT
      - DESC
      - TEXT
      - KEY
      - PROTECTED_TEXT
      - BINARY
      - STRING
      - NUMBER
      - INTEGER
      - BOOLEAN
      - ARRAY
      - OBJECT
      - FLOAT
      - DOUBLE
      - INT32
      - INT64
      - OTHER
    PartyTypes:
      type: string
      description: types of partys.
      enum:
      - DEALER
      - EMPLOYEE
      - CUSTOMER
      - ORGANIZATION
      - VENDOR
      - SUPPLIER
      - PUBLIC
      - TAX
      - SERVICE
      - PROSPECT
      - PARTNER
      - CONTRACTOR
      - DEVICE
      - FLEET
      - DRIVER
      - FINANCE
      - GOVERNMENT
    PayrollCycleFrequencyTypes:
      type: string
      description: types of payroll cycle frequencys.
      enum:
      - ANNUAL
      - SEMI_ANNUAL
      - QUARTERLY
      - BI_MONTHLY
      - MONTHLY
      - SEMI_MONTHLY
      - BI_WEEKLY
      - WEEKLY
      - DAILY
      - OFF_CYCLE
      - ON_DEMAND
    PayrollRate:
      type: object
      properties:
        payroll_rate_id:
          type: string
          description: A unique identifier for the payroll rate...
        payroll_rate_key:
          type: string
          description: A unique identifier for the payroll rate...
        staff_member_key:
          type: string
          description: Business key or code for the staff membe...
          example: EMPLOYEE_001
        payroll_cycle_type:
          $ref: "#/components/schemas/PayrollCycleFrequencyTypes"
        amount:
          $ref: "#/components/schemas/Money"
        effective_period:
          $ref: "#/components/schemas/EffectivePeriod"
        occurrence_date_time:
          type: string
          description: The date and time the rate occurred.
          format: date-time
        textual_details:
          $ref: "#/components/schemas/TextualDetails"
        staff_pay_type:
          $ref: "#/components/schemas/StaffPayTypes"
      description: Represents a single rate that affects a staff m...
    PayrollRateCreate:
      required:
      - payroll_rate_key
      type: object
      description: PayrollRate Create for Post
      allOf:
      - $ref: "#/components/schemas/PayrollRate"
    PayrollRateUpdate:
      type: object
      properties:
        textual_details:
          $ref: "#/components/schemas/TextualDetails"
        amount:
          $ref: "#/components/schemas/Money"
        staff_pay_type:
          $ref: "#/components/schemas/StaffPayTypes"
        effective_period:
          $ref: "#/components/schemas/EffectivePeriod"
        payroll_cycle_type:
          $ref: "#/components/schemas/PayrollCycleFrequencyTypes"
        occurrence_date_time:
          type: string
          description: The date and time the rate occurred.
          format: date-time
      description: PayrollRate Update for Patch and Put
    PayrollRates:
      type: array
      items:
        $ref: "#/components/schemas/PayrollRate"
    Person:
      type: object
      properties:
        person_id:
          type: string
          description: Unique person identifier.
        person_key:
          type: string
          description: Business key for the person.
          example: PER-12345
        addresses:
          $ref: "#/components/schemas/Addresses"
        age:
          maximum: 130
          minimum: 0
          type: integer
          description: Age of the person in years.
          format: int32
          example: "35"
        birth_date:
          type: string
          description: Birth date of the person.
          format: date
          example: 1985-08-25
        comments:
          type: string
          description: Additional comments about the person.
          example: No known allergies.
        communication_channels:
          $ref: "#/components/schemas/CommunicationChannels"
        country_code:
          type: string
          description: ISO 3166-1 alpha-2 country code.
          example: US
        gender_type:
          $ref: "#/components/schemas/GenderTypes"
        birth_country:
          type: string
          description: ISO 3166-1 alpha-2 code for the country ...
          example: US
        citizenship_country:
          type: string
          description: ISO 3166-1 alpha-2 code for citizenship ...
          example: US
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        identifiers_href:
          type: string
          description: Link to identifiers resource.
          format: href
        language_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        marital_status_type:
          $ref: "#/components/schemas/MaritalStatusTypes"
        org_profiles_href:
          type: string
          description: Link to organizational profiles resource...
          format: href
        party_key:
          type: string
          description: Business key for the party.
          example: PTY-12345
        party_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PartyTypes"
        person_attributes:
          $ref: "#/components/schemas/MetricNameValues"
        preferred_language_code:
          type: string
          description: Preferred language code.
          example: en-US
        privacies:
          $ref: "#/components/schemas/PrivacyItems"
        privacies_href:
          type: string
          description: Link to privacy settings resource.
          format: href
        special_remarks_description:
          type: string
          description: Special remarks about the person.
        person_name:
          $ref: "#/components/schemas/PersonName"
      description: a Base Person or Organization
    PersonCreate:
      required:
      - person_key
      type: object
      description: Person Create for Post
      allOf:
      - $ref: "#/components/schemas/Person"
    PersonName:
      type: object
      properties:
        alias:
          maxLength: 40
          type: string
          description: Alternative or informal name.
          example: AJ
        family_name:
          maxLength: 50
          minLength: 3
          type: string
          description: Family or last name.
          example: Smith
        full_name:
          maxLength: 50
          type: string
          description: Complete name as one string.
          example: Alice Jane Smith
        given_name:
          maxLength: 50
          minLength: 1
          type: string
          description: First or given name.
          example: Alice
        identifiers_href:
          maxLength: 255
          type: string
          description: Reference to external identifiers.
          format: href
          example: /identifiers/12345
        maiden_name:
          maxLength: 50
          type: string
          description: Previous family name before marriage.
          example: Johnson
        middle_name:
          maxLength: 50
          type: string
          description: Middle name(s).
          example: Jane
        name_suffix:
          maxLength: 20
          type: string
          description: "Suffix such as Jr., Sr., III."
          example: Jr.
        party_key:
          maxLength: 40
          type: string
          description: External party key.
          example: PARTY-KEY-123
        person_key:
          maxLength: 40
          type: string
          description: External person key.
          example: PERSON-KEY-789
        person_name_key:
          maxLength: 40
          type: string
          description: Unique person name key.
          example: NAME-KEY-456
        preferred_name:
          maxLength: 40
          type: string
          description: Preferred name used in informal situatio...
          example: Ali
        salutation:
          maxLength: 20
          type: string
          description: "Salutation or title such as Mr., Ms., Dr..."
          example: Dr.
        honorific_prefix:
          type: string
          description: Prefix title used before the person's na...
          example: Dr.
        honorific_suffix:
          maxLength: 20
          type: string
          description: Suffix title used after the person's nam...
          example: Jr.
        locale:
          maxLength: 20
          type: string
          description: Locale or language region associated wit...
          example: en-US
        phonetic_family_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the family na...
          example: FAM-nee
        phonetic_given_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the given nam...
          example: GA-vin
        phonetic_middle_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the middle na...
          example: MI-chal
        nationality:
          maxLength: 60
          type: string
          description: Nationality of the person.
          example: Canadian
      description: a Name for a person
    PersonNameCreate:
      required:
      - person_name_key
      type: object
      description: PersonName Create for Post
      allOf:
      - $ref: "#/components/schemas/PersonName"
    PersonNameUpdate:
      type: object
      properties:
        phonetic_family_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the family na...
          example: FAM-nee
        maiden_name:
          maxLength: 50
          type: string
          description: Previous family name before marriage.
          example: Johnson
        name_suffix:
          maxLength: 20
          type: string
          description: "Suffix such as Jr., Sr., III."
          example: Jr.
        honorific_prefix:
          type: string
          description: Prefix title used before the person's na...
          example: Dr.
        given_name:
          maxLength: 50
          minLength: 1
          type: string
          description: First or given name.
          example: Alice
        middle_name:
          maxLength: 50
          type: string
          description: Middle name(s).
          example: Jane
        locale:
          maxLength: 20
          type: string
          description: Locale or language region associated wit...
          example: en-US
        honorific_suffix:
          maxLength: 20
          type: string
          description: Suffix title used after the person's nam...
          example: Jr.
        phonetic_given_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the given nam...
          example: GA-vin
        full_name:
          maxLength: 50
          type: string
          description: Complete name as one string.
          example: Alice Jane Smith
        phonetic_middle_name:
          maxLength: 60
          type: string
          description: Phonetic representation of the middle na...
          example: MI-chal
        nationality:
          maxLength: 60
          type: string
          description: Nationality of the person.
          example: Canadian
        alias:
          maxLength: 40
          type: string
          description: Alternative or informal name.
          example: AJ
        salutation:
          maxLength: 20
          type: string
          description: "Salutation or title such as Mr., Ms., Dr..."
          example: Dr.
        family_name:
          maxLength: 50
          minLength: 3
          type: string
          description: Family or last name.
          example: Smith
        identifiers_href:
          maxLength: 255
          type: string
          description: Reference to external identifiers.
          format: href
          example: /identifiers/12345
        preferred_name:
          maxLength: 40
          type: string
          description: Preferred name used in informal situatio...
          example: Ali
      description: PersonName Update for Patch and Put
    PersonUpdate:
      type: object
      properties:
        privacies:
          $ref: "#/components/schemas/PrivacyItems"
        addresses:
          $ref: "#/components/schemas/Addresses"
        communication_channels:
          $ref: "#/components/schemas/CommunicationChannels"
        comments:
          type: string
          description: Additional comments about the person.
          example: No known allergies.
        person_attributes:
          $ref: "#/components/schemas/MetricNameValues"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        birth_date:
          type: string
          description: Birth date of the person.
          format: date
          example: 1985-08-25
        special_remarks_description:
          type: string
          description: Special remarks about the person.
        privacies_href:
          type: string
          description: Link to privacy settings resource.
          format: href
        marital_status_type:
          $ref: "#/components/schemas/MaritalStatusTypes"
        person_name:
          $ref: "#/components/schemas/PersonName"
        citizenship_country:
          type: string
          description: ISO 3166-1 alpha-2 code for citizenship ...
          example: US
        preferred_language_code:
          type: string
          description: Preferred language code.
          example: en-US
        party_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/PartyTypes"
        birth_country:
          type: string
          description: ISO 3166-1 alpha-2 code for the country ...
          example: US
        country_code:
          type: string
          description: ISO 3166-1 alpha-2 country code.
          example: US
        gender_type:
          $ref: "#/components/schemas/GenderTypes"
        identifiers_href:
          type: string
          description: Link to identifiers resource.
          format: href
        age:
          maximum: 130
          minimum: 0
          type: integer
          description: Age of the person in years.
          format: int32
          example: "35"
        org_profiles_href:
          type: string
          description: Link to organizational profiles resource...
          format: href
        language_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
      description: Person Update for Patch and Put
    Position:
      type: object
      properties:
        position_id:
          type: string
          description: Unique identifier for this position
          example: pos-001
        position_key:
          type: string
          description: Business key for this position record
          example: POS-ATL-WEST
        latitude:
          type: string
          description: Latitude of the location
          example: "33.7490"
        longitude:
          type: string
          description: Longitude of the location
          example: "-84.3880"
        altitude:
          type: string
          description: Altitude of the location
          example: "320"
        bearing:
          type: string
          description: Bearing direction (compass)
          example: NW
        postal_code:
          type: string
          description: Postal code of the location
          example: "30303"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
      description: "position meta-data that includes postal code, a..."
    PositionCreate:
      required:
      - position_key
      type: object
      description: Position Create for Post
      allOf:
      - $ref: "#/components/schemas/Position"
    PositionUpdate:
      type: object
      properties:
        altitude:
          type: string
          description: Altitude of the location
          example: "320"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: "Standardized code for the county. Pattern: ^[a-..."
        bearing:
          type: string
          description: Bearing direction (compass)
          example: NW
        latitude:
          type: string
          description: Latitude of the location
          example: "33.7490"
        postal_code:
          type: string
          description: Postal code of the location
          example: "30303"
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        longitude:
          type: string
          description: Longitude of the location
          example: "-84.3880"
      description: Position Update for Patch and Put
    PrivacyContextTypes:
      type: string
      description: types of privacy contexts.
      enum:
      - GIVE_CONSENT
      - INFORMED_OF_COLLECTION
      - GET_BREACH_NOTIFICATION
      - ACCESS_DATA
      - RECTIFICATION_OF_DATA
      - ERASURE_OF_DATA
      - RESTRICT_PROCESSING
      - DATA_PORTABILITY
      - OBJECT_TO_PROCESSING
      - OTHER
    PrivacyEvent:
      type: object
      properties:
        privacy_event_id:
          type: string
          description: A unique identifier for the privacy even...
        privacy_event_key:
          type: string
          description: A unique identifier for the privacy even...
        legal_basis_type:
          $ref: "#/components/schemas/PrivacyLegalBasisTypes"
        privacy_context_type:
          $ref: "#/components/schemas/PrivacyContextTypes"
        privacy_state_type:
          $ref: "#/components/schemas/PrivacyStateTypes"
        value:
          type: string
          description: The monetary or hours-based value of the...
        event_reference:
          type: string
          description: Event Reference for this event
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
      description: Represents a single event that affects a staff ...
    PrivacyEventCreate:
      required:
      - privacy_event_key
      type: object
      description: PrivacyEvent Create for Post
      allOf:
      - $ref: "#/components/schemas/PrivacyEvent"
    PrivacyEventUpdate:
      type: object
      properties:
        privacy_context_type:
          $ref: "#/components/schemas/PrivacyContextTypes"
        privacy_state_type:
          $ref: "#/components/schemas/PrivacyStateTypes"
        event_reference:
          type: string
          description: Event Reference for this event
        legal_basis_type:
          $ref: "#/components/schemas/PrivacyLegalBasisTypes"
        value:
          type: string
          description: The monetary or hours-based value of the...
        occurrence_date_time:
          type: string
          description: The date and time the event occurred.
          format: date-time
      description: PrivacyEvent Update for Patch and Put
    PrivacyEvents:
      type: array
      items:
        $ref: "#/components/schemas/PrivacyEvent"
    PrivacyItem:
      type: object
      properties:
        privacy_id:
          type: string
          description: A unique key for the privacy record.
        privacy_key:
          type: string
          description: A unique key for the privacy record.
        privacy_event:
          $ref: "#/components/schemas/PrivacyEvents"
        privacy_type:
          $ref: "#/components/schemas/PrivacyTypes"
        encrypted_value:
          type: string
          description: The encrypted value of the sensitive inf...
        hash_value:
          type: string
          description: A one-way hash of the sensitive informat...
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        last_updated_date_time:
          type: string
          description: The date and time when the privacy recor...
          format: date-time
      description: Represents a single piece of sensitive or perso...
    PrivacyItemCreate:
      required:
      - privacy_item_key
      type: object
      description: PrivacyItem Create for Post
      allOf:
      - $ref: "#/components/schemas/PrivacyItem"
    PrivacyItemUpdate:
      type: object
      properties:
        privacy_type:
          $ref: "#/components/schemas/PrivacyTypes"
        last_updated_date_time:
          type: string
          description: The date and time when the privacy recor...
          format: date-time
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        encrypted_value:
          type: string
          description: The encrypted value of the sensitive inf...
        hash_value:
          type: string
          description: A one-way hash of the sensitive informat...
        privacy_event:
          $ref: "#/components/schemas/PrivacyEvents"
      description: PrivacyItem Update for Patch and Put
    PrivacyItems:
      type: array
      items:
        $ref: "#/components/schemas/PrivacyItem"
    PrivacyLegalBasisTypes:
      type: string
      description: types of privacy legal basis.
      enum:
      - CONSENT
      - CONTRACT
      - LEGAL_OBLIGATION
      - VITAL_INTERESTS
      - PUBLIC_TASK
      - LEGITIMATE_INTERESTS
      - UNKNOWN
    PrivacyStateTypes:
      type: string
      description: types of privacy states.
      enum:
      - PENDING
      - ACTIVE_REVIEW
      - PENDING_RESOLUTION
      - RESOLVED
      - GIVEN
      - REVOKED
      - IN_COMPLIANCE
      - OUT_OF_COMPLIANCE
      - NOT_APPLICABLE
      - OTHER
    PrivacyTypes:
      type: string
      description: types of privacys.
      enum:
      - SOCIAL_SECURITY_NUMBER
      - DATE_OF_BIRTH
      - DRIVER_LICENSE_NUMBER
      - PASSPORT_NUMBER
      - PHONE_NUMBER
      - EMAIL_ADDRESS
      - HOME_ADDRESS
      - MOTHER_MAIDEN_NAME
      - TAX_ID_NUMBER
      - GENERAL_PERSONAL
      - CREDIT_CARD_NUMBER
      - BANK_ACCOUNT_NUMBER
      - ROUTING_NUMBER
      - PAYPAL_ACCOUNT
      - CRYPTO_WALLET_ADDRESS
      - GENERAL_FINANCIAL
      - HEALTH_INSURANCE_ID
      - MEDICAL_RECORD_NUMBER
      - PRESCRIPTION_DETAILS
      - GENERAL_MEDICAL
      - FINGERPRINT
      - FACE_SCAN
      - VOICE_PRINT
      - GENERAL_BIO
      - SOCIAL_MEDIA_HANDLE
      - SOCIAL_MEDIA_PROFILE
      - IP_ADDRESS
      - DEVICE_ID
      - GEOLOCATION_DATA
      - ONLINE_BEHAVIORAL_DATA
      - GENERAL_SOCIAL
      - THIRD_PARTY_CUSTOMER_DATA
      - THIRD_PARTY_BUSINESS_DATA
      - UNKNOWN
    RoleTypes:
      type: string
      description: types of roles.
      enum:
      - GEN_MGR
      - CLOSING_MGR
      - FI_MGR
      - SALES_MGR
      - SALES_ASSOC
      - BUSINESS_DEVELOPMENT
      - FINANCE
      - CONCIERGE_STAFF
      - CONCIERGE_MGR
      - VALET
      - SERVICE_MGR
      - SERVICE_TECH
      - STAFF
      - SPECIALIST
      - ADVISOR
    StaffMember:
      type: object
      properties:
        staff_member_id:
          type: string
          description: Unique identifier for the staff member.
          example: sm_123456
        staff_member_key:
          type: string
          description: Business key or code for the staff membe...
          example: EMPLOYEE_001
        employee_key:
          type: string
          description: Employee key associated with the staff m...
          example: E12345
        party_key:
          type: string
          description: Party key linking to person or organizat...
          example: party_67890
        person:
          $ref: "#/components/schemas/Person"
        email:
          type: string
          description: Primary email address of the staff membe...
          example: jane.doe@example.com
        state_license_number:
          type: string
          description: State license number if applicable.
          example: SLN-987654
        active_indicator:
          type: boolean
          description: Indicates if the staff member is current...
          example: "true"
        job_title:
          type: string
          description: Job title of the staff member.
          example: Sales Manager
        department_name:
          type: string
          description: Name of the department the staff member ...
          example: Sales
        department_href:
          type: string
          description: URL reference to the department resource...
          format: href
          example: /departments/123
        department:
          $ref: "#/components/schemas/Department"
        manager_key:
          type: string
          description: Key identifying the manager of this staf...
          example: sm_manager_001
        manager_name:
          type: string
          description: Name of the manager.
          example: John Smith
        team_id:
          type: string
          description: Identifier of the team the staff member ...
          example: team_007
        team_key:
          type: string
          description: Key of the team the staff member belongs...
          example: SALES_TEAM
        team_name:
          type: string
          description: Name of the team the staff member belong...
          example: Regional Sales
        communication_channels:
          $ref: "#/components/schemas/CommunicationChannels"
        contact_methods_href:
          type: string
          description: Reference URL to the contact methods for...
          format: href
          example: /staff/123/contactMethods
        comments:
          type: string
          description: Additional comments about the staff memb...
          example: Key sales staff member with 5 years experience.
        role_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/RoleTypes"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        profile_href:
          type: string
          description: URL reference to the staff member's prof...
          format: href
          example: /profiles/staff_12345
        payroll_rates:
          $ref: "#/components/schemas/PayrollRates"
        privacy_items:
          $ref: "#/components/schemas/PrivacyItems"
      description: Dealership Staff Member
    StaffMemberCreate:
      required:
      - staff_member_key
      type: object
      description: StaffMember Create for Post
      allOf:
      - $ref: "#/components/schemas/StaffMember"
    StaffMemberUpdate:
      type: object
      properties:
        communication_channels:
          $ref: "#/components/schemas/CommunicationChannels"
        comments:
          type: string
          description: Additional comments about the staff memb...
          example: Key sales staff member with 5 years experience.
        department_href:
          type: string
          description: URL reference to the department resource...
          format: href
          example: /departments/123
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        department_name:
          type: string
          description: Name of the department the staff member ...
          example: Sales
        profile_href:
          type: string
          description: URL reference to the staff member's prof...
          format: href
          example: /profiles/staff_12345
        privacy_items:
          $ref: "#/components/schemas/PrivacyItems"
        team_name:
          type: string
          description: Name of the team the staff member belong...
          example: Regional Sales
        manager_name:
          type: string
          description: Name of the manager.
          example: John Smith
        payroll_rates:
          $ref: "#/components/schemas/PayrollRates"
        person:
          $ref: "#/components/schemas/Person"
        department:
          $ref: "#/components/schemas/Department"
        role_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/RoleTypes"
        state_license_number:
          type: string
          description: State license number if applicable.
          example: SLN-987654
        job_title:
          type: string
          description: Job title of the staff member.
          example: Sales Manager
        email:
          type: string
          description: Primary email address of the staff membe...
          example: jane.doe@example.com
        active_indicator:
          type: boolean
          description: Indicates if the staff member is current...
          example: "true"
        contact_methods_href:
          type: string
          description: Reference URL to the contact methods for...
          format: href
          example: /staff/123/contactMethods
      description: StaffMember Update for Patch and Put
    StaffPayTypes:
      type: string
      description: types of staff pays.
      enum:
      - HOURLY
      - SALARIED
      - COMMISSION
      - CONTRACTOR
      - SICK
      - VACATION
      - OVERTIME
      - TECHNICIAN_HOURS
      - BONUS
      - ADJUSTMENT
      - REGULAR
      - OTHER
    TextualDetail:
      type: object
      properties:
        textual_detail_key:
          type: string
          description: Natural key or business-friendly key for...
        textual_detail_id:
          type: string
          description: Unique identifier for textual detail.
        comments:
          $ref: "#/components/schemas/Memo"
        locale_codes:
          $ref: "#/components/schemas/Codes"
        notes:
          $ref: "#/components/schemas/Memo"
        short_description:
          type: string
          description: Short description for the detail.
        description:
          type: string
          description: Full description in memo format.
        text_key:
          type: string
          description: Key reference for reusable text blocks.
        name:
          type: string
          description: Name or title of the textual detail.
        value:
          type: string
          description: Value associated with the textual detail...
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        groups:
          $ref: "#/components/schemas/Codes"
        label:
          type: string
          description: Label for UI representation.
        alt_text:
          type: string
          description: Alternative text for accessibility or im...
        textual_detail_code:
          type: string
          description: Code identifier for textual detail refer...
        tags:
          $ref: "#/components/schemas/Codes"
        uom_display:
          type: string
          description: Unit of Measure display value.
        uom_code:
          type: string
          description: "Unit of Measure code (e.g., EA, PR)."
      description: not nullable
    TextualDetailCreate:
      required:
      - textual_detail_key
      type: object
      description: TextualDetail Create for Post
      allOf:
      - $ref: "#/components/schemas/TextualDetail"
    TextualDetailUpdate:
      type: object
      properties:
        short_description:
          type: string
          description: Short description for the detail.
        comments:
          $ref: "#/components/schemas/Memo"
        notes:
          $ref: "#/components/schemas/Memo"
        uom_display:
          type: string
          description: Unit of Measure display value.
        locale_codes:
          $ref: "#/components/schemas/Codes"
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        description:
          type: string
          description: Full description in memo format.
        groups:
          $ref: "#/components/schemas/Codes"
        textual_detail_code:
          type: string
          description: Code identifier for textual detail refer...
        label:
          type: string
          description: Label for UI representation.
        tags:
          $ref: "#/components/schemas/Codes"
        alt_text:
          type: string
          description: Alternative text for accessibility or im...
        name:
          type: string
          description: Name or title of the textual detail.
        value:
          type: string
          description: Value associated with the textual detail...
        uom_code:
          type: string
          description: "Unit of Measure code (e.g., EA, PR)."
      description: TextualDetail Update for Patch and Put
    TextualDetails:
      type: array
      items:
        $ref: "#/components/schemas/TextualDetail"
    TimeSlot:
      type: object
      properties:
        time_slot_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique business key for the time slot. Pattern:...
        end_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        end_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "End time in HH:mm format. Pattern: ^([01]\\d|2[0..."
          format: date-time
        recurring_indicator:
          type: boolean
          description: Indicates if the time slot is recurring.
        recurring_period:
          $ref: "#/components/schemas/EffectivePeriod"
        start_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        start_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "Start time in HH:mm format. Pattern: ^([01]\\d|2..."
          format: date-time
        timeslot_directive_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeslotDirectiveTypes"
        time_slot_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: string
          description: Unique identifier for the time slot reco... Pat...
        week_of_date:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          format: date
          items:
            type: string
      description: Range of time for the appointment including sta...
    TimeSlotCreate:
      required:
      - time_slot_key
      type: object
      description: TimeSlot Create for Post
      allOf:
      - $ref: "#/components/schemas/TimeSlot"
    TimeSlotUpdate:
      type: object
      properties:
        end_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        recurring_period:
          $ref: "#/components/schemas/EffectivePeriod"
        start_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "Start time in HH:mm format. Pattern: ^([01]\\d|2..."
          format: date-time
        recurring_indicator:
          type: boolean
          description: Indicates if the time slot is recurring.
        start_day:
          $ref: "#/components/schemas/DaysOfWeekTypes"
        timeslot_directive_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            $ref: "#/components/schemas/TimeslotDirectiveTypes"
        week_of_date:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          format: date
          items:
            type: string
        end_time:
          pattern: "^([01]\\d|2[0-3]):([0-5]\\d)$"
          type: string
          description: "End time in HH:mm format. Pattern: ^([01]\\d|2[0..."
          format: date-time
      description: TimeSlot Update for Patch and Put
    TimeslotDirectiveTypes:
      type: string
      description: types of timeslot directives.
      enum:
      - AFTER
      - ANYTIME
      - BEFORE
      - BETWEEN
      - BLOCK
      - UNKNOWN
    CommandOption:
      type: object
      properties:
        type:
          type: string
          description: The command name to send to the /commands endpoint
          example: Approve
        description:
          type: string
          description: Guidance for the agent on when to use this command
          example: Moves the invoice to ACTIVE state and triggers the payment gateway.
        target_state:
          type: string
          description: The state the entity will transition to upon success
          example: ACTIVE
    StaffMemberWorkflowMetadata:
      type: object
      properties:
        current_state:
          type: string
          description: The current lifecycle phase of this entity.
          enum:
          - CREATED
          - ACTIVE
          - DELETED
          - ARCHIVED
        allowed_commands:
          type: array
          description: Intents the agent can execute on this specific instance right
            now.
          items:
            $ref: "#/components/schemas/CommandOption"
    StaffMemberResponse:
      allOf:
      - $ref: "#/components/schemas/StaffMember"
      - $ref: "#/components/schemas/StaffMemberWorkflowMetadata"
  responses:
    ForbiddenError:
      description: The authenticated user does not have the necess...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: The request could not be completed because it c...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: An unexpected error occurred on the server whil...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    PreconditionFailedError:
      description: The request was not processed because the 'If-M...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    NotFoundError:
      description: The requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: Authentication failed. The access token is miss...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
  securitySchemes:
    industry-oauth:
      type: oauth2
      description: Centralized OAuth2 service for all industry members
      flows:
        clientCredentials:
          tokenUrl: https://auth.example.com/token
          scopes:
            standard:read: Read access
    member-api-key:
      type: apiKey
      name: X-API-KEY
      in: header
    bearer-auth:
      type: http
      description: JWT token obtained from authentication service
      scheme: bearer
      bearerFormat: JWT
