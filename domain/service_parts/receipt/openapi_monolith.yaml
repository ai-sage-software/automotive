---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /receipts:
    get:
      tags:
      - "Receipt"
      summary: "Retrieve a list of all Receipt records."
      operationId: "listReceipts"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Receipt records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Receipt"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Receipt"
      summary: "Create a new Receipt record."
      operationId: "createReceipt"
      requestBody:
        description: "Receipt data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptCreate"
        required: true
      responses:
        "201":
          description: "Receipt created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/receipts/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /receipts/{receiptKey}:
    get:
      tags:
      - "Receipt"
      summary: "Retrieve a specific Receipt record."
      operationId: "getReceiptById"
      responses:
        "200":
          description: "The requested Receipt record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Receipt"
      summary: "Replace a Receipt record."
      operationId: "replaceReceipt"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Receipt"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Receipt"
      summary: "Delete a Receipt Record"
      description: "Marks the Receipt record as inactive or removes..."
      operationId: "deleteReceiptRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Receipt"
      summary: "Partially update a Receipt record."
      operationId: "updateReceipt"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Receipt"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the specific Receipt."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/shipment-references:
    get:
      tags:
      - "ShipmentReference"
      summary: "Retrieve a list of ShipmentReference records sc..."
      operationId: "listShipmentReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ShipmentReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ShipmentReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ShipmentReference"
      summary: "Create a new ShipmentReference record."
      operationId: "createShipmentReference"
      requestBody:
        description: "ShipmentReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceCreate"
        required: true
      responses:
        "201":
          description: "ShipmentReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/shipment-references/{shipmentReferenceKey}:
    get:
      tags:
      - "ShipmentReference"
      summary: "Retrieve a specific ShipmentReference record."
      operationId: "getShipmentReferenceById"
      responses:
        "200":
          description: "The requested ShipmentReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ShipmentReference"
      summary: "Replace a ShipmentReference record."
      operationId: "replaceShipmentReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ShipmentReference"
      summary: "Delete a ShipmentReference Record"
      description: "Marks the ShipmentReference record as inactive ..."
      operationId: "deleteShipmentReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ShipmentReference"
      summary: "Partially update a ShipmentReference record."
      operationId: "updateShipmentReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "shipmentReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ShipmentReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/unit-of-measures:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a list of UnitOfMeasure records scoped..."
      operationId: "listUnitOfMeasuresByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of UnitOfMeasure records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/UnitOfMeasure"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/unit-of-measures/{unitOfMeasureKey}:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a specific UnitOfMeasure record."
      operationId: "getUnitOfMeasureById"
      responses:
        "200":
          description: "The requested UnitOfMeasure record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "UnitOfMeasure"
      summary: "Replace a UnitOfMeasure record."
      operationId: "replaceUnitOfMeasure"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "unitOfMeasureKey"
      in: "path"
      description: "The unique identifier for the specific UnitOfMeasure."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/positions:
    get:
      tags:
      - "Position"
      summary: "Retrieve a list of Position records scoped by r..."
      operationId: "listPositionsByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Position records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Position"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Position"
      summary: "Create a new Position record."
      operationId: "createPosition"
      requestBody:
        description: "Position data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionCreate"
        required: true
      responses:
        "201":
          description: "Position created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/positions/{positionKey}:
    get:
      tags:
      - "Position"
      summary: "Retrieve a specific Position record."
      operationId: "getPositionById"
      responses:
        "200":
          description: "The requested Position record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Position"
      summary: "Replace a Position record."
      operationId: "replacePosition"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Position"
      summary: "Delete a Position Record"
      description: "Marks the Position record as inactive or remove..."
      operationId: "deletePositionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Position"
      summary: "Partially update a Position record."
      operationId: "updatePosition"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "positionKey"
      in: "path"
      description: "The unique identifier for the specific Position."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/party-identifiers:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a list of PartyIdentifier records scop..."
      operationId: "listPartyIdentifiersByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartyIdentifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartyIdentifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartyIdentifier"
      summary: "Create a new PartyIdentifier record."
      operationId: "createPartyIdentifier"
      requestBody:
        description: "PartyIdentifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifierCreate"
        required: true
      responses:
        "201":
          description: "PartyIdentifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/party-identifiers/{partyIdentifierKey}:
    get:
      tags:
      - "PartyIdentifier"
      summary: "Retrieve a specific PartyIdentifier record."
      operationId: "getPartyIdentifierById"
      responses:
        "200":
          description: "The requested PartyIdentifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartyIdentifier"
      summary: "Replace a PartyIdentifier record."
      operationId: "replacePartyIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartyIdentifier"
      summary: "Delete a PartyIdentifier Record"
      description: "Marks the PartyIdentifier record as inactive or..."
      operationId: "deletePartyIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartyIdentifier"
      summary: "Partially update a PartyIdentifier record."
      operationId: "updatePartyIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartyIdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartyIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partyIdentifierKey"
      in: "path"
      description: "The unique identifier for the specific PartyIdentifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/address-references:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a list of AddressReference records sco..."
      operationId: "listAddressReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of AddressReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/AddressReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AddressReference"
      summary: "Create a new AddressReference record."
      operationId: "createAddressReference"
      requestBody:
        description: "AddressReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReferenceCreate"
        required: true
      responses:
        "201":
          description: "AddressReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/address-references/{addressReferenceKey}:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a specific AddressReference record."
      operationId: "getAddressReferenceById"
      responses:
        "200":
          description: "The requested AddressReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AddressReference"
      summary: "Replace a AddressReference record."
      operationId: "replaceAddressReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AddressReference"
      summary: "Delete a AddressReference Record"
      description: "Marks the AddressReference record as inactive o..."
      operationId: "deleteAddressReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AddressReference"
      summary: "Partially update a AddressReference record."
      operationId: "updateAddressReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "addressReferenceKey"
      in: "path"
      description: "The unique identifier for the specific AddressReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/staff-assignment-references:
    get:
      tags:
      - "StaffAssignmentReference"
      summary: "Retrieve a list of StaffAssignmentReference rec..."
      operationId: "listStaffAssignmentReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of StaffAssignmentReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/StaffAssignmentReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "StaffAssignmentReference"
      summary: "Create a new StaffAssignmentReference record."
      operationId: "createStaffAssignmentReference"
      requestBody:
        description: "StaffAssignmentReference data to create the new..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceCreate"
        required: true
      responses:
        "201":
          description: "StaffAssignmentReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/staff-assignment-references/{staffAssignmentReferenceKey}:
    get:
      tags:
      - "StaffAssignmentReference"
      summary: "Retrieve a specific StaffAssignmentReference re..."
      operationId: "getStaffAssignmentReferenceById"
      responses:
        "200":
          description: "The requested StaffAssignmentReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "StaffAssignmentReference"
      summary: "Replace a StaffAssignmentReference record."
      operationId: "replaceStaffAssignmentReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "StaffAssignmentReference"
      summary: "Delete a StaffAssignmentReference Record"
      description: "Marks the StaffAssignmentReference record as in..."
      operationId: "deleteStaffAssignmentReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "StaffAssignmentReference"
      summary: "Partially update a StaffAssignmentReference rec..."
      operationId: "updateStaffAssignmentReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "staffAssignmentReferenceKey"
      in: "path"
      description: "The unique identifier for the specific StaffAssignmentReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/shipment-events:
    get:
      tags:
      - "ShipmentEvent"
      summary: "Retrieve a list of ShipmentEvent records scoped..."
      operationId: "listShipmentEventsByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ShipmentEvent records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ShipmentEvent"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ShipmentEvent"
      summary: "Create a new ShipmentEvent record."
      operationId: "createShipmentEvent"
      requestBody:
        description: "ShipmentEvent data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEventCreate"
        required: true
      responses:
        "201":
          description: "ShipmentEvent created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/shipment-events/{shipmentEventKey}:
    get:
      tags:
      - "ShipmentEvent"
      summary: "Retrieve a specific ShipmentEvent record."
      operationId: "getShipmentEventById"
      responses:
        "200":
          description: "The requested ShipmentEvent record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ShipmentEvent"
      summary: "Replace a ShipmentEvent record."
      operationId: "replaceShipmentEvent"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEvent"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ShipmentEvent"
      summary: "Delete a ShipmentEvent Record"
      description: "Marks the ShipmentEvent record as inactive or r..."
      operationId: "deleteShipmentEventRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ShipmentEvent"
      summary: "Partially update a ShipmentEvent record."
      operationId: "updateShipmentEvent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEventUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "shipmentEventKey"
      in: "path"
      description: "The unique identifier for the specific ShipmentEvent."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by..."
      operationId: "listIdentifiersByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or remo..."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/prices:
    get:
      tags:
      - "Price"
      summary: "Retrieve a list of Price records scoped by rece..."
      operationId: "listPricesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Price records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Price"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Price"
      summary: "Create a new Price record."
      operationId: "createPrice"
      requestBody:
        description: "Price data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceCreate"
        required: true
      responses:
        "201":
          description: "Price created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/prices/{priceKey}:
    get:
      tags:
      - "Price"
      summary: "Retrieve a specific Price record."
      operationId: "getPriceById"
      responses:
        "200":
          description: "The requested Price record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Price"
      summary: "Replace a Price record."
      operationId: "replacePrice"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Price"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Price"
      summary: "Delete a Price Record"
      description: "Marks the Price record as inactive or removes i..."
      operationId: "deletePriceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Price"
      summary: "Partially update a Price record."
      operationId: "updatePrice"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "priceKey"
      in: "path"
      description: "The unique identifier for the specific Price."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/purchase-order-references:
    get:
      tags:
      - "PurchaseOrderReference"
      summary: "Retrieve a list of PurchaseOrderReference recor..."
      operationId: "listPurchaseOrderReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PurchaseOrderReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PurchaseOrderReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PurchaseOrderReference"
      summary: "Create a new PurchaseOrderReference record."
      operationId: "createPurchaseOrderReference"
      requestBody:
        description: "PurchaseOrderReference data to create the new r..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReferenceCreate"
        required: true
      responses:
        "201":
          description: "PurchaseOrderReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/purchase-order-references/{purchaseOrderReferenceKey}:
    get:
      tags:
      - "PurchaseOrderReference"
      summary: "Retrieve a specific PurchaseOrderReference record."
      operationId: "getPurchaseOrderReferenceById"
      responses:
        "200":
          description: "The requested PurchaseOrderReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PurchaseOrderReference"
      summary: "Replace a PurchaseOrderReference record."
      operationId: "replacePurchaseOrderReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PurchaseOrderReference"
      summary: "Delete a PurchaseOrderReference Record"
      description: "Marks the PurchaseOrderReference record as inac..."
      operationId: "deletePurchaseOrderReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PurchaseOrderReference"
      summary: "Partially update a PurchaseOrderReference record."
      operationId: "updatePurchaseOrderReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "purchaseOrderReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PurchaseOrderReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/receipt-line-items:
    get:
      tags:
      - "ReceiptLineItem"
      summary: "Retrieve a list of ReceiptLineItem records scop..."
      operationId: "listReceiptLineItemsByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ReceiptLineItem records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ReceiptLineItem"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ReceiptLineItem"
      summary: "Create a new ReceiptLineItem record."
      operationId: "createReceiptLineItem"
      requestBody:
        description: "ReceiptLineItem data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItemCreate"
        required: true
      responses:
        "201":
          description: "ReceiptLineItem created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/receipt-line-items/{receiptLineItemKey}:
    get:
      tags:
      - "ReceiptLineItem"
      summary: "Retrieve a specific ReceiptLineItem record."
      operationId: "getReceiptLineItemById"
      responses:
        "200":
          description: "The requested ReceiptLineItem record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ReceiptLineItem"
      summary: "Replace a ReceiptLineItem record."
      operationId: "replaceReceiptLineItem"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItem"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ReceiptLineItem"
      summary: "Delete a ReceiptLineItem Record"
      description: "Marks the ReceiptLineItem record as inactive or..."
      operationId: "deleteReceiptLineItemRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ReceiptLineItem"
      summary: "Partially update a ReceiptLineItem record."
      operationId: "updateReceiptLineItem"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReceiptLineItemUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "receiptLineItemKey"
      in: "path"
      description: "The unique identifier for the specific ReceiptLineItem."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/part-references:
    get:
      tags:
      - "PartReference"
      summary: "Retrieve a list of PartReference records scoped..."
      operationId: "listPartReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartReference"
      summary: "Create a new PartReference record."
      operationId: "createPartReference"
      requestBody:
        description: "PartReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReferenceCreate"
        required: true
      responses:
        "201":
          description: "PartReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/part-references/{partReferenceKey}:
    get:
      tags:
      - "PartReference"
      summary: "Retrieve a specific PartReference record."
      operationId: "getPartReferenceById"
      responses:
        "200":
          description: "The requested PartReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartReference"
      summary: "Replace a PartReference record."
      operationId: "replacePartReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartReference"
      summary: "Delete a PartReference Record"
      description: "Marks the PartReference record as inactive or r..."
      operationId: "deletePartReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartReference"
      summary: "Partially update a PartReference record."
      operationId: "updatePartReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PartReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/effective-periods:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a list of EffectivePeriod records scop..."
      operationId: "listEffectivePeriodsByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of EffectivePeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/EffectivePeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a specific EffectivePeriod record."
      operationId: "getEffectivePeriodById"
      responses:
        "200":
          description: "The requested EffectivePeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "EffectivePeriod"
      summary: "Replace a EffectivePeriod record."
      operationId: "replaceEffectivePeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "effectivePeriodKey"
      in: "path"
      description: "The unique identifier for the specific EffectivePeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/lifecycle-events:
    get:
      tags:
      - "LifecycleEvent"
      summary: "Retrieve a list of LifecycleEvent records scope..."
      operationId: "listLifecycleEventsByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of LifecycleEvent records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/LifecycleEvent"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "LifecycleEvent"
      summary: "Create a new LifecycleEvent record."
      operationId: "createLifecycleEvent"
      requestBody:
        description: "LifecycleEvent data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifecycleEventCreate"
        required: true
      responses:
        "201":
          description: "LifecycleEvent created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/lifecycle-events/{lifecycleEventKey}:
    get:
      tags:
      - "LifecycleEvent"
      summary: "Retrieve a specific LifecycleEvent record."
      operationId: "getLifecycleEventById"
      responses:
        "200":
          description: "The requested LifecycleEvent record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "LifecycleEvent"
      summary: "Replace a LifecycleEvent record."
      operationId: "replaceLifecycleEvent"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifecycleEvent"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "LifecycleEvent"
      summary: "Delete a LifecycleEvent Record"
      description: "Marks the LifecycleEvent record as inactive or ..."
      operationId: "deleteLifecycleEventRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "LifecycleEvent"
      summary: "Partially update a LifecycleEvent record."
      operationId: "updateLifecycleEvent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifecycleEventUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifecycleEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "lifecycleEventKey"
      in: "path"
      description: "The unique identifier for the specific LifecycleEvent."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/warehouse-references:
    get:
      tags:
      - "WarehouseReference"
      summary: "Retrieve a list of WarehouseReference records s..."
      operationId: "listWarehouseReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of WarehouseReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/WarehouseReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "WarehouseReference"
      summary: "Create a new WarehouseReference record."
      operationId: "createWarehouseReference"
      requestBody:
        description: "WarehouseReference data to create the new resou..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReferenceCreate"
        required: true
      responses:
        "201":
          description: "WarehouseReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/warehouse-references/{warehouseReferenceKey}:
    get:
      tags:
      - "WarehouseReference"
      summary: "Retrieve a specific WarehouseReference record."
      operationId: "getWarehouseReferenceById"
      responses:
        "200":
          description: "The requested WarehouseReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "WarehouseReference"
      summary: "Replace a WarehouseReference record."
      operationId: "replaceWarehouseReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "WarehouseReference"
      summary: "Delete a WarehouseReference Record"
      description: "Marks the WarehouseReference record as inactive..."
      operationId: "deleteWarehouseReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "WarehouseReference"
      summary: "Partially update a WarehouseReference record."
      operationId: "updateWarehouseReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WarehouseReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "warehouseReferenceKey"
      in: "path"
      description: "The unique identifier for the specific WarehouseReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /receipts/{receiptKey}/financial-track-references:
    get:
      tags:
      - "FinancialTrackReference"
      summary: "Retrieve a list of FinancialTrackReference reco..."
      operationId: "listFinancialTrackReferencesByReceiptKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialTrackReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialTrackReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialTrackReference"
      summary: "Create a new FinancialTrackReference record."
      operationId: "createFinancialTrackReference"
      requestBody:
        description: "FinancialTrackReference data to create the new ..."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceCreate"
        required: true
      responses:
        "201":
          description: "FinancialTrackReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /receipts/{receiptKey}/financial-track-references/{financialTrackReferenceKey}:
    get:
      tags:
      - "FinancialTrackReference"
      summary: "Retrieve a specific FinancialTrackReference rec..."
      operationId: "getFinancialTrackReferenceById"
      responses:
        "200":
          description: "The requested FinancialTrackReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialTrackReference"
      summary: "Replace a FinancialTrackReference record."
      operationId: "replaceFinancialTrackReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialTrackReference"
      summary: "Delete a FinancialTrackReference Record"
      description: "Marks the FinancialTrackReference record as ina..."
      operationId: "deleteFinancialTrackReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialTrackReference"
      summary: "Partially update a FinancialTrackReference record."
      operationId: "updateFinancialTrackReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "receiptKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialTrackReferenceKey"
      in: "path"
      description: "The unique identifier for the specific FinancialTrackReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas:
    AddressReference:
      type: "object"
      properties:
        address_reference_key:
          type: "string"
          description: "A unique key for the address reference."
          nullable: false
        address_reference_value:
          type: "string"
          description: "The unique value of the address reference."
          nullable: true
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          description: "A list of usage types for the address."
          nullable: true
          example: ""
          items:
            $ref: "#/components/schemas/AddressTypes"
        address_line1:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Primary address line (Street, PO Box, etc)."
          nullable: true
        address_line2:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line3:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line4:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line5:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        city:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Name of the city."
          nullable: true
        country_code:
          maxLength: 100
          minLength: 2
          type: "string"
          description: "Full name of the country."
          nullable: true
        district:
          maxLength: 100
          type: "string"
          description: "Name of the district."
          nullable: true
        label:
          maxLength: 50
          type: "string"
          description: "Label or display name for the address."
          nullable: true
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: "string"
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
          nullable: true
        state_province:
          maxLength: 50
          type: "string"
          description: "State or equivalent subdivision."
          nullable: true
      description: "A reference to an address, including its unique..."
    AddressReferenceCreate:
      required:
      - "address_reference_key"
      type: "object"
      description: "AddressReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/AddressReference"
    AddressReferenceUpdate:
      type: "object"
      properties:
        address_reference_value:
          type: "string"
          description: "The unique value of the address reference."
          nullable: true
        city:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Name of the city."
          nullable: true
        state_province:
          maxLength: 50
          type: "string"
          description: "State or equivalent subdivision."
          nullable: true
        label:
          maxLength: 50
          type: "string"
          description: "Label or display name for the address."
          nullable: true
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          description: "A list of usage types for the address."
          nullable: true
          example: ""
          items:
            $ref: "#/components/schemas/AddressTypes"
        address_line5:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        country_code:
          maxLength: 100
          minLength: 2
          type: "string"
          description: "Full name of the country."
          nullable: true
        address_line4:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line3:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line2:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)."
          nullable: true
        address_line1:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Primary address line (Street, PO Box, etc)."
          nullable: true
        district:
          maxLength: 100
          type: "string"
          description: "Name of the district."
          nullable: true
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: "string"
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
          nullable: true
      description: "AddressReference Update for Patch and Put"
    AddressTypes:
      type: "string"
      description: "types of address."
      enum:
      - "BILLING"
      - "SHIPPING"
      - "MAILING"
      - "POSTAL"
      - "HOME"
      - "RESIDENCE"
      - "WORK"
      - "BUSINESS"
      - "LEGAL"
      - "SERVICE"
      - "DEALER"
      - "CONTRACT"
      - "AR_ADDRESS"
      - "RECIPIENT"
      - "DELIVERY"
      - "DESTINATION"
      - "ORIGINAL"
      - "CURRENT"
      - "PREVIOUS"
      - "TEMPORARY"
      - "VACATION"
      - "GARAGE"
      - "EMERGENCY"
      - "REBATE"
      - "SERVICE_CONTRACT"
      - "INSURANCE"
      - "PRODUCT"
      - "ARCHIVE"
      - "OTHER"
    Codes:
      type: "array"
      description: "List of Codes"
      items:
        type: "string"
    DaysOfWeekTypes:
      type: "string"
      description: "Status of the account"
      enum:
      - "SUN"
      - "MON"
      - "TUES"
      - "WED"
      - "THURS"
      - "FRI"
      - "SAT"
    DurationUOMTypes:
      type: "string"
      description: "types of duration u o ms."
      enum:
      - "MINUTES"
      - "HOURS"
      - "DAYS"
      - "DAY_OF_MONTH"
      - "WEEKS"
      - "BI_WEEKS"
      - "SEMI_MONTHS"
      - "MONTHS"
      - "QUARTERS"
      - "SEMI_YEARS"
      - "YEARS"
      - "MULTI_YEARS"
      - "CUSTOM"
      - "OCCURRENCE"
      - "UNLIMITED"
      - "NONE"
      - "OTHER"
    EffectivePeriod:
      $ref: "#/components/schemas/TimeSlot"
    ErrorDetail:
      required:
      - "code"
      - "message"
      - "timestamp"
      type: "object"
      properties:
        code:
          type: "string"
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: "NOT_FOUND_PR"
        message:
          type: "string"
          description: "A human-readable summary providing specific det..."
          example: "Personnel Record with ID PR-1234567 was not found."
        details:
          type: "array"
          description: "Optional array of specific field-level validati..."
          items:
            type: "object"
            properties:
              field:
                type: "string"
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: "payrollId"
              issue:
                type: "string"
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: "must be unique"
        timestamp:
          type: "string"
          description: "The time the error occurred, in ISO 8601 format."
          format: "date-time"
          example: "2025-11-10T10:30:00Z"
      description: "Standard format for all API error responses."
    FinancialTrackReference:
      $ref: "#/components/schemas/Identifier"
    FinancialTrackReferenceCreate:
      required:
      - "financial_track_reference_key"
      type: "object"
      description: "FinancialTrackReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/FinancialTrackReference"
    FinancialTrackReferenceUpdate:
      $ref: "#/components/schemas/Identifier"
    FinancialTrackReferences:
      type: "array"
      description: "List of FinancialTrackReference"
      items:
        $ref: "#/components/schemas/FinancialTrackReference"
    FinancialTrackReferencesCreate:
      type: "array"
      description: "FinancialTrackReferences Create for Post"
      items:
        $ref: "#/components/schemas/FinancialTrackReferences"
    FinancialTrackReferencesUpdate:
      type: "array"
      description: "FinancialTrackReferences Update for Patch and Put"
      items:
        $ref: "#/components/schemas/FinancialTrackReferences"
    Identifier:
      $ref: "#/components/schemas/Codes"
    IdentifierCreate:
      required:
      - "identifier_key"
      type: "object"
      description: "Identifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      $ref: "#/components/schemas/Codes"
    Identifiers:
      type: "array"
      description: "List of Identifier"
      items:
        $ref: "#/components/schemas/Identifier"
    IdentifiersCreate:
      type: "array"
      description: "Identifiers Create for Post"
      items:
        $ref: "#/components/schemas/Identifiers"
    IdentifiersUpdate:
      type: "array"
      description: "Identifiers Update for Patch and Put"
      items:
        $ref: "#/components/schemas/Identifiers"
    LifecycleEvent:
      $ref: "#/components/schemas/ResourceTypes"
    LifecycleEventCreate:
      required:
      - "lifecycle_event_key"
      type: "object"
      description: "LifecycleEvent Create for Post"
      allOf:
      - $ref: "#/components/schemas/LifecycleEvent"
    LifecycleEventTypes:
      type: "string"
      description: "types of lifecycle events."
      enum:
      - "CREATED"
      - "UPDATED"
      - "DELETE_HARD"
      - "DELETE_SOFT"
      - "ACCESS_ALLOWED"
      - "ACCESS_DENIED"
      - "SUBMITTED"
      - "CLOSED"
      - "CANCELLED"
      - "DEAD"
      - "INITIALIZED"
      - "INITIALIZING"
      - "PROCESSING"
      - "PROCESSED"
      - "FAILED"
      - "VALIDATED"
      - "QUALIFIED"
    LifecycleEventUpdate:
      $ref: "#/components/schemas/ResourceTypes"
    LifecycleEvents:
      type: "array"
      description: "List of LifecycleEvent"
      items:
        $ref: "#/components/schemas/LifecycleEvent"
    LifecycleEventsCreate:
      type: "array"
      description: "LifecycleEvents Create for Post"
      items:
        $ref: "#/components/schemas/LifecycleEvents"
    LifecycleEventsUpdate:
      type: "array"
      description: "LifecycleEvents Update for Patch and Put"
      items:
        $ref: "#/components/schemas/LifecycleEvents"
    Link:
      type: "array"
      description: "Quick Link"
      items:
        type: "string"
    Links:
      type: "array"
      description: "List of Links"
      items:
        type: "string"
    OrderTypes:
      type: "string"
      description: "types of orders."
      enum:
      - "STANDARD"
      - "SALES"
      - "PURCHASE"
      - "RUSH"
      - "BACK_ORDER"
      - "SPECIAL"
      - "SUBSCRIPTION"
      - "SERVICE"
      - "MAINTENANCE"
      - "WARRANTY"
      - "SAMPLE"
      - "INTERNAL_TRANSFER"
      - "CONSIGNMENT"
      - "TRIAL"
      - "RETURN"
    PartReference:
      $ref: "#/components/schemas/Codes"
    PartReferenceCreate:
      required:
      - "part_reference_key"
      type: "object"
      description: "PartReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartReference"
    PartReferenceUpdate:
      $ref: "#/components/schemas/Identifiers"
    PartReferences:
      type: "array"
      description: "List of PartReference"
      items:
        $ref: "#/components/schemas/PartReference"
    PartReferencesCreate:
      type: "array"
      description: "PartReferences Create for Post"
      items:
        $ref: "#/components/schemas/PartReferences"
    PartReferencesUpdate:
      type: "array"
      description: "PartReferences Update for Patch and Put"
      items:
        $ref: "#/components/schemas/PartReferences"
    PartyIdentifier:
      $ref: "#/components/schemas/EffectivePeriod"
    PartyIdentifierCreate:
      required:
      - "party_identifier_key"
      type: "object"
      description: "PartyIdentifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartyIdentifier"
    PartyIdentifierUpdate:
      $ref: "#/components/schemas/Identifier"
    PartyIdentifiers:
      type: "array"
      description: "List of PartyIdentifier"
      items:
        $ref: "#/components/schemas/PartyIdentifier"
    PartyIdentifiersCreate:
      type: "array"
      description: "PartyIdentifiers Create for Post"
      items:
        $ref: "#/components/schemas/PartyIdentifiers"
    PartyIdentifiersUpdate:
      type: "array"
      description: "PartyIdentifiers Update for Patch and Put"
      items:
        $ref: "#/components/schemas/PartyIdentifiers"
    PartyRelationshipTypes:
      type: "string"
      description: "types of party relationships."
      enum:
      - "CORPORATION"
      - "GROUP"
      - "STORE"
      - "BRANCH"
      - "DEPARTMENT"
      - "SECTION"
      - "TEAM"
      - "DEALERSHIP"
      - "THIRD_PARTY"
      - "VENDOR"
      - "SUPPLIER"
      - "PARTNER_ORGANIZATION"
      - "AFFILIATE"
      - "DISTRIBUTOR"
      - "MANUFACTURER"
      - "SERVICE_PROVIDER"
      - "EMPLOYER"
      - "INTERNAL_ORGANIZATION"
      - "COST_CENTER"
      - "FREIGHT_FORWARDER"
      - "BUYER"
      - "REGULATOR"
      - "GOVERNMENT_AGENCY"
      - "NON_PROFIT"
      - "FRANCHISE"
      - "SUBSIDIARY"
      - "DIVISION"
      - "JOINT_VENTURE"
      - "AUDITOR"
      - "ACTOR"
      - "ACCOUNTABLE"
      - "RESPONSIBLE"
      - "CONSULT"
      - "NOTIFY"
      - "EXECUTE"
      - "PART_OF"
      - "MEMBERSHIP"
      - "COMPOSED_IN"
      - "CUSTOMER"
      - "CO_SIGNER"
      - "SPOUSE"
      - "PARENT"
      - "CHILD"
      - "SIBLING"
      - "LEGAL_GUARDIAN"
      - "DEPENDENT"
      - "EMERGENCY_CONTACT"
      - "FAMILY_MEMBER"
      - "FRIEND"
      - "ACQUAINTANCE"
      - "ASSOCIATE"
      - "EX_SPOUSE"
      - "PROFESSIONAL_CONTACT"
      - "MANAGER"
      - "CO_WORKER"
      - "FINANCIAL_ADVISOR"
      - "SELF"
      - "ROMANTIC_PARTNER"
      - "OTHER_FAMILY"
      - "CAREGIVER"
      - "MENTOR"
      - "MENTEE"
      - "HEIR"
      - "TRUSTEE_PERSON"
      - "REFERENCE_PERSONAL"
      - "REFERENCE_PROFESSIONAL"
      - "AGENT"
      - "BENEFICIARY"
      - "GUARANTOR"
      - "EMPLOYEE"
      - "CONTRACTOR"
      - "OWNER"
      - "BUSINESS_PARTNER"
      - "SERVICE_CONSUMER"
      - "POLITICAL_AFFILIATE"
      - "ADVISOR_CONSULTANT"
      - "PROXY"
      - "SHAREHOLDER"
      - "INVESTOR"
      - "CUSTODIAN"
      - "SOLD_TO"
      - "SHIP_TO"
      - "PAYER"
      - "PAYEE"
      - "GIFT_TO"
      - "LOAN_PROVIDER"
      - "CREDIT_CARD_PROVIDER"
      - "LESSEE"
      - "LESSOR"
      - "INSURED"
      - "INSURER"
      - "UNDERWRITER"
      - "RECIPIENT"
      - "TRANSACTION_FROM"
      - "TRANSACTION_TO"
      - "REMOVE"
      - "OTHER"
    Position:
      type: "object"
      properties:
        position_id:
          type: "string"
          description: "Unique identifier for this position"
          nullable: true
          example: "pos-001"
        position_key:
          type: "string"
          description: "Business key for this position record"
          nullable: true
          example: "POS-ATL-WEST"
        latitude:
          type: "string"
          description: "Latitude of the location"
          nullable: true
          example: "33.7490"
        longitude:
          type: "string"
          description: "Longitude of the location"
          nullable: true
          example: "-84.3880"
        altitude:
          type: "string"
          description: "Altitude of the location"
          nullable: true
          example: "320"
        bearing:
          type: "string"
          description: "Bearing direction (compass)"
          nullable: true
          example: "NW"
        postal_code:
          type: "string"
          description: "Postal code of the location"
          nullable: true
          example: "30303"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
          nullable: true
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          description: "List of associated position codes (zone, region..."
          nullable: true
          example:
          - "ZONE_1"
          - "REGION_EAST"
          items:
            type: "string"
      description: "position meta-data that includes postal code, a..."
    PositionCreate:
      required:
      - "position_key"
      type: "object"
      description: "Position Create for Post"
      allOf:
      - $ref: "#/components/schemas/Position"
    PositionUpdate:
      type: "object"
      properties:
        altitude:
          type: "string"
          description: "Altitude of the location"
          nullable: true
          example: "320"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
          nullable: true
        bearing:
          type: "string"
          description: "Bearing direction (compass)"
          nullable: true
          example: "NW"
        latitude:
          type: "string"
          description: "Latitude of the location"
          nullable: true
          example: "33.7490"
        postal_code:
          type: "string"
          description: "Postal code of the location"
          nullable: true
          example: "30303"
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          description: "List of associated position codes (zone, region..."
          nullable: true
          example:
          - "ZONE_1"
          - "REGION_EAST"
          items:
            type: "string"
        longitude:
          type: "string"
          description: "Longitude of the location"
          nullable: true
          example: "-84.3880"
      description: "Position Update for Patch and Put"
    Price:
      $ref: "#/components/schemas/EffectivePeriod"
    PriceCreate:
      required:
      - "price_key"
      type: "object"
      description: "Price Create for Post"
      allOf:
      - $ref: "#/components/schemas/Price"
    PriceTypes:
      type: "string"
      description: "types of prices."
      enum:
      - "METERED"
      - "SEAT"
      - "FLAT"
      - "ITEMIZED"
      - "BUNDLE"
      - "QUANTITY"
      - "PER_UNIT"
      - "PERCENT"
      - "VOLUME"
      - "USAGE"
      - "STAIR_STEP"
      - "SUBSCRIPTION"
      - "CUSTOM"
      - "STANDARD"
      - "LICENSED"
      - "TIER_VOLUME"
      - "TIER_GRADUATED"
      - "ACTUAL"
      - "LIST"
      - "RETAIL"
      - "MSRP"
      - "WHOLESALE"
      - "DEALER"
      - "TRADE"
      - "CORE"
      - "LANDED"
      - "PROMOTIONAL"
      - "DISCOUNT"
      - "CONTRACT"
      - "SALE"
      - "CUSTOMER"
      - "AFFILIATE"
      - "ONLINE"
      - "STORE"
      - "REGION"
      - "LOCATION"
      - "ESTIMATED"
      - "NET"
      - "PURCHASE"
      - "MONEY"
      - "TAXABLE"
      - "NONTAXABLE"
      - "TOTAL"
      - "UNIT"
      - "TOTAL_COST"
      - "UNIT_COST"
      - "TAX"
      - "OTHER"
    PriceUpdate:
      $ref: "#/components/schemas/EffectivePeriod"
    ProductTypes:
      type: "string"
      description: "types of products."
      enum:
      - "VEHICLE"
      - "INFORMATION"
      - "DURABLE_GOODS"
      - "SOFTWARE"
      - "APP"
      - "INSURANCE"
      - "FINANCE"
      - "SERVICE"
      - "PART"
      - "ACCESSORY"
      - "F_N_I_PRODUCT"
      - "SERVICE_PACKAGE"
      - "DEALER_PRODUCT"
      - "EQUIPMENT"
      - "PACKAGE_GOODS"
      - "SERVICE_PLAN"
      - "WARRANTY_PLAN"
      - "SUBSCRIPTION"
      - "OTHER"
    PurchaseOrderReference:
      $ref: "#/components/schemas/Identifier"
    PurchaseOrderReferenceCreate:
      required:
      - "purchase_order_reference_key"
      type: "object"
      description: "PurchaseOrderReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/PurchaseOrderReference"
    PurchaseOrderReferenceUpdate:
      $ref: "#/components/schemas/Identifier"
    Receipt:
      $ref: "#/components/schemas/ReceiptLineItems"
    ReceiptCreate:
      required:
      - "receipt_key"
      type: "object"
      description: "Receipt Create for Post"
      allOf:
      - $ref: "#/components/schemas/Receipt"
    ReceiptLineItem:
      $ref: "#/components/schemas/PartyIdentifiers"
    ReceiptLineItemCreate:
      required:
      - "receipt_line_item_key"
      type: "object"
      description: "ReceiptLineItem Create for Post"
      allOf:
      - $ref: "#/components/schemas/ReceiptLineItem"
    ReceiptLineItemUpdate:
      $ref: "#/components/schemas/ReceiptStatusTypes"
    ReceiptLineItems:
      type: "array"
      description: "List of ReceiptLineItem"
      items:
        $ref: "#/components/schemas/ReceiptLineItem"
    ReceiptLineItemsCreate:
      type: "array"
      description: "ReceiptLineItems Create for Post"
      items:
        $ref: "#/components/schemas/ReceiptLineItems"
    ReceiptLineItemsUpdate:
      type: "array"
      description: "ReceiptLineItems Update for Patch and Put"
      items:
        $ref: "#/components/schemas/ReceiptLineItems"
    ReceiptStatusTypes:
      type: "string"
      description: "types of receipt status."
      enum:
      - "DRAFT"
      - "PENDING_APPROVAL"
      - "CANCELLED"
      - "CLOSED"
      - "PARTIALLY_RECEIVED"
      - "FULLY_RECEIVED"
      - "PARTIALLY_SHIPPED"
      - "SHIPPED"
      - "DELIVERED"
      - "ON_HOLD"
      - "SALES_HOLD"
      - "PAYMENT_HOLD"
      - "FINANCE_HOLD"
      - "CUSTOMER_HOLD"
      - "INTERNAL_HOLD"
    ReceiptUpdate:
      $ref: "#/components/schemas/ReceiptStatusTypes"
    ResourceTypes:
      type: "string"
      description: "types of resources."
      enum:
      - "SYSTEM"
      - "WORKFLOW"
      - "ACTIVITY"
      - "FUNCTION"
      - "PROCESS"
      - "INCIDENT"
      - "PARTY"
      - "CUSTOMER"
      - "APPLICANT"
      - "VENDOR"
      - "DEALER_STAFF"
      - "DRIVER"
      - "ADDRESS"
      - "BILL_ADDRESS"
      - "BILL_CONTACT"
      - "COMM_CHANNEL"
      - "ACCOUNT"
      - "VEHICLE"
      - "TRADE_IN"
      - "PRODUCT"
      - "MANUFACTURER"
      - "LEAD"
      - "OPPORTUNITY"
      - "DEAL"
      - "FINANCE"
      - "INVOICE"
      - "LINEITEM"
      - "EXPENSE"
      - "DISCOUNT"
      - "TAX"
      - "FEE"
      - "INCENTIVE"
      - "DEALERSHIP"
      - "APPOINTMENT"
      - "OTHER"
    ShipmentEvent:
      $ref: "#/components/schemas/Position"
    ShipmentEventCreate:
      required:
      - "shipment_event_key"
      type: "object"
      description: "ShipmentEvent Create for Post"
      allOf:
      - $ref: "#/components/schemas/ShipmentEvent"
    ShipmentEventUpdate:
      $ref: "#/components/schemas/Position"
    ShipmentEvents:
      type: "array"
      description: "List of ShipmentEvent"
      items:
        $ref: "#/components/schemas/ShipmentEvent"
    ShipmentEventsCreate:
      type: "array"
      description: "ShipmentEvents Create for Post"
      items:
        $ref: "#/components/schemas/ShipmentEvents"
    ShipmentEventsUpdate:
      type: "array"
      description: "ShipmentEvents Update for Patch and Put"
      items:
        $ref: "#/components/schemas/ShipmentEvents"
    ShipmentReference:
      $ref: "#/components/schemas/UnitOfMeasures"
    ShipmentReferenceCreate:
      required:
      - "shipment_reference_key"
      type: "object"
      description: "ShipmentReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/ShipmentReference"
    ShipmentReferenceUpdate:
      $ref: "#/components/schemas/UnitOfMeasures"
    ShipmentReferences:
      type: "array"
      description: "List of ShipmentReference"
      items:
        $ref: "#/components/schemas/ShipmentReference"
    ShipmentReferencesCreate:
      type: "array"
      description: "ShipmentReferences Create for Post"
      items:
        $ref: "#/components/schemas/ShipmentReferences"
    ShipmentReferencesUpdate:
      type: "array"
      description: "ShipmentReferences Update for Patch and Put"
      items:
        $ref: "#/components/schemas/ShipmentReferences"
    ShipmentStateTypes:
      type: "string"
      description: "types of shipment states."
      enum:
      - "OPEN"
      - "ACTIVE"
      - "ESTIMATED_SHIP"
      - "ACTUAL_SHIP"
      - "UPDATING"
      - "ESTIMATED_DELIVERY"
      - "ACTUAL_DELIVERY"
      - "TRACKED"
      - "CONFIRM"
      - "PICKED_UP"
      - "DELIVERED"
      - "MANIFEST_CREATED"
      - "CLOSED"
      - "RETURN_INITIATED"
      - "RETURNED_TO_SENDER"
      - "LOST_IN_TRANSIT"
      - "RETURNED_TO_WAREHOUSE"
    StaffAssignmentReference:
      $ref: "#/components/schemas/Identifier"
    StaffAssignmentReferenceCreate:
      required:
      - "staff_assignment_reference_key"
      type: "object"
      description: "StaffAssignmentReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/StaffAssignmentReference"
    StaffAssignmentReferenceUpdate:
      $ref: "#/components/schemas/Identifier"
    TimeSlot:
      $ref: "#/components/schemas/DaysOfWeekTypes"
    TimeslotDirectiveTypes:
      type: "string"
      description: "Represents the directive for a timeslot."
      enum:
      - "AFTER"
      - "ANYTIME"
      - "BEFORE"
      - "BETWEEN"
      - "BLOCK"
      - "UNKNOWN"
    UOMQuantityCategoryTypes:
      type: "string"
      description: "types of u o m quantity categorys."
      enum:
      - "COUNT"
      - "WEIGHT"
      - "VOLUME"
      - "TIME"
      - "NET_WEIGHT"
      - "NET_VOLUME"
      - "PACKAGE_QUANTITY"
      - "MIN_ORDER_QUANTITY"
      - "GROSS_WEIGHT"
      - "GROSS_VOLUME"
      - "SHIPPING_WEIGHT"
      - "DIMENSIONAL_WEIGHT"
      - "MATERIAL"
      - "ELECTRICAL_RATING"
      - "DEFAULT_LEAD_TIME"
      - "MAX_LEAD_TIME"
      - "DIMENSION_UNIT"
      - "MIN_LENGTH"
      - "MAX_LENGTH"
      - "MIN_WIDTH"
      - "MAX_WIDTH"
      - "MIN_HEIGHT"
      - "MAX_HEIGHT"
      - "MIN_WEIGHT"
      - "MAX_WEIGHT"
      - "MAX_ORDER_QUANTITY"
      - "MIN_ORDER_AVAILABLE"
      - "DIMENSIONS"
      - "SHIPPING_DIM_LENGTH"
      - "SHIPPING_DIM_WIDTH"
      - "SHIPPING_DIM_HEIGHT"
      - "SHIPPING_DIM_GROSS_WEIGHT"
      - "SHIPPING_DIM_NET_WEIGHT"
      - "SHELF_LIFE_HOURS"
      - "EXPIRATION_DATE"
    UnitOfMeasure:
      $ref: "#/components/schemas/UOMQuantityCategoryTypes"
    UnitOfMeasures:
      type: "array"
      description: "List of UnitOfMeasure"
      items:
        $ref: "#/components/schemas/UnitOfMeasure"
    UnitOfMeasuresCreate:
      type: "array"
      description: "UnitOfMeasures Create for Post"
      items:
        $ref: "#/components/schemas/UnitOfMeasures"
    UnitOfMeasuresUpdate:
      type: "array"
      description: "UnitOfMeasures Update for Patch and Put"
      items:
        $ref: "#/components/schemas/UnitOfMeasures"
    WarehouseReference:
      $ref: "#/components/schemas/Identifier"
    WarehouseReferenceCreate:
      required:
      - "warehouse_reference_key"
      type: "object"
      description: "WarehouseReference Create for Post"
      allOf:
      - $ref: "#/components/schemas/WarehouseReference"
    WarehouseReferenceUpdate:
      $ref: "#/components/schemas/Identifier"
  responses:
    NotFoundError:
      description: "The requested resource was not found."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: "Authentication failed. The access token is miss..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: "The authenticated user does not have the necess..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: "The request could not be completed because it c..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: "An unexpected error occurred on the server whil..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
