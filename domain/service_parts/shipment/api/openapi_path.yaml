---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /shipments:
    get:
      tags:
      - "Shipment"
      summary: "Retrieve a list of all Shipment records."
      operationId: "listShipments"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of Shipment records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/Shipment"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Shipment"
      summary: "Create a new Shipment record."
      operationId: "createShipment"
      requestBody:
        description: "Shipment data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Shipment_Create"
              
        required: true
      responses:
        "201":
          description: "Shipment created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Shipment"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}:
    get:
      tags:
      - "Shipment"
      summary: "Retrieve a specific Shipment record."
      operationId: "getShipmentById"
      responses:
        "200":
          description: "The requested Shipment record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Shipment"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Shipment"
      summary: "replace a Shipment record."
      operationId: "replaceShipment"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Shipment"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Shipment"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Shipment"
      summary: "Delete a Shipment Record"
      description: "Marks the Shipment record as inactive or removes it entirely."
      operationId: "deleteShipmentRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Shipment"
      summary: "Partially update a Shipment record."
      operationId: "updateShipment"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Shipment_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Shipment"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "shipmentKey"
      in: "path"
      description: "The unique identifier for the specific Shipment."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/invoice-references:
    get:
      tags:
      - "InvoiceReference"
      summary: "Retrieve a list of all InvoiceReference records."
      operationId: "listInvoiceReferences"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of InvoiceReference records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/InvoiceReference"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "InvoiceReference"
      summary: "Create a new InvoiceReference record."
      operationId: "createInvoiceReference"
      requestBody:
        description: "InvoiceReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvoiceReference_Create"
              
        required: true
      responses:
        "201":
          description: "InvoiceReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/invoice-references/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/invoice-references/{invoiceReferenceKey}:
    get:
      tags:
      - "InvoiceReference"
      summary: "Retrieve a specific InvoiceReference record."
      operationId: "getInvoiceReferenceById"
      responses:
        "200":
          description: "The requested InvoiceReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceReference"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "InvoiceReference"
      summary: "replace a InvoiceReference record."
      operationId: "replaceInvoiceReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvoiceReference"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "InvoiceReference"
      summary: "Delete a InvoiceReference Record"
      description: "Marks the InvoiceReference record as inactive or removes it entirely."
      operationId: "deleteInvoiceReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "InvoiceReference"
      summary: "Partially update a InvoiceReference record."
      operationId: "updateInvoiceReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvoiceReference_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "invoiceReferenceKey"
      in: "path"
      description: "The unique identifier for the specific InvoiceReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/shipment-events:
    get:
      tags:
      - "ShipmentEvent"
      summary: "Retrieve a list of all ShipmentEvent records."
      operationId: "listShipmentEvents"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of ShipmentEvent records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/ShipmentEvent"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ShipmentEvent"
      summary: "Create a new ShipmentEvent record."
      operationId: "createShipmentEvent"
      requestBody:
        description: "ShipmentEvent data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEvent_Create"
              
        required: true
      responses:
        "201":
          description: "ShipmentEvent created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/shipment-events/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/shipment-events/{shipmentEventKey}:
    get:
      tags:
      - "ShipmentEvent"
      summary: "Retrieve a specific ShipmentEvent record."
      operationId: "getShipmentEventById"
      responses:
        "200":
          description: "The requested ShipmentEvent record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ShipmentEvent"
      summary: "replace a ShipmentEvent record."
      operationId: "replaceShipmentEvent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEvent"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ShipmentEvent"
      summary: "Delete a ShipmentEvent Record"
      description: "Marks the ShipmentEvent record as inactive or removes it entirely."
      operationId: "deleteShipmentEventRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ShipmentEvent"
      summary: "Partially update a ShipmentEvent record."
      operationId: "updateShipmentEvent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentEvent_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentEvent"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "shipmentEventKey"
      in: "path"
      description: "The unique identifier for the specific ShipmentEvent."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of all Identifier records."
      operationId: "listIdentifiers"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/Identifier"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Create"
              
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/identifiers/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or removes it entirely."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/unit-of-measures:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a list of all UnitOfMeasure records."
      operationId: "listUnitOfMeasures"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of UnitOfMeasure records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/UnitOfMeasure"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "UnitOfMeasure"
      summary: "Create a new UnitOfMeasure record."
      operationId: "createUnitOfMeasure"
      requestBody:
        description: "UnitOfMeasure data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure_Create"
              
        required: true
      responses:
        "201":
          description: "UnitOfMeasure created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/unit-of-measures/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/unit-of-measures/{unitOfMeasureKey}:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a specific UnitOfMeasure record."
      operationId: "getUnitOfMeasureById"
      responses:
        "200":
          description: "The requested UnitOfMeasure record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "UnitOfMeasure"
      summary: "replace a UnitOfMeasure record."
      operationId: "replaceUnitOfMeasure"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "UnitOfMeasure"
      summary: "Delete a UnitOfMeasure Record"
      description: "Marks the UnitOfMeasure record as inactive or removes it entirely."
      operationId: "deleteUnitOfMeasureRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "UnitOfMeasure"
      summary: "Partially update a UnitOfMeasure record."
      operationId: "updateUnitOfMeasure"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "unitOfMeasureKey"
      in: "path"
      description: "The unique identifier for the specific UnitOfMeasure."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/positions:
    get:
      tags:
      - "Position"
      summary: "Retrieve a list of all Position records."
      operationId: "listPositions"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of Position records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/Position"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Position"
      summary: "Create a new Position record."
      operationId: "createPosition"
      requestBody:
        description: "Position data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position_Create"
              
        required: true
      responses:
        "201":
          description: "Position created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/positions/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/positions/{positionKey}:
    get:
      tags:
      - "Position"
      summary: "Retrieve a specific Position record."
      operationId: "getPositionById"
      responses:
        "200":
          description: "The requested Position record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Position"
      summary: "replace a Position record."
      operationId: "replacePosition"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Position"
      summary: "Delete a Position Record"
      description: "Marks the Position record as inactive or removes it entirely."
      operationId: "deletePositionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Position"
      summary: "Partially update a Position record."
      operationId: "updatePosition"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "positionKey"
      in: "path"
      description: "The unique identifier for the specific Position."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
  /shipments/{shipmentKey}/address-references:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a list of all AddressReference records."
      operationId: "listAddressReferences"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
          
      responses:
        "200":
          description: "A list of AddressReference records."
          content:
            application/json:
              schema:
                type: "array"
                
                items:
                  $ref: "#/components/schemas/AddressReference"
                  
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AddressReference"
      summary: "Create a new AddressReference record."
      operationId: "createAddressReference"
      requestBody:
        description: "AddressReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference_Create"
              
        required: true
      responses:
        "201":
          description: "AddressReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "//shipments/{shipmentKey}/address-references/PR-1234567"

          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /shipments/{shipmentKey}/address-references/{addressReferenceKey}:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a specific AddressReference record."
      operationId: "getAddressReferenceById"
      responses:
        "200":
          description: "The requested AddressReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
                
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AddressReference"
      summary: "replace a AddressReference record."
      operationId: "replaceAddressReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AddressReference"
      summary: "Delete a AddressReference Record"
      description: "Marks the AddressReference record as inactive or removes it entirely."
      operationId: "deleteAddressReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AddressReference"
      summary: "Partially update a AddressReference record."
      operationId: "updateAddressReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference_Update"
              
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
                
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "addressReferenceKey"
      in: "path"
      description: "The unique identifier for the specific AddressReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
        exampleSetFlag: true
components:
  schemas: {}
