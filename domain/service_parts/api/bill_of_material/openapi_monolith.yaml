---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /bill-of-materials:
    get:
      tags:
      - "BillOfMaterial"
      summary: "Retrieve a list of all BillOfMaterial records."
      operationId: "listBillOfMaterials"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of BillOfMaterial records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/BillOfMaterial"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "BillOfMaterial"
      summary: "Create a new BillOfMaterial record."
      operationId: "createBillOfMaterial"
      requestBody:
        description: "BillOfMaterial data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillOfMaterialCreate"
        required: true
      responses:
        "201":
          description: "BillOfMaterial created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/bill-of-materials/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillOfMaterial"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /bill-of-materials/{billOfMaterialKey}:
    get:
      tags:
      - "BillOfMaterial"
      summary: "Retrieve a specific BillOfMaterial record."
      operationId: "getBillOfMaterialById"
      responses:
        "200":
          description: "The requested BillOfMaterial record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillOfMaterial"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "BillOfMaterial"
      summary: "Replace a BillOfMaterial record."
      operationId: "replaceBillOfMaterial"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillOfMaterial"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillOfMaterial"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "BillOfMaterial"
      summary: "Delete a BillOfMaterial Record"
      description: "Marks the BillOfMaterial record as inactive or ..."
      operationId: "deleteBillOfMaterialRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "BillOfMaterial"
      summary: "Partially update a BillOfMaterial record."
      operationId: "updateBillOfMaterial"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillOfMaterialUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillOfMaterial"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the specific BillOfMaterial."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/bom-components:
    get:
      tags:
      - "BomComponent"
      summary: "Retrieve a list of BomComponent records scoped ..."
      operationId: "listBomComponentsByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of BomComponent records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/BomComponent"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "BomComponent"
      summary: "Create a new BomComponent record."
      operationId: "createBomComponent"
      requestBody:
        description: "BomComponent data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BomComponentCreate"
        required: true
      responses:
        "201":
          description: "BomComponent created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BomComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/bom-components/{bomComponentKey}:
    get:
      tags:
      - "BomComponent"
      summary: "Retrieve a specific BomComponent record."
      operationId: "getBomComponentById"
      responses:
        "200":
          description: "The requested BomComponent record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BomComponent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "BomComponent"
      summary: "Replace a BomComponent record."
      operationId: "replaceBomComponent"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BomComponent"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BomComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "BomComponent"
      summary: "Delete a BomComponent Record"
      description: "Marks the BomComponent record as inactive or re..."
      operationId: "deleteBomComponentRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "BomComponent"
      summary: "Partially update a BomComponent record."
      operationId: "updateBomComponent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BomComponentUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BomComponent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "bomComponentKey"
      in: "path"
      description: "The unique identifier for the specific BomComponent."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by..."
      operationId: "listIdentifiersByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or remo..."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/part-names:
    get:
      tags:
      - "PartName"
      summary: "Retrieve a list of PartName records scoped by b..."
      operationId: "listPartNamesByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartName records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartName"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartName"
      summary: "Create a new PartName record."
      operationId: "createPartName"
      requestBody:
        description: "PartName data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartNameCreate"
        required: true
      responses:
        "201":
          description: "PartName created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/part-names/{partNameKey}:
    get:
      tags:
      - "PartName"
      summary: "Retrieve a specific PartName record."
      operationId: "getPartNameById"
      responses:
        "200":
          description: "The requested PartName record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartName"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartName"
      summary: "Replace a PartName record."
      operationId: "replacePartName"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartName"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartName"
      summary: "Delete a PartName Record"
      description: "Marks the PartName record as inactive or remove..."
      operationId: "deletePartNameRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartName"
      summary: "Partially update a PartName record."
      operationId: "updatePartName"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartNameUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartName"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partNameKey"
      in: "path"
      description: "The unique identifier for the specific PartName."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/unit-of-measures:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a list of UnitOfMeasure records scoped..."
      operationId: "listUnitOfMeasuresByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of UnitOfMeasure records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/UnitOfMeasure"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/unit-of-measures/{unitOfMeasureKey}:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a specific UnitOfMeasure record."
      operationId: "getUnitOfMeasureById"
      responses:
        "200":
          description: "The requested UnitOfMeasure record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "UnitOfMeasure"
      summary: "Replace a UnitOfMeasure record."
      operationId: "replaceUnitOfMeasure"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "unitOfMeasureKey"
      in: "path"
      description: "The unique identifier for the specific UnitOfMeasure."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/part-identifiers:
    get:
      tags:
      - "PartIdentifier"
      summary: "Retrieve a list of PartIdentifier records scope..."
      operationId: "listPartIdentifiersByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartIdentifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartIdentifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartIdentifier"
      summary: "Create a new PartIdentifier record."
      operationId: "createPartIdentifier"
      requestBody:
        description: "PartIdentifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartIdentifierCreate"
        required: true
      responses:
        "201":
          description: "PartIdentifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/part-identifiers/{partIdentifierKey}:
    get:
      tags:
      - "PartIdentifier"
      summary: "Retrieve a specific PartIdentifier record."
      operationId: "getPartIdentifierById"
      responses:
        "200":
          description: "The requested PartIdentifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartIdentifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartIdentifier"
      summary: "Replace a PartIdentifier record."
      operationId: "replacePartIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartIdentifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartIdentifier"
      summary: "Delete a PartIdentifier Record"
      description: "Marks the PartIdentifier record as inactive or ..."
      operationId: "deletePartIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartIdentifier"
      summary: "Partially update a PartIdentifier record."
      operationId: "updatePartIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartIdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartIdentifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partIdentifierKey"
      in: "path"
      description: "The unique identifier for the specific PartIdentifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/effective-periods:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a list of EffectivePeriod records scop..."
      operationId: "listEffectivePeriodsByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of EffectivePeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/EffectivePeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a specific EffectivePeriod record."
      operationId: "getEffectivePeriodById"
      responses:
        "200":
          description: "The requested EffectivePeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "EffectivePeriod"
      summary: "Replace a EffectivePeriod record."
      operationId: "replaceEffectivePeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "effectivePeriodKey"
      in: "path"
      description: "The unique identifier for the specific EffectivePeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/part-specifications:
    get:
      tags:
      - "PartSpecification"
      summary: "Retrieve a list of PartSpecification records sc..."
      operationId: "listPartSpecificationsByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartSpecification records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartSpecification"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartSpecification"
      summary: "Create a new PartSpecification record."
      operationId: "createPartSpecification"
      requestBody:
        description: "PartSpecification data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartSpecificationCreate"
        required: true
      responses:
        "201":
          description: "PartSpecification created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartSpecification"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/part-specifications/{partSpecificationKey}:
    get:
      tags:
      - "PartSpecification"
      summary: "Retrieve a specific PartSpecification record."
      operationId: "getPartSpecificationById"
      responses:
        "200":
          description: "The requested PartSpecification record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartSpecification"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartSpecification"
      summary: "Replace a PartSpecification record."
      operationId: "replacePartSpecification"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartSpecification"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartSpecification"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartSpecification"
      summary: "Delete a PartSpecification Record"
      description: "Marks the PartSpecification record as inactive ..."
      operationId: "deletePartSpecificationRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartSpecification"
      summary: "Partially update a PartSpecification record."
      operationId: "updatePartSpecification"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartSpecificationUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartSpecification"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partSpecificationKey"
      in: "path"
      description: "The unique identifier for the specific PartSpecification."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /bill-of-materials/{billOfMaterialKey}/textual-details:
    get:
      tags:
      - "TextualDetail"
      summary: "Retrieve a list of TextualDetail records scoped..."
      operationId: "listTextualDetailsByBillOfMaterialKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of TextualDetail records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/TextualDetail"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "TextualDetail"
      summary: "Create a new TextualDetail record."
      operationId: "createTextualDetail"
      requestBody:
        description: "TextualDetail data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetailCreate"
        required: true
      responses:
        "201":
          description: "TextualDetail created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /bill-of-materials/{billOfMaterialKey}/textual-details/{textualDetailKey}:
    get:
      tags:
      - "TextualDetail"
      summary: "Retrieve a specific TextualDetail record."
      operationId: "getTextualDetailById"
      responses:
        "200":
          description: "The requested TextualDetail record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "TextualDetail"
      summary: "Replace a TextualDetail record."
      operationId: "replaceTextualDetail"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetail"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "TextualDetail"
      summary: "Delete a TextualDetail Record"
      description: "Marks the TextualDetail record as inactive or r..."
      operationId: "deleteTextualDetailRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "TextualDetail"
      summary: "Partially update a TextualDetail record."
      operationId: "updateTextualDetail"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetailUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "billOfMaterialKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "textualDetailKey"
      in: "path"
      description: "The unique identifier for the specific TextualDetail."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas:
    BillOfMaterial:
      $ref: "#/components/schemas/PartSpecification"
    BillOfMaterialCreate:
      required:
      - "bill_of_material_key"
      type: "object"
      description: "BillOfMaterial Create for Post"
      allOf:
      - $ref: "#/components/schemas/BillOfMaterial"
    BillOfMaterialUpdate:
      $ref: "#/components/schemas/PartSpecification"
    BomComponent:
      $ref: "#/components/schemas/PartSpecification"
    BomComponentCreate:
      required:
      - "bom_component_key"
      type: "object"
      description: "BomComponent Create for Post"
      allOf:
      - $ref: "#/components/schemas/BomComponent"
    BomComponentUpdate:
      $ref: "#/components/schemas/PartSpecification"
    BomComponents:
      type: "array"
      description: "List of BomComponent"
      items:
        $ref: "#/components/schemas/BomComponent"
    BomComponentsCreate:
      type: "array"
      description: "BomComponents Create for Post"
      items:
        $ref: "#/components/schemas/BomComponents"
    BomComponentsUpdate:
      type: "array"
      description: "BomComponents Update for Patch and Put"
      items:
        $ref: "#/components/schemas/BomComponents"
    Codes:
      type: "array"
      description: "List of Codes"
      items:
        type: "string"
    DaysOfWeekTypes:
      type: "string"
      description: "Status of the account"
      enum:
      - "SUN"
      - "MON"
      - "TUES"
      - "WED"
      - "THURS"
      - "FRI"
      - "SAT"
    DurationUOMTypes:
      type: "string"
      description: "types of duration u o ms."
      enum:
      - "MINUTES"
      - "HOURS"
      - "DAYS"
      - "DAY_OF_MONTH"
      - "WEEKS"
      - "BI_WEEKS"
      - "SEMI_MONTHS"
      - "MONTHS"
      - "QUARTERS"
      - "SEMI_YEARS"
      - "YEARS"
      - "MULTI_YEARS"
      - "CUSTOM"
      - "OCCURRENCE"
      - "UNLIMITED"
      - "NONE"
      - "OTHER"
    EffectivePeriod:
      $ref: "#/components/schemas/TimeSlot"
    ErrorDetail:
      required:
      - "code"
      - "message"
      - "timestamp"
      type: "object"
      properties:
        code:
          type: "string"
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: "NOT_FOUND_PR"
        message:
          type: "string"
          description: "A human-readable summary providing specific det..."
          example: "Personnel Record with ID PR-1234567 was not found."
        details:
          type: "array"
          description: "Optional array of specific field-level validati..."
          items:
            type: "object"
            properties:
              field:
                type: "string"
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: "payrollId"
              issue:
                type: "string"
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: "must be unique"
        timestamp:
          type: "string"
          description: "The time the error occurred, in ISO 8601 format."
          format: "date-time"
          example: "2025-11-10T10:30:00Z"
      description: "Standard format for all API error responses."
    Identifier:
      $ref: "#/components/schemas/Codes"
    IdentifierCreate:
      required:
      - "identifier_key"
      type: "object"
      description: "Identifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      $ref: "#/components/schemas/Codes"
    Identifiers:
      type: "array"
      description: "List of Identifier"
      items:
        $ref: "#/components/schemas/Identifier"
    IdentifiersCreate:
      type: "array"
      description: "Identifiers Create for Post"
      items:
        $ref: "#/components/schemas/Identifiers"
    IdentifiersUpdate:
      type: "array"
      description: "Identifiers Update for Patch and Put"
      items:
        $ref: "#/components/schemas/Identifiers"
    Link:
      type: "array"
      description: "Quick Link"
      items:
        type: "string"
    Links:
      type: "array"
      description: "List of Links"
      items:
        type: "string"
    Memo:
      type: "array"
      description: "List of String"
      items:
        type: "string"
    MemoCreate:
      type: "array"
      description: "Memo Create for Post"
      items:
        $ref: "#/components/schemas/Memo"
    MemoUpdate:
      type: "array"
      description: "Memo Update for Patch and Put"
      items:
        $ref: "#/components/schemas/Memo"
    PartConditionGradeTypes:
      type: "string"
      description: "types of part condition grades."
      enum:
      - "EXCELLENT"
      - "LIKE_NEW"
      - "VERY_GOOD"
      - "GOOD"
      - "ACCEPTABLE"
      - "FAIR"
      - "POOR"
      - "DAMAGED"
      - "UNGRADED"
      - "OTHER"
    PartConditionTypes:
      type: "string"
      description: "types of part conditions."
      enum:
      - "NEW"
      - "NEW_OLD_STOCK"
      - "REMANUFACTURED"
      - "REBUILT"
      - "REFURBISHED"
      - "USED"
      - "SALVAGE"
      - "DAMAGED_MINOR"
      - "DAMAGED_COSMETIC"
      - "SCRAP"
      - "COLLECTIBLE"
      - "OTHER"
    PartIdentifier:
      $ref: "#/components/schemas/PartNames"
    PartIdentifierCreate:
      required:
      - "part_identifier_key"
      type: "object"
      description: "PartIdentifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartIdentifier"
    PartIdentifierTypes:
      type: "string"
      description: "types of part identifiers."
      enum:
      - "VIN"
      - "SKU"
      - "UPCA"
      - "UPCE"
      - "ISBN"
      - "MPN"
      - "CONFIG_CODE"
      - "MFR_CODE"
      - "ALT_CODE"
      - "STOCK_NBR"
      - "UNIV_CODE"
      - "PRODUCT_CODE"
      - "CATALOG_CODE"
      - "NAME"
      - "NUMBER"
      - "PREFIX"
      - "COUNTRY_OF_ORIGIN_CODE"
      - "MODEL_CODE"
      - "MAKE_CODE"
      - "YEAR_CODE"
      - "MARKET_DIVISION_CODE"
      - "TRIM_CODE"
      - "STYLE_CODE"
      - "SUB_MODEL_CODE"
      - "ENGINE_CODE"
      - "TRANSMISSION_CODE"
      - "DRAWING_NUMBER"
      - "OTHER"
    PartIdentifierUpdate:
      $ref: "#/components/schemas/PartNames"
    PartIdentifiers:
      type: "array"
      description: "List of PartIdentifier"
      items:
        $ref: "#/components/schemas/PartIdentifier"
    PartIdentifiersCreate:
      type: "array"
      description: "PartIdentifiers Create for Post"
      items:
        $ref: "#/components/schemas/PartIdentifiers"
    PartIdentifiersUpdate:
      type: "array"
      description: "PartIdentifiers Update for Patch and Put"
      items:
        $ref: "#/components/schemas/PartIdentifiers"
    PartName:
      $ref: "#/components/schemas/Codes"
    PartNameCreate:
      required:
      - "part_name_key"
      type: "object"
      description: "PartName Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartName"
    PartNameTypes:
      type: "string"
      description: "types of part names."
      enum:
      - "GENERIC"
      - "MANUFACTURING"
      - "MARKETING"
      - "CORE"
      - "REMANUFACTURING"
      - "OEM"
      - "AFTERMARKET"
      - "SUPERSEDING"
      - "SUPERSEDED"
    PartNameUpdate:
      $ref: "#/components/schemas/Codes"
    PartNames:
      type: "array"
      description: "List of PartName"
      items:
        $ref: "#/components/schemas/PartName"
    PartNamesCreate:
      type: "array"
      description: "PartNames Create for Post"
      items:
        $ref: "#/components/schemas/PartNames"
    PartNamesUpdate:
      type: "array"
      description: "PartNames Update for Patch and Put"
      items:
        $ref: "#/components/schemas/PartNames"
    PartSpecification:
      $ref: "#/components/schemas/ProductPackageTypes"
    PartSpecificationCreate:
      required:
      - "part_specification_key"
      type: "object"
      description: "PartSpecification Create for Post"
      allOf:
      - $ref: "#/components/schemas/PartSpecification"
    PartSpecificationUpdate:
      $ref: "#/components/schemas/ProductPackageTypes"
    ProductPackageTypes:
      type: "string"
      description: "types of product packages."
      enum:
      - "SKU"
      - "COMPONENT"
      - "STANDALONE"
      - "ITEM"
      - "UNIT"
      - "OCCURRENCE"
      - "EACH"
      - "KIT"
      - "BUNDLE"
      - "GROUP"
      - "MIXED_BUNDLE"
      - "PURE_BUNDLE"
      - "SET"
      - "PACK"
      - "CASE"
      - "PALLET"
      - "SERVICE"
      - "SUBSCRIPTION"
      - "PRODUCT"
    ProductStatusTypes:
      type: "string"
      description: "types of product status."
      enum:
      - "DRAFT"
      - "PENDING"
      - "REVIEW"
      - "ACTIVE"
      - "PILOT"
      - "RETIRED"
      - "CANCELLED"
      - "SUSPENDED"
      - "OBSOLETE"
      - "UNAVAILABLE"
      - "TERMINATED"
    TextualDetail:
      $ref: "#/components/schemas/Codes"
    TextualDetails:
      type: "array"
      description: "List of TextualDetail"
      items:
        $ref: "#/components/schemas/TextualDetail"
    TextualDetailsCreate:
      type: "array"
      description: "TextualDetails Create for Post"
      items:
        $ref: "#/components/schemas/TextualDetails"
    TextualDetailsUpdate:
      type: "array"
      description: "TextualDetails Update for Patch and Put"
      items:
        $ref: "#/components/schemas/TextualDetails"
    TimeSlot:
      $ref: "#/components/schemas/DaysOfWeekTypes"
    TimeslotDirectiveTypes:
      type: "string"
      description: "Represents the directive for a timeslot."
      enum:
      - "AFTER"
      - "ANYTIME"
      - "BEFORE"
      - "BETWEEN"
      - "BLOCK"
      - "UNKNOWN"
    UOMQuantityCategoryTypes:
      type: "string"
      description: "types of u o m quantity categorys."
      enum:
      - "COUNT"
      - "WEIGHT"
      - "VOLUME"
      - "TIME"
      - "NET_WEIGHT"
      - "NET_VOLUME"
      - "PACKAGE_QUANTITY"
      - "MIN_ORDER_QUANTITY"
      - "GROSS_WEIGHT"
      - "GROSS_VOLUME"
      - "SHIPPING_WEIGHT"
      - "DIMENSIONAL_WEIGHT"
      - "MATERIAL"
      - "ELECTRICAL_RATING"
      - "DEFAULT_LEAD_TIME"
      - "MAX_LEAD_TIME"
      - "DIMENSION_UNIT"
      - "MIN_LENGTH"
      - "MAX_LENGTH"
      - "MIN_WIDTH"
      - "MAX_WIDTH"
      - "MIN_HEIGHT"
      - "MAX_HEIGHT"
      - "MIN_WEIGHT"
      - "MAX_WEIGHT"
      - "MAX_ORDER_QUANTITY"
      - "MIN_ORDER_AVAILABLE"
      - "DIMENSIONS"
      - "SHIPPING_DIM_LENGTH"
      - "SHIPPING_DIM_WIDTH"
      - "SHIPPING_DIM_HEIGHT"
      - "SHIPPING_DIM_GROSS_WEIGHT"
      - "SHIPPING_DIM_NET_WEIGHT"
      - "SHELF_LIFE_HOURS"
      - "EXPIRATION_DATE"
    UnitOfMeasure:
      $ref: "#/components/schemas/UOMQuantityCategoryTypes"
    UnitOfMeasures:
      type: "array"
      description: "List of UnitOfMeasure"
      items:
        $ref: "#/components/schemas/UnitOfMeasure"
    UnitOfMeasuresCreate:
      type: "array"
      description: "UnitOfMeasures Create for Post"
      items:
        $ref: "#/components/schemas/UnitOfMeasures"
    UnitOfMeasuresUpdate:
      type: "array"
      description: "UnitOfMeasures Update for Patch and Put"
      items:
        $ref: "#/components/schemas/UnitOfMeasures"
  responses:
    NotFoundError:
      description: "The requested resource was not found."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: "Authentication failed. The access token is miss..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: "The authenticated user does not have the necess..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: "The request could not be completed because it c..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: "An unexpected error occurred on the server whil..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
