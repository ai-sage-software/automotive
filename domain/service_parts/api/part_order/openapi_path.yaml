---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /part-orders:
    get:
      tags:
      - "PartOrder"
      summary: "Retrieve a list of all PartOrder records."
      operationId: "listPartOrders"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartOrder records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartOrder"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartOrder"
      summary: "Create a new PartOrder record."
      operationId: "createPartOrder"
      requestBody:
        description: "PartOrder data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrderCreate"
        required: true
      responses:
        "201":
          description: "PartOrder created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-orders/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrder"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /part-orders/{partOrderKey}:
    get:
      tags:
      - "PartOrder"
      summary: "Retrieve a specific PartOrder record."
      operationId: "getPartOrderById"
      responses:
        "200":
          description: "The requested PartOrder record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrder"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartOrder"
      summary: "Replace a PartOrder record."
      operationId: "replacePartOrder"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrder"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrder"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartOrder"
      summary: "Delete a PartOrder Record"
      description: "Marks the PartOrder record as inactive or removes it entirely."
      operationId: "deletePartOrderRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartOrder"
      summary: "Partially update a PartOrder record."
      operationId: "updatePartOrder"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrderUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrder"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the specific PartOrder."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/payment-term-references:
    get:
      tags:
      - "PaymentTermReference"
      summary: "Retrieve a list of PaymentTermReference records scoped by partOrderKey."
      operationId: "listPaymentTermReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PaymentTermReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PaymentTermReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PaymentTermReference"
      summary: "Create a new PaymentTermReference record."
      operationId: "createPaymentTermReference"
      requestBody:
        description: "PaymentTermReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentTermReferenceCreate"
        required: true
      responses:
        "201":
          description: "PaymentTermReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentTermReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/payment-term-references/{paymentTermReferenceKey}:
    get:
      tags:
      - "PaymentTermReference"
      summary: "Retrieve a specific PaymentTermReference record."
      operationId: "getPaymentTermReferenceById"
      responses:
        "200":
          description: "The requested PaymentTermReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentTermReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PaymentTermReference"
      summary: "Replace a PaymentTermReference record."
      operationId: "replacePaymentTermReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentTermReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentTermReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PaymentTermReference"
      summary: "Delete a PaymentTermReference Record"
      description: "Marks the PaymentTermReference record as inactive or removes it\
        \ entirely."
      operationId: "deletePaymentTermReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PaymentTermReference"
      summary: "Partially update a PaymentTermReference record."
      operationId: "updatePaymentTermReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentTermReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentTermReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "paymentTermReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PaymentTermReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/discount-references:
    get:
      tags:
      - "DiscountReference"
      summary: "Retrieve a list of DiscountReference records scoped by partOrderKey."
      operationId: "listDiscountReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of DiscountReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/DiscountReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "DiscountReference"
      summary: "Create a new DiscountReference record."
      operationId: "createDiscountReference"
      requestBody:
        description: "DiscountReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscountReferenceCreate"
        required: true
      responses:
        "201":
          description: "DiscountReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/discount-references/{discountReferenceKey}:
    get:
      tags:
      - "DiscountReference"
      summary: "Retrieve a specific DiscountReference record."
      operationId: "getDiscountReferenceById"
      responses:
        "200":
          description: "The requested DiscountReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscountReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "DiscountReference"
      summary: "Replace a DiscountReference record."
      operationId: "replaceDiscountReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscountReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "DiscountReference"
      summary: "Delete a DiscountReference Record"
      description: "Marks the DiscountReference record as inactive or removes it entirely."
      operationId: "deleteDiscountReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "DiscountReference"
      summary: "Partially update a DiscountReference record."
      operationId: "updateDiscountReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscountReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "discountReferenceKey"
      in: "path"
      description: "The unique identifier for the specific DiscountReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/address-references:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a list of AddressReference records scoped by partOrderKey."
      operationId: "listAddressReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of AddressReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/AddressReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AddressReference"
      summary: "Create a new AddressReference record."
      operationId: "createAddressReference"
      requestBody:
        description: "AddressReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReferenceCreate"
        required: true
      responses:
        "201":
          description: "AddressReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/address-references/{addressReferenceKey}:
    get:
      tags:
      - "AddressReference"
      summary: "Retrieve a specific AddressReference record."
      operationId: "getAddressReferenceById"
      responses:
        "200":
          description: "The requested AddressReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AddressReference"
      summary: "Replace a AddressReference record."
      operationId: "replaceAddressReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AddressReference"
      summary: "Delete a AddressReference Record"
      description: "Marks the AddressReference record as inactive or removes it entirely."
      operationId: "deleteAddressReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AddressReference"
      summary: "Partially update a AddressReference record."
      operationId: "updateAddressReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "addressReferenceKey"
      in: "path"
      description: "The unique identifier for the specific AddressReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/financial-category-references:
    get:
      tags:
      - "FinancialCategoryReference"
      summary: "Retrieve a list of FinancialCategoryReference records scoped by partOrderKey."
      operationId: "listFinancialCategoryReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialCategoryReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialCategoryReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialCategoryReference"
      summary: "Create a new FinancialCategoryReference record."
      operationId: "createFinancialCategoryReference"
      requestBody:
        description: "FinancialCategoryReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialCategoryReferenceCreate"
        required: true
      responses:
        "201":
          description: "FinancialCategoryReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialCategoryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/financial-category-references/{financialCategoryReferenceKey}:
    get:
      tags:
      - "FinancialCategoryReference"
      summary: "Retrieve a specific FinancialCategoryReference record."
      operationId: "getFinancialCategoryReferenceById"
      responses:
        "200":
          description: "The requested FinancialCategoryReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialCategoryReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialCategoryReference"
      summary: "Replace a FinancialCategoryReference record."
      operationId: "replaceFinancialCategoryReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialCategoryReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialCategoryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialCategoryReference"
      summary: "Delete a FinancialCategoryReference Record"
      description: "Marks the FinancialCategoryReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteFinancialCategoryReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialCategoryReference"
      summary: "Partially update a FinancialCategoryReference record."
      operationId: "updateFinancialCategoryReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialCategoryReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialCategoryReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialCategoryReferenceKey"
      in: "path"
      description: "The unique identifier for the specific FinancialCategoryReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/staff-assignment-references:
    get:
      tags:
      - "StaffAssignmentReference"
      summary: "Retrieve a list of StaffAssignmentReference records scoped by partOrderKey."
      operationId: "listStaffAssignmentReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of StaffAssignmentReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/StaffAssignmentReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "StaffAssignmentReference"
      summary: "Create a new StaffAssignmentReference record."
      operationId: "createStaffAssignmentReference"
      requestBody:
        description: "StaffAssignmentReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceCreate"
        required: true
      responses:
        "201":
          description: "StaffAssignmentReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/staff-assignment-references/{staffAssignmentReferenceKey}:
    get:
      tags:
      - "StaffAssignmentReference"
      summary: "Retrieve a specific StaffAssignmentReference record."
      operationId: "getStaffAssignmentReferenceById"
      responses:
        "200":
          description: "The requested StaffAssignmentReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "StaffAssignmentReference"
      summary: "Replace a StaffAssignmentReference record."
      operationId: "replaceStaffAssignmentReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "StaffAssignmentReference"
      summary: "Delete a StaffAssignmentReference Record"
      description: "Marks the StaffAssignmentReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteStaffAssignmentReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "StaffAssignmentReference"
      summary: "Partially update a StaffAssignmentReference record."
      operationId: "updateStaffAssignmentReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffAssignmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StaffAssignmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "staffAssignmentReferenceKey"
      in: "path"
      description: "The unique identifier for the specific StaffAssignmentReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/fee-references:
    get:
      tags:
      - "FeeReference"
      summary: "Retrieve a list of FeeReference records scoped by partOrderKey."
      operationId: "listFeeReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FeeReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FeeReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FeeReference"
      summary: "Create a new FeeReference record."
      operationId: "createFeeReference"
      requestBody:
        description: "FeeReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeeReferenceCreate"
        required: true
      responses:
        "201":
          description: "FeeReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeeReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/fee-references/{feeReferenceKey}:
    get:
      tags:
      - "FeeReference"
      summary: "Retrieve a specific FeeReference record."
      operationId: "getFeeReferenceById"
      responses:
        "200":
          description: "The requested FeeReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeeReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FeeReference"
      summary: "Replace a FeeReference record."
      operationId: "replaceFeeReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeeReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeeReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FeeReference"
      summary: "Delete a FeeReference Record"
      description: "Marks the FeeReference record as inactive or removes it entirely."
      operationId: "deleteFeeReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FeeReference"
      summary: "Partially update a FeeReference record."
      operationId: "updateFeeReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeeReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeeReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "feeReferenceKey"
      in: "path"
      description: "The unique identifier for the specific FeeReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by partOrderKey."
      operationId: "listIdentifiersByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or removes it entirely."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/part-references:
    get:
      tags:
      - "PartReference"
      summary: "Retrieve a list of PartReference records scoped by partOrderKey."
      operationId: "listPartReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartReference"
      summary: "Create a new PartReference record."
      operationId: "createPartReference"
      requestBody:
        description: "PartReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReferenceCreate"
        required: true
      responses:
        "201":
          description: "PartReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/part-references/{partReferenceKey}:
    get:
      tags:
      - "PartReference"
      summary: "Retrieve a specific PartReference record."
      operationId: "getPartReferenceById"
      responses:
        "200":
          description: "The requested PartReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartReference"
      summary: "Replace a PartReference record."
      operationId: "replacePartReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartReference"
      summary: "Delete a PartReference Record"
      description: "Marks the PartReference record as inactive or removes it entirely."
      operationId: "deletePartReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartReference"
      summary: "Partially update a PartReference record."
      operationId: "updatePartReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PartReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/effective-periods:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a list of EffectivePeriod records scoped by partOrderKey."
      operationId: "listEffectivePeriodsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of EffectivePeriod records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/EffectivePeriod"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/effective-periods/{effectivePeriodKey}:
    get:
      tags:
      - "EffectivePeriod"
      summary: "Retrieve a specific EffectivePeriod record."
      operationId: "getEffectivePeriodById"
      responses:
        "200":
          description: "The requested EffectivePeriod record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "EffectivePeriod"
      summary: "Replace a EffectivePeriod record."
      operationId: "replaceEffectivePeriod"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EffectivePeriod"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "effectivePeriodKey"
      in: "path"
      description: "The unique identifier for the specific EffectivePeriod."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/part-order-items:
    get:
      tags:
      - "PartOrderItem"
      summary: "Retrieve a list of PartOrderItem records scoped by partOrderKey."
      operationId: "listPartOrderItemsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PartOrderItem records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PartOrderItem"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PartOrderItem"
      summary: "Create a new PartOrderItem record."
      operationId: "createPartOrderItem"
      requestBody:
        description: "PartOrderItem data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrderItemCreate"
        required: true
      responses:
        "201":
          description: "PartOrderItem created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrderItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/part-order-items/{partOrderItemKey}:
    get:
      tags:
      - "PartOrderItem"
      summary: "Retrieve a specific PartOrderItem record."
      operationId: "getPartOrderItemById"
      responses:
        "200":
          description: "The requested PartOrderItem record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrderItem"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PartOrderItem"
      summary: "Replace a PartOrderItem record."
      operationId: "replacePartOrderItem"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrderItem"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrderItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PartOrderItem"
      summary: "Delete a PartOrderItem Record"
      description: "Marks the PartOrderItem record as inactive or removes it entirely."
      operationId: "deletePartOrderItemRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PartOrderItem"
      summary: "Partially update a PartOrderItem record."
      operationId: "updatePartOrderItem"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PartOrderItemUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PartOrderItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "partOrderItemKey"
      in: "path"
      description: "The unique identifier for the specific PartOrderItem."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/credit-references:
    get:
      tags:
      - "CreditReference"
      summary: "Retrieve a list of CreditReference records scoped by partOrderKey."
      operationId: "listCreditReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CreditReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CreditReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CreditReference"
      summary: "Create a new CreditReference record."
      operationId: "createCreditReference"
      requestBody:
        description: "CreditReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditReferenceCreate"
        required: true
      responses:
        "201":
          description: "CreditReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/credit-references/{creditReferenceKey}:
    get:
      tags:
      - "CreditReference"
      summary: "Retrieve a specific CreditReference record."
      operationId: "getCreditReferenceById"
      responses:
        "200":
          description: "The requested CreditReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CreditReference"
      summary: "Replace a CreditReference record."
      operationId: "replaceCreditReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CreditReference"
      summary: "Delete a CreditReference Record"
      description: "Marks the CreditReference record as inactive or removes it entirely."
      operationId: "deleteCreditReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CreditReference"
      summary: "Partially update a CreditReference record."
      operationId: "updateCreditReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreditReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "creditReferenceKey"
      in: "path"
      description: "The unique identifier for the specific CreditReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/sales-profile-references:
    get:
      tags:
      - "SalesProfileReference"
      summary: "Retrieve a list of SalesProfileReference records scoped by partOrderKey."
      operationId: "listSalesProfileReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of SalesProfileReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/SalesProfileReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "SalesProfileReference"
      summary: "Create a new SalesProfileReference record."
      operationId: "createSalesProfileReference"
      requestBody:
        description: "SalesProfileReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesProfileReferenceCreate"
        required: true
      responses:
        "201":
          description: "SalesProfileReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesProfileReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/sales-profile-references/{salesProfileReferenceKey}:
    get:
      tags:
      - "SalesProfileReference"
      summary: "Retrieve a specific SalesProfileReference record."
      operationId: "getSalesProfileReferenceById"
      responses:
        "200":
          description: "The requested SalesProfileReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesProfileReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "SalesProfileReference"
      summary: "Replace a SalesProfileReference record."
      operationId: "replaceSalesProfileReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesProfileReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesProfileReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "SalesProfileReference"
      summary: "Delete a SalesProfileReference Record"
      description: "Marks the SalesProfileReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteSalesProfileReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "SalesProfileReference"
      summary: "Partially update a SalesProfileReference record."
      operationId: "updateSalesProfileReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesProfileReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesProfileReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "salesProfileReferenceKey"
      in: "path"
      description: "The unique identifier for the specific SalesProfileReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/rebate-references:
    get:
      tags:
      - "RebateReference"
      summary: "Retrieve a list of RebateReference records scoped by partOrderKey."
      operationId: "listRebateReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of RebateReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/RebateReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "RebateReference"
      summary: "Create a new RebateReference record."
      operationId: "createRebateReference"
      requestBody:
        description: "RebateReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RebateReferenceCreate"
        required: true
      responses:
        "201":
          description: "RebateReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RebateReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/rebate-references/{rebateReferenceKey}:
    get:
      tags:
      - "RebateReference"
      summary: "Retrieve a specific RebateReference record."
      operationId: "getRebateReferenceById"
      responses:
        "200":
          description: "The requested RebateReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RebateReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "RebateReference"
      summary: "Replace a RebateReference record."
      operationId: "replaceRebateReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RebateReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RebateReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "RebateReference"
      summary: "Delete a RebateReference Record"
      description: "Marks the RebateReference record as inactive or removes it entirely."
      operationId: "deleteRebateReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "RebateReference"
      summary: "Partially update a RebateReference record."
      operationId: "updateRebateReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RebateReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RebateReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "rebateReferenceKey"
      in: "path"
      description: "The unique identifier for the specific RebateReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/shipment-references:
    get:
      tags:
      - "ShipmentReference"
      summary: "Retrieve a list of ShipmentReference records scoped by partOrderKey."
      operationId: "listShipmentReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ShipmentReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ShipmentReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ShipmentReference"
      summary: "Create a new ShipmentReference record."
      operationId: "createShipmentReference"
      requestBody:
        description: "ShipmentReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceCreate"
        required: true
      responses:
        "201":
          description: "ShipmentReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/shipment-references/{shipmentReferenceKey}:
    get:
      tags:
      - "ShipmentReference"
      summary: "Retrieve a specific ShipmentReference record."
      operationId: "getShipmentReferenceById"
      responses:
        "200":
          description: "The requested ShipmentReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ShipmentReference"
      summary: "Replace a ShipmentReference record."
      operationId: "replaceShipmentReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ShipmentReference"
      summary: "Delete a ShipmentReference Record"
      description: "Marks the ShipmentReference record as inactive or removes it entirely."
      operationId: "deleteShipmentReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ShipmentReference"
      summary: "Partially update a ShipmentReference record."
      operationId: "updateShipmentReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ShipmentReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShipmentReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "shipmentReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ShipmentReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/unit-of-measures:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a list of UnitOfMeasure records scoped by partOrderKey."
      operationId: "listUnitOfMeasuresByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of UnitOfMeasure records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/UnitOfMeasure"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/unit-of-measures/{unitOfMeasureKey}:
    get:
      tags:
      - "UnitOfMeasure"
      summary: "Retrieve a specific UnitOfMeasure record."
      operationId: "getUnitOfMeasureById"
      responses:
        "200":
          description: "The requested UnitOfMeasure record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "UnitOfMeasure"
      summary: "Replace a UnitOfMeasure record."
      operationId: "replaceUnitOfMeasure"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnitOfMeasure"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitOfMeasure"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "unitOfMeasureKey"
      in: "path"
      description: "The unique identifier for the specific UnitOfMeasure."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/tax-splits:
    get:
      tags:
      - "TaxSplit"
      summary: "Retrieve a list of TaxSplit records scoped by partOrderKey."
      operationId: "listTaxSplitsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of TaxSplit records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/TaxSplit"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "TaxSplit"
      summary: "Create a new TaxSplit record."
      operationId: "createTaxSplit"
      requestBody:
        description: "TaxSplit data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaxSplitCreate"
        required: true
      responses:
        "201":
          description: "TaxSplit created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/tax-splits/{taxSplitKey}:
    get:
      tags:
      - "TaxSplit"
      summary: "Retrieve a specific TaxSplit record."
      operationId: "getTaxSplitById"
      responses:
        "200":
          description: "The requested TaxSplit record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxSplit"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "TaxSplit"
      summary: "Replace a TaxSplit record."
      operationId: "replaceTaxSplit"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaxSplit"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "TaxSplit"
      summary: "Delete a TaxSplit Record"
      description: "Marks the TaxSplit record as inactive or removes it entirely."
      operationId: "deleteTaxSplitRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "TaxSplit"
      summary: "Partially update a TaxSplit record."
      operationId: "updateTaxSplit"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TaxSplitUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaxSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "taxSplitKey"
      in: "path"
      description: "The unique identifier for the specific TaxSplit."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/payment-schedule-references:
    get:
      tags:
      - "PaymentScheduleReference"
      summary: "Retrieve a list of PaymentScheduleReference records scoped by partOrderKey."
      operationId: "listPaymentScheduleReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PaymentScheduleReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PaymentScheduleReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PaymentScheduleReference"
      summary: "Create a new PaymentScheduleReference record."
      operationId: "createPaymentScheduleReference"
      requestBody:
        description: "PaymentScheduleReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentScheduleReferenceCreate"
        required: true
      responses:
        "201":
          description: "PaymentScheduleReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/payment-schedule-references/{paymentScheduleReferenceKey}:
    get:
      tags:
      - "PaymentScheduleReference"
      summary: "Retrieve a specific PaymentScheduleReference record."
      operationId: "getPaymentScheduleReferenceById"
      responses:
        "200":
          description: "The requested PaymentScheduleReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentScheduleReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PaymentScheduleReference"
      summary: "Replace a PaymentScheduleReference record."
      operationId: "replacePaymentScheduleReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentScheduleReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PaymentScheduleReference"
      summary: "Delete a PaymentScheduleReference Record"
      description: "Marks the PaymentScheduleReference record as inactive or removes\
        \ it entirely."
      operationId: "deletePaymentScheduleReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PaymentScheduleReference"
      summary: "Partially update a PaymentScheduleReference record."
      operationId: "updatePaymentScheduleReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentScheduleReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentScheduleReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "paymentScheduleReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PaymentScheduleReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/price-plan-references:
    get:
      tags:
      - "PricePlanReference"
      summary: "Retrieve a list of PricePlanReference records scoped by partOrderKey."
      operationId: "listPricePlanReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of PricePlanReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/PricePlanReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "PricePlanReference"
      summary: "Create a new PricePlanReference record."
      operationId: "createPricePlanReference"
      requestBody:
        description: "PricePlanReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlanReferenceCreate"
        required: true
      responses:
        "201":
          description: "PricePlanReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlanReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/price-plan-references/{pricePlanReferenceKey}:
    get:
      tags:
      - "PricePlanReference"
      summary: "Retrieve a specific PricePlanReference record."
      operationId: "getPricePlanReferenceById"
      responses:
        "200":
          description: "The requested PricePlanReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlanReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "PricePlanReference"
      summary: "Replace a PricePlanReference record."
      operationId: "replacePricePlanReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlanReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlanReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "PricePlanReference"
      summary: "Delete a PricePlanReference Record"
      description: "Marks the PricePlanReference record as inactive or removes it\
        \ entirely."
      operationId: "deletePricePlanReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "PricePlanReference"
      summary: "Partially update a PricePlanReference record."
      operationId: "updatePricePlanReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PricePlanReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricePlanReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "pricePlanReferenceKey"
      in: "path"
      description: "The unique identifier for the specific PricePlanReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/control-account-references:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a list of ControlAccountReference records scoped by partOrderKey."
      operationId: "listControlAccountReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of ControlAccountReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ControlAccountReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "ControlAccountReference"
      summary: "Create a new ControlAccountReference record."
      operationId: "createControlAccountReference"
      requestBody:
        description: "ControlAccountReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceCreate"
        required: true
      responses:
        "201":
          description: "ControlAccountReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/control-account-references/{controlAccountReferenceKey}:
    get:
      tags:
      - "ControlAccountReference"
      summary: "Retrieve a specific ControlAccountReference record."
      operationId: "getControlAccountReferenceById"
      responses:
        "200":
          description: "The requested ControlAccountReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "ControlAccountReference"
      summary: "Replace a ControlAccountReference record."
      operationId: "replaceControlAccountReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "ControlAccountReference"
      summary: "Delete a ControlAccountReference Record"
      description: "Marks the ControlAccountReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteControlAccountReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "ControlAccountReference"
      summary: "Partially update a ControlAccountReference record."
      operationId: "updateControlAccountReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ControlAccountReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlAccountReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "controlAccountReferenceKey"
      in: "path"
      description: "The unique identifier for the specific ControlAccountReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/reward-references:
    get:
      tags:
      - "RewardReference"
      summary: "Retrieve a list of RewardReference records scoped by partOrderKey."
      operationId: "listRewardReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of RewardReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/RewardReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "RewardReference"
      summary: "Create a new RewardReference record."
      operationId: "createRewardReference"
      requestBody:
        description: "RewardReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RewardReferenceCreate"
        required: true
      responses:
        "201":
          description: "RewardReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/reward-references/{rewardReferenceKey}:
    get:
      tags:
      - "RewardReference"
      summary: "Retrieve a specific RewardReference record."
      operationId: "getRewardReferenceById"
      responses:
        "200":
          description: "The requested RewardReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "RewardReference"
      summary: "Replace a RewardReference record."
      operationId: "replaceRewardReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RewardReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "RewardReference"
      summary: "Delete a RewardReference Record"
      description: "Marks the RewardReference record as inactive or removes it entirely."
      operationId: "deleteRewardReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "RewardReference"
      summary: "Partially update a RewardReference record."
      operationId: "updateRewardReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RewardReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "rewardReferenceKey"
      in: "path"
      description: "The unique identifier for the specific RewardReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/customer-order-references:
    get:
      tags:
      - "CustomerOrderReference"
      summary: "Retrieve a list of CustomerOrderReference records scoped by partOrderKey."
      operationId: "listCustomerOrderReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of CustomerOrderReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/CustomerOrderReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "CustomerOrderReference"
      summary: "Create a new CustomerOrderReference record."
      operationId: "createCustomerOrderReference"
      requestBody:
        description: "CustomerOrderReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerOrderReferenceCreate"
        required: true
      responses:
        "201":
          description: "CustomerOrderReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/customer-order-references/{customerOrderReferenceKey}:
    get:
      tags:
      - "CustomerOrderReference"
      summary: "Retrieve a specific CustomerOrderReference record."
      operationId: "getCustomerOrderReferenceById"
      responses:
        "200":
          description: "The requested CustomerOrderReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerOrderReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "CustomerOrderReference"
      summary: "Replace a CustomerOrderReference record."
      operationId: "replaceCustomerOrderReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerOrderReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "CustomerOrderReference"
      summary: "Delete a CustomerOrderReference Record"
      description: "Marks the CustomerOrderReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteCustomerOrderReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "CustomerOrderReference"
      summary: "Partially update a CustomerOrderReference record."
      operationId: "updateCustomerOrderReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerOrderReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerOrderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "customerOrderReferenceKey"
      in: "path"
      description: "The unique identifier for the specific CustomerOrderReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/prices:
    get:
      tags:
      - "Price"
      summary: "Retrieve a list of Price records scoped by partOrderKey."
      operationId: "listPricesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Price records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Price"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Price"
      summary: "Create a new Price record."
      operationId: "createPrice"
      requestBody:
        description: "Price data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceCreate"
        required: true
      responses:
        "201":
          description: "Price created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/prices/{priceKey}:
    get:
      tags:
      - "Price"
      summary: "Retrieve a specific Price record."
      operationId: "getPriceById"
      responses:
        "200":
          description: "The requested Price record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Price"
      summary: "Replace a Price record."
      operationId: "replacePrice"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Price"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Price"
      summary: "Delete a Price Record"
      description: "Marks the Price record as inactive or removes it entirely."
      operationId: "deletePriceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Price"
      summary: "Partially update a Price record."
      operationId: "updatePrice"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PriceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Price"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "priceKey"
      in: "path"
      description: "The unique identifier for the specific Price."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/financial-events:
    get:
      tags:
      - "FinancialEvent"
      summary: "Retrieve a list of FinancialEvent records scoped by partOrderKey."
      operationId: "listFinancialEventsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialEvent records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialEvent"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialEvent"
      summary: "Create a new FinancialEvent record."
      operationId: "createFinancialEvent"
      requestBody:
        description: "FinancialEvent data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialEventCreate"
        required: true
      responses:
        "201":
          description: "FinancialEvent created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/financial-events/{financialEventKey}:
    get:
      tags:
      - "FinancialEvent"
      summary: "Retrieve a specific FinancialEvent record."
      operationId: "getFinancialEventById"
      responses:
        "200":
          description: "The requested FinancialEvent record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialEvent"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialEvent"
      summary: "Replace a FinancialEvent record."
      operationId: "replaceFinancialEvent"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialEvent"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialEvent"
      summary: "Delete a FinancialEvent Record"
      description: "Marks the FinancialEvent record as inactive or removes it entirely."
      operationId: "deleteFinancialEventRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialEvent"
      summary: "Partially update a FinancialEvent record."
      operationId: "updateFinancialEvent"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialEventUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialEvent"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialEventKey"
      in: "path"
      description: "The unique identifier for the specific FinancialEvent."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/textual-details:
    get:
      tags:
      - "TextualDetail"
      summary: "Retrieve a list of TextualDetail records scoped by partOrderKey."
      operationId: "listTextualDetailsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of TextualDetail records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/TextualDetail"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "TextualDetail"
      summary: "Create a new TextualDetail record."
      operationId: "createTextualDetail"
      requestBody:
        description: "TextualDetail data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetailCreate"
        required: true
      responses:
        "201":
          description: "TextualDetail created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/textual-details/{textualDetailKey}:
    get:
      tags:
      - "TextualDetail"
      summary: "Retrieve a specific TextualDetail record."
      operationId: "getTextualDetailById"
      responses:
        "200":
          description: "The requested TextualDetail record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "TextualDetail"
      summary: "Replace a TextualDetail record."
      operationId: "replaceTextualDetail"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetail"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "TextualDetail"
      summary: "Delete a TextualDetail Record"
      description: "Marks the TextualDetail record as inactive or removes it entirely."
      operationId: "deleteTextualDetailRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "TextualDetail"
      summary: "Partially update a TextualDetail record."
      operationId: "updateTextualDetail"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TextualDetailUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextualDetail"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "textualDetailKey"
      in: "path"
      description: "The unique identifier for the specific TextualDetail."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/lender-references:
    get:
      tags:
      - "LenderReference"
      summary: "Retrieve a list of LenderReference records scoped by partOrderKey."
      operationId: "listLenderReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of LenderReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/LenderReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "LenderReference"
      summary: "Create a new LenderReference record."
      operationId: "createLenderReference"
      requestBody:
        description: "LenderReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LenderReferenceCreate"
        required: true
      responses:
        "201":
          description: "LenderReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LenderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/lender-references/{lenderReferenceKey}:
    get:
      tags:
      - "LenderReference"
      summary: "Retrieve a specific LenderReference record."
      operationId: "getLenderReferenceById"
      responses:
        "200":
          description: "The requested LenderReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LenderReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "LenderReference"
      summary: "Replace a LenderReference record."
      operationId: "replaceLenderReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LenderReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LenderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "LenderReference"
      summary: "Delete a LenderReference Record"
      description: "Marks the LenderReference record as inactive or removes it entirely."
      operationId: "deleteLenderReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "LenderReference"
      summary: "Partially update a LenderReference record."
      operationId: "updateLenderReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LenderReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LenderReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "lenderReferenceKey"
      in: "path"
      description: "The unique identifier for the specific LenderReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/financial-track-references:
    get:
      tags:
      - "FinancialTrackReference"
      summary: "Retrieve a list of FinancialTrackReference records scoped by partOrderKey."
      operationId: "listFinancialTrackReferencesByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialTrackReference records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialTrackReference"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialTrackReference"
      summary: "Create a new FinancialTrackReference record."
      operationId: "createFinancialTrackReference"
      requestBody:
        description: "FinancialTrackReference data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceCreate"
        required: true
      responses:
        "201":
          description: "FinancialTrackReference created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/financial-track-references/{financialTrackReferenceKey}:
    get:
      tags:
      - "FinancialTrackReference"
      summary: "Retrieve a specific FinancialTrackReference record."
      operationId: "getFinancialTrackReferenceById"
      responses:
        "200":
          description: "The requested FinancialTrackReference record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialTrackReference"
      summary: "Replace a FinancialTrackReference record."
      operationId: "replaceFinancialTrackReference"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReference"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialTrackReference"
      summary: "Delete a FinancialTrackReference Record"
      description: "Marks the FinancialTrackReference record as inactive or removes\
        \ it entirely."
      operationId: "deleteFinancialTrackReferenceRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialTrackReference"
      summary: "Partially update a FinancialTrackReference record."
      operationId: "updateFinancialTrackReference"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialTrackReferenceUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialTrackReference"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialTrackReferenceKey"
      in: "path"
      description: "The unique identifier for the specific FinancialTrackReference."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /part-orders/{partOrderKey}/financial-splits:
    get:
      tags:
      - "FinancialSplit"
      summary: "Retrieve a list of FinancialSplit records scoped by partOrderKey."
      operationId: "listFinancialSplitsByPartOrderKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of FinancialSplit records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/FinancialSplit"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "FinancialSplit"
      summary: "Create a new FinancialSplit record."
      operationId: "createFinancialSplit"
      requestBody:
        description: "FinancialSplit data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplitCreate"
        required: true
      responses:
        "201":
          description: "FinancialSplit created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /part-orders/{partOrderKey}/financial-splits/{financialSplitKey}:
    get:
      tags:
      - "FinancialSplit"
      summary: "Retrieve a specific FinancialSplit record."
      operationId: "getFinancialSplitById"
      responses:
        "200":
          description: "The requested FinancialSplit record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplit"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "FinancialSplit"
      summary: "Replace a FinancialSplit record."
      operationId: "replaceFinancialSplit"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplit"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "FinancialSplit"
      summary: "Delete a FinancialSplit Record"
      description: "Marks the FinancialSplit record as inactive or removes it entirely."
      operationId: "deleteFinancialSplitRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "FinancialSplit"
      summary: "Partially update a FinancialSplit record."
      operationId: "updateFinancialSplit"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinancialSplitUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the updated record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancialSplit"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "partOrderKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "financialSplitKey"
      in: "path"
      description: "The unique identifier for the specific FinancialSplit."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas: {}
