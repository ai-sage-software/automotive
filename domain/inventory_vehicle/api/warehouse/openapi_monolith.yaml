---
openapi: "3.0.1"
info:
  title: "Domain API Specification"
  description: "Automotive Retail Ontology."
  version: "1.0.0"
paths:
  /warehouses:
    get:
      tags:
      - "Warehouse"
      summary: "Retrieve a list of all Warehouse records."
      operationId: "listWarehouses"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Warehouse records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Warehouse"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Warehouse"
      summary: "Create a new Warehouse record."
      operationId: "createWarehouse"
      requestBody:
        description: "Warehouse data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseCreate"
        required: true
      responses:
        "201":
          description: "Warehouse created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/warehouses/PR-1234567"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Warehouse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /warehouses/{warehouseKey}:
    get:
      tags:
      - "Warehouse"
      summary: "Retrieve a specific Warehouse record."
      operationId: "getWarehouseById"
      responses:
        "200":
          description: "The requested Warehouse record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Warehouse"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Warehouse"
      summary: "Replace a Warehouse record."
      operationId: "replaceWarehouse"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Warehouse"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Warehouse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Warehouse"
      summary: "Delete a Warehouse Record"
      description: "Marks the Warehouse record as inactive or remov..."
      operationId: "deleteWarehouseRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Warehouse"
      summary: "Partially update a Warehouse record."
      operationId: "updateWarehouse"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Warehouse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the specific Warehouse."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /warehouses/{warehouseKey}/authorizations:
    get:
      tags:
      - "Authorization"
      summary: "Retrieve a list of Authorization records scoped..."
      operationId: "listAuthorizationsByWarehouseKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Authorization records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Authorization"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Authorization"
      summary: "Create a new Authorization record."
      operationId: "createAuthorization"
      requestBody:
        description: "Authorization data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthorizationCreate"
        required: true
      responses:
        "201":
          description: "Authorization created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /warehouses/{warehouseKey}/authorizations/{authorizationKey}:
    get:
      tags:
      - "Authorization"
      summary: "Retrieve a specific Authorization record."
      operationId: "getAuthorizationById"
      responses:
        "200":
          description: "The requested Authorization record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Authorization"
      summary: "Replace a Authorization record."
      operationId: "replaceAuthorization"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Authorization"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Authorization"
      summary: "Delete a Authorization Record"
      description: "Marks the Authorization record as inactive or r..."
      operationId: "deleteAuthorizationRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Authorization"
      summary: "Partially update a Authorization record."
      operationId: "updateAuthorization"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthorizationUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Authorization"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "authorizationKey"
      in: "path"
      description: "The unique identifier for the specific Authorization."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /warehouses/{warehouseKey}/identifiers:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a list of Identifier records scoped by..."
      operationId: "listIdentifiersByWarehouseKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Identifier records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Identifier"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Identifier"
      summary: "Create a new Identifier record."
      operationId: "createIdentifier"
      requestBody:
        description: "Identifier data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: "Identifier created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /warehouses/{warehouseKey}/identifiers/{identifierKey}:
    get:
      tags:
      - "Identifier"
      summary: "Retrieve a specific Identifier record."
      operationId: "getIdentifierById"
      responses:
        "200":
          description: "The requested Identifier record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Identifier"
      summary: "Replace a Identifier record."
      operationId: "replaceIdentifier"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Identifier"
      summary: "Delete a Identifier Record"
      description: "Marks the Identifier record as inactive or remo..."
      operationId: "deleteIdentifierRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Identifier"
      summary: "Partially update a Identifier record."
      operationId: "updateIdentifier"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "identifierKey"
      in: "path"
      description: "The unique identifier for the specific Identifier."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /warehouses/{warehouseKey}/addresses:
    get:
      tags:
      - "Address"
      summary: "Retrieve a list of Address records scoped by wa..."
      operationId: "listAddresssByWarehouseKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Address records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Address"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Address"
      summary: "Create a new Address record."
      operationId: "createAddress"
      requestBody:
        description: "Address data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressCreate"
        required: true
      responses:
        "201":
          description: "Address created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /warehouses/{warehouseKey}/addresses/{addressKey}:
    get:
      tags:
      - "Address"
      summary: "Retrieve a specific Address record."
      operationId: "getAddressById"
      responses:
        "200":
          description: "The requested Address record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Address"
      summary: "Replace a Address record."
      operationId: "replaceAddress"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Address"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Address"
      summary: "Delete a Address Record"
      description: "Marks the Address record as inactive or removes..."
      operationId: "deleteAddressRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Address"
      summary: "Partially update a Address record."
      operationId: "updateAddress"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Address"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "addressKey"
      in: "path"
      description: "The unique identifier for the specific Address."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /warehouses/{warehouseKey}/positions:
    get:
      tags:
      - "Position"
      summary: "Retrieve a list of Position records scoped by w..."
      operationId: "listPositionsByWarehouseKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of Position records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Position"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "Position"
      summary: "Create a new Position record."
      operationId: "createPosition"
      requestBody:
        description: "Position data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionCreate"
        required: true
      responses:
        "201":
          description: "Position created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /warehouses/{warehouseKey}/positions/{positionKey}:
    get:
      tags:
      - "Position"
      summary: "Retrieve a specific Position record."
      operationId: "getPositionById"
      responses:
        "200":
          description: "The requested Position record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "Position"
      summary: "Replace a Position record."
      operationId: "replacePosition"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Position"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "Position"
      summary: "Delete a Position Record"
      description: "Marks the Position record as inactive or remove..."
      operationId: "deletePositionRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "Position"
      summary: "Partially update a Position record."
      operationId: "updatePosition"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PositionUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Position"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "positionKey"
      in: "path"
      description: "The unique identifier for the specific Position."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
  /warehouses/{warehouseKey}/address-locales:
    get:
      tags:
      - "AddressLocale"
      summary: "Retrieve a list of AddressLocale records scoped..."
      operationId: "listAddressLocalesByWarehouseKey"
      parameters:
      - name: "limit"
        in: "query"
        description: "Maximum number of records to return."
        schema:
          maximum: 100
          minimum: 1
          type: "integer"
          default: 50
      - name: "cursor"
        in: "query"
        description: "starting point for next set of results"
        schema:
          type: "string"
      responses:
        "200":
          description: "A list of AddressLocale records."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/AddressLocale"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - "AddressLocale"
      summary: "Create a new AddressLocale record."
      operationId: "createAddressLocale"
      requestBody:
        description: "AddressLocale data to create the new resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressLocaleCreate"
        required: true
      responses:
        "201":
          description: "AddressLocale created successfully."
          headers:
            Location:
              description: "The URI of the newly created resource."
              schema:
                type: "string"
                example: "/part-search-results/PS-12345/bill-of-materials/BOM-001"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "409":
          $ref: "#/components/responses/ConflictError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
  /warehouses/{warehouseKey}/address-locales/{addressLocaleKey}:
    get:
      tags:
      - "AddressLocale"
      summary: "Retrieve a specific AddressLocale record."
      operationId: "getAddressLocaleById"
      responses:
        "200":
          description: "The requested AddressLocale record."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - "AddressLocale"
      summary: "Replace a AddressLocale record."
      operationId: "replaceAddressLocale"
      requestBody:
        description: "Full record data to replace the existing resource."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressLocale"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - "AddressLocale"
      summary: "Delete a AddressLocale Record"
      description: "Marks the AddressLocale record as inactive or r..."
      operationId: "deleteAddressLocaleRecord"
      responses:
        "204":
          description: "Record successfully deleted (or deactivated)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - "AddressLocale"
      summary: "Partially update a AddressLocale record."
      operationId: "updateAddressLocale"
      requestBody:
        description: "Included Fields to be updated will be modified."
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddressLocaleUpdate"
        required: true
      responses:
        "200":
          description: "Record updated successfully. Returns the update..."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddressLocale"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: "warehouseKey"
      in: "path"
      description: "The unique identifier for the parent resource."
      required: true
      schema:
        type: "string"
    - name: "addressLocaleKey"
      in: "path"
      description: "The unique identifier for the specific AddressLocale."
      required: true
      schema:
        type: "string"
        example: "PR-1234567"
components:
  schemas:
    Address:
      type: "object"
      properties:
        address_locales:
          $ref: "#/components/schemas/AddressLocales"
        priority:
          maxLength: 10
          minLength: 1
          type: "string"
          description: "Priority indicator for address usage (e...."
        city_code:
          maxLength: 20
          minLength: 1
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized city code. Pattern: ^[a-zA-Z0-9_]+..."
        address_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique business key for the address. Pattern: ^..."
        address_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier for the address record... Pat..."
        address_input:
          maxLength: 255
          minLength: 1
          type: "string"
          description: "Raw input provided for address processin..."
        address_line1:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Primary address line (Street, PO Box, et..."
        address_line2:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line3:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_line4:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_line5:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/AddressTypes"
        authorizations:
          $ref: "#/components/schemas/Authorizations"
        city:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Name of the city."
        city_subdivision:
          maxLength: 100
          type: "string"
          description: "Subdivision within the city (e.g., neigh..."
        country:
          maxLength: 100
          minLength: 2
          type: "string"
          description: "Full name of the country."
        country_code:
          maxLength: 2
          minLength: 2
          pattern: "^[A-Z]{2}$"
          type: "string"
          description: "ISO 3166-1 Alpha-2 country code. Pattern: ^[A-Z..."
        county:
          maxLength: 100
          type: "string"
          description: "Name of the county."
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
        label:
          maxLength: 50
          type: "string"
          description: "Label or display name for the address."
        position:
          $ref: "#/components/schemas/Position"
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: "string"
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
        postal_code_ext:
          maxLength: 10
          type: "string"
          description: "Extended postal code (e.g., ZIP+4)."
        preferred_indicator:
          type: "boolean"
          description: "Flag indicating if this address is the p..."
        region:
          maxLength: 50
          type: "string"
          description: "Administrative region (e.g., province, s..."
        state:
          maxLength: 50
          type: "string"
          description: "State or equivalent subdivision."
        locale:
          maxLength: 10
          minLength: 2
          pattern: "^[a-z]{2}(-[A-Z]{2})?$"
          type: "string"
          description: "Locale code associated with this address... Pat..."
      description: "Address represents a physical or mailing locati..."
    AddressCreate:
      required:
      - "address_key"
      type: "object"
      description: "Address Create for Post"
      allOf:
      - $ref: "#/components/schemas/Address"
    AddressLocale:
      type: "object"
      properties:
        address_locale_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key for the address locale. Pattern: ^..."
        address_locale_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier for the address locale... Pat..."
        region_code:
          maxLength: 20
          type: "string"
          description: "Code representing the region for the loc..."
        region_name:
          maxLength: 100
          type: "string"
          description: "Name of the region for the locale."
        region_aliases:
          $ref: "#/components/schemas/Codes"
        location_type:
          $ref: "#/components/schemas/LocationTypes"
      description: "External system address locale including region..."
    AddressLocaleCreate:
      required:
      - "address_locale_key"
      type: "object"
      description: "AddressLocale Create for Post"
      allOf:
      - $ref: "#/components/schemas/AddressLocale"
    AddressLocaleUpdate:
      type: "object"
      properties:
        region_name:
          maxLength: 100
          type: "string"
          description: "Name of the region for the locale."
        region_aliases:
          $ref: "#/components/schemas/Codes"
        location_type:
          $ref: "#/components/schemas/LocationTypes"
        region_code:
          maxLength: 20
          type: "string"
          description: "Code representing the region for the loc..."
      description: "AddressLocale Update for Patch and Put"
    AddressLocales:
      type: "array"
      items:
        $ref: "#/components/schemas/AddressLocale"
    AddressTypes:
      type: "string"
      description: "types of address."
      enum:
      - "BILLING"
      - "SHIPPING"
      - "MAILING"
      - "POSTAL"
      - "HOME"
      - "RESIDENCE"
      - "WORK"
      - "BUSINESS"
      - "LEGAL"
      - "SERVICE"
      - "DEALER"
      - "CONTRACT"
      - "AR_ADDRESS"
      - "RECIPIENT"
      - "DELIVERY"
      - "DESTINATION"
      - "ORIGINAL"
      - "CURRENT"
      - "PREVIOUS"
      - "TEMPORARY"
      - "VACATION"
      - "GARAGE"
      - "EMERGENCY"
      - "REBATE"
      - "SERVICE_CONTRACT"
      - "INSURANCE"
      - "PRODUCT"
      - "ARCHIVE"
      - "OTHER"
    AddressUpdate:
      type: "object"
      properties:
        address_input:
          maxLength: 255
          minLength: 1
          type: "string"
          description: "Raw input provided for address processin..."
        country:
          maxLength: 100
          minLength: 2
          type: "string"
          description: "Full name of the country."
        city:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Name of the city."
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
        authorizations:
          $ref: "#/components/schemas/Authorizations"
        county:
          maxLength: 100
          type: "string"
          description: "Name of the county."
        city_code:
          maxLength: 20
          minLength: 1
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized city code. Pattern: ^[a-zA-Z0-9_]+..."
        locale:
          maxLength: 10
          minLength: 2
          pattern: "^[a-z]{2}(-[A-Z]{2})?$"
          type: "string"
          description: "Locale code associated with this address... Pat..."
        address_locales:
          $ref: "#/components/schemas/AddressLocales"
        address_line5:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_line4:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_line3:
          maxLength: 100
          type: "string"
          description: "Additional address line information."
        address_line2:
          maxLength: 100
          type: "string"
          description: "Secondary address line (Suite, Apt, etc)..."
        address_line1:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Primary address line (Street, PO Box, et..."
        postal_code_ext:
          maxLength: 10
          type: "string"
          description: "Extended postal code (e.g., ZIP+4)."
        state:
          maxLength: 50
          type: "string"
          description: "State or equivalent subdivision."
        preferred_indicator:
          type: "boolean"
          description: "Flag indicating if this address is the p..."
        city_subdivision:
          maxLength: 100
          type: "string"
          description: "Subdivision within the city (e.g., neigh..."
        label:
          maxLength: 50
          type: "string"
          description: "Label or display name for the address."
        priority:
          maxLength: 10
          minLength: 1
          type: "string"
          description: "Priority indicator for address usage (e...."
        address_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/AddressTypes"
        country_code:
          maxLength: 2
          minLength: 2
          pattern: "^[A-Z]{2}$"
          type: "string"
          description: "ISO 3166-1 Alpha-2 country code. Pattern: ^[A-Z..."
        position:
          $ref: "#/components/schemas/Position"
        postal_code:
          maxLength: 20
          minLength: 3
          pattern: "^[0-9a-zA-Z\\-\\s]+$"
          type: "string"
          description: "Primary postal or ZIP code. Pattern: ^[0-9a-zA-..."
        region:
          maxLength: 50
          type: "string"
          description: "Administrative region (e.g., province, s..."
      description: "Address Update for Patch and Put"
    Addresses:
      type: "array"
      items:
        $ref: "#/components/schemas/Address"
    Authorization:
      type: "object"
      properties:
        update_date_time:
          type: "string"
          description: "Timestamp of the last update to this aut..."
          format: "date-time"
        authorized_indicator:
          type: "boolean"
          description: "Indicates whether the authorization is c..."
        authorization_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key uniquely identifying the au... Pat..."
      description: "Establish authorization level"
    AuthorizationCreate:
      required:
      - "authorization_key"
      type: "object"
      description: "Authorization Create for Post"
      allOf:
      - $ref: "#/components/schemas/Authorization"
    AuthorizationUpdate:
      type: "object"
      properties:
        authorized_indicator:
          type: "boolean"
          description: "Indicates whether the authorization is c..."
        update_date_time:
          type: "string"
          description: "Timestamp of the last update to this aut..."
          format: "date-time"
      description: "Authorization Update for Patch and Put"
    Authorizations:
      type: "array"
      items:
        $ref: "#/components/schemas/Authorization"
    Codes:
      type: "array"
      items:
        type: "string"
    ErrorDetail:
      required:
      - "code"
      - "message"
      - "timestamp"
      type: "object"
      properties:
        code:
          type: "string"
          description: "A unique, system-defined error code (e.g., 'AUT..."
          example: "NOT_FOUND_PR"
        message:
          type: "string"
          description: "A human-readable summary providing specific det..."
          example: "Personnel Record with ID PR-1234567 was not found."
        details:
          type: "array"
          description: "Optional array of specific field-level validati..."
          items:
            type: "object"
            properties:
              field:
                type: "string"
                description: "The specific field that failed validation (e.g., 'firstName')."
                example: "payrollId"
              issue:
                type: "string"
                description: "The validation message for the field (e.g., 'must be\
                  \ numeric')."
                example: "must be unique"
        timestamp:
          type: "string"
          description: "The time the error occurred, in ISO 8601 format."
          format: "date-time"
          example: "2025-11-10T10:30:00Z"
      description: "Standard format for all API error responses."
    Identifier:
      type: "object"
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier value Pattern: ^[a-zA-Z0-9_]+..."
        identifier_key:
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key for the identifier Pattern: ^[a-zA..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: "External system identifier"
    IdentifierCreate:
      required:
      - "identifier_key"
      type: "object"
      description: "Identifier Create for Post"
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: "object"
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: "string"
          description: "The monetary or hours-based value of the..."
        context:
          type: "string"
          description: "Context or domain of the identifier"
        detail:
          type: "string"
          description: "The monetary or hours-based value of the..."
        value:
          type: "string"
          description: "The monetary or hours-based value of the..."
      description: "Identifier Update for Patch and Put"
    Identifiers:
      type: "array"
      items:
        $ref: "#/components/schemas/Identifier"
    LocationTypes:
      type: "string"
      description: "types of locations."
      enum:
      - "BOROUGH"
      - "PARISH"
      - "REGION"
      - "BOUNDARY"
      - "PROVINCE"
      - "VILLAGE"
      - "STATE"
      - "CITY"
      - "COUNTRY"
      - "LOCALITY"
      - "POSTAL"
      - "STREET"
      - "SUB_ADMIN_AREA"
      - "SUB_LOCALITY"
      - "FEDERAL"
      - "COUNTY"
      - "TELEMATICS"
      - "ADDRESS"
      - "NO_PREFERENCE"
      - "GPS"
      - "WAREHOUSE"
      - "DISTRICT"
    Position:
      type: "object"
      properties:
        position_id:
          type: "string"
          description: "Unique identifier for this position"
          example: "pos-001"
        position_key:
          type: "string"
          description: "Business key for this position record"
          example: "POS-ATL-WEST"
        latitude:
          type: "string"
          description: "Latitude of the location"
          example: "33.7490"
        longitude:
          type: "string"
          description: "Longitude of the location"
          example: "-84.3880"
        altitude:
          type: "string"
          description: "Altitude of the location"
          example: "320"
        bearing:
          type: "string"
          description: "Bearing direction (compass)"
          example: "NW"
        postal_code:
          type: "string"
          description: "Postal code of the location"
          example: "30303"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            type: "string"
      description: "position meta-data that includes postal code, a..."
    PositionCreate:
      required:
      - "position_key"
      type: "object"
      description: "Position Create for Post"
      allOf:
      - $ref: "#/components/schemas/Position"
    PositionUpdate:
      type: "object"
      properties:
        altitude:
          type: "string"
          description: "Altitude of the location"
          example: "320"
        county_code:
          maxLength: 20
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Standardized code for the county. Pattern: ^[a-..."
        bearing:
          type: "string"
          description: "Bearing direction (compass)"
          example: "NW"
        latitude:
          type: "string"
          description: "Latitude of the location"
          example: "33.7490"
        postal_code:
          type: "string"
          description: "Postal code of the location"
          example: "30303"
        position_codes:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            type: "string"
        longitude:
          type: "string"
          description: "Longitude of the location"
          example: "-84.3880"
      description: "Position Update for Patch and Put"
    Warehouse:
      type: "object"
      properties:
        warehouse_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Unique identifier for the warehouse. Pattern: ^..."
        warehouse_key:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Business key used for external integrati... Pat..."
        warehouse_code:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Code of the warehouse."
        warehouse_name:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Descriptive name of the warehouse."
        party_id:
          maximum: 100
          minimum: 0
          maxLength: 50
          minLength: 5
          pattern: "^[a-zA-Z0-9_]+$"
          type: "string"
          description: "Identifier of the party (organization or... Pat..."
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        warehouse_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/WarehouseTypes"
        vehicle_identifiers:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            type: "string"
        addresses:
          $ref: "#/components/schemas/Addresses"
      description: "Warehouse represents a physical or virtual stor..."
    WarehouseCreate:
      required:
      - "warehouse_key"
      type: "object"
      description: "Warehouse Create for Post"
      allOf:
      - $ref: "#/components/schemas/Warehouse"
    WarehouseTypes:
      type: "string"
      description: "types of warehouses."
      enum:
      - "DEALERSHIP"
      - "OEM_WAREHOUSE"
      - "PARTS_SUPPLIER"
      - "REGIONAL_DISTRIBUTION_CENTER"
      - "NATIONAL_DISTRIBUTION_CENTER"
      - "RETURN_CORE_CENTER"
      - "IN_HOUSE_PARTS_STORAGE"
      - "DISTRIBUTOR"
      - "THIRD_PARTY_LOGISTICS"
      - "AFTERMARKET_SUPPLIER"
      - "BODY_SHOP_WAREHOUSE"
      - "TIRES_ONLY_DISTRIBUTION"
      - "ACCESSORY_WAREHOUSE"
      - "USED_PARTS_YARD"
      - "MOBILE_PARTS_UNIT"
      - "OTHER"
    WarehouseUpdate:
      type: "object"
      properties:
        addresses:
          $ref: "#/components/schemas/Addresses"
        warehouse_name:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Descriptive name of the warehouse."
        vehicle_identifiers:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            type: "string"
        warehouse_code:
          maxLength: 100
          minLength: 1
          type: "string"
          description: "Code of the warehouse."
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        warehouse_types:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: "array"
          items:
            $ref: "#/components/schemas/WarehouseTypes"
      description: "Warehouse Update for Patch and Put"
  responses:
    NotFoundError:
      description: "The requested resource was not found."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: "Authentication failed. The access token is miss..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ForbiddenError:
      description: "The authenticated user does not have the necess..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: "The request could not be completed because it c..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: "An unexpected error occurred on the server whil..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
