openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: "This API provides a standardized interface for Automotive Retail operations,\
    \ built upon a formal Retail Ontology. It enables seamless integration between\
    \ Master Catalog systems, Inventory Management, and Financial Invoicing workflows.\
    \ \n\n**Key Capabilities:**\n* **Catalog Management:** Unified definitions for\
    \ parts, assemblies, and BOMs.\n* **Inventory Orchestration:** Real-time visibility\
    \ into warehouse and dealership stock.\n* **Financial Workflows:** Automated invoicing\
    \ and batch processing for high-volume retail transactions.\n\nDesigned for high-reliability\
    \ CI/CD environments and asynchronous batch processing."
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
paths:
  /geographic-boundaries:
    get:
      tags:
      - GeographicBoundary
      summary: Retrieve a list of GeographicBoundary entities.
      operationId: listGeographicBoundary
      parameters:
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      responses:
        "200":
          description: A list of GeographicBoundary entities.
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/GeographicBoundary"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - GeographicBoundary
      summary: Create a new GeographicBoundary entity.
      operationId: createGeographicBoundary
      requestBody:
        description: GeographicBoundary data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeographicBoundaryCreate"
        required: true
      responses:
        "201":
          description: GeographicBoundary created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /geographic-boundaries/PR-1234567
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundary"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /geographic-boundaries/{geographicBoundaryKey}:
    get:
      tags:
      - GeographicBoundary
      summary: Retrieve a specific GeographicBoundary entity.
      operationId: getGeographicBoundaryById
      responses:
        "200":
          description: The requested GeographicBoundary entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundary"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
    put:
      tags:
      - GeographicBoundary
      summary: Replace a GeographicBoundary entity.
      operationId: replaceGeographicBoundary
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeographicBoundary"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeographicBoundary"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    delete:
      tags:
      - GeographicBoundary
      summary: Delete a GeographicBoundary entity
      description: Marks the GeographicBoundary entity as inactive or removes it entirely.
      operationId: deleteGeographicBoundaryEntity
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    patch:
      tags:
      - GeographicBoundary
      summary: Partially update a GeographicBoundary entity.
      operationId: updateGeographicBoundary
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/GeographicBoundaryUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the updated entity.
          content:
            application/merge-patch+json:
              schema:
                $ref: "#/components/schemas/GeographicBoundary"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "404":
          $ref: "#/components/responses/NotFoundError"
    parameters:
    - name: geographicBoundaryKey
      in: path
      description: The unique identifier for the specific GeographicBoundary.
      required: true
      schema:
        type: string
        example: PR-1234567
components:
  schemas: {}
