{
  "name": "automotive-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/generate-domain-pages.js && docusaurus start",
    "build": "node scripts/generate-domain-pages.js && docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy"
  },
  "dependencies": {
    "@docusaurus/core": "^3.6.0",
    "@docusaurus/preset-classic": "^3.6.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.1.1",
    "prism-react-renderer": "^2.4.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
