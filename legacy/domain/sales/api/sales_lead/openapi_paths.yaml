openapi: 3.0.3
info:
  title: Sales Lead API Specification
  description: Sales Process Domain Sales Lead API Specification
  termsOfService: http://swagger.io/terms/
  contact:
    name: Star Standards
    url: https://www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  version: "1.0"
servers:
- url: https://virtserver.swaggerhub.com/star-standard/sales-leads/1.0
  description: SwaggerHub API Auto Mocking
- url: "https://{host}/services/v1"
  variables:
    host:
      enum:
      - dev.appName.dmsDomain.com
      - test.appName.dmsDomain.com
      - qa.appName.dmsDomain.com
      - appName.dmsDomain.com
      default: appName.dmsDomain.com
paths:
  /sales-leads/{sales-lead-id}/applicants:
    summary: List Sales Lead  Applicant By Criteria with filtering support to narrow the results
    get:
      tags:
      - sales-leads applicants
      summary: List Sales Lead  Applicant By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead  Applicant based on the search criteria provided.  If no search criteria is provided, Sales Lead  Applicant are returned based on default sort order."
      operationId: getSalesLeadsApplicantsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllApplicantsResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads applicants
      summary: Create Sales Lead  Applicant
      description: Create Sales Lead  Applicant
      operationId: postSalesLeadsApplicantsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Applicant"
      responses:
        "201":
          description: Created the Applicant Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedApplicant"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/co-applicants:
    summary: List Sales Lead Co Applicant By Criteria with filtering support to narrow the results
    get:
      tags:
      - sales-leads co-applicants
      summary: List Sales Lead Co Applicant By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead Co Applicant based on the search criteria provided.  If no search criteria is provided, Sales Lead Co Applicant are returned based on default sort order."
      operationId: getSalesLeadsCoApplicantsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllCoApplicantsResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads co-applicants
      summary: Create Sales Lead Co Applicant
      description: Create Sales Lead Co Applicant
      operationId: postSalesLeadsCoApplicantsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Coapplicant"
      responses:
        "201":
          description: Created the Co Applicant Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedCoApplicant"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads:
    summary: Create Sales Lead
    get:
      tags:
      - sales-leads
      summary: List Sales Lead By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead based on the search criteria provided.  If no search criteria is provided, Sales Lead are returned based on default sort order."
      operationId: getSalesLeads
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllSalesLeadsResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads
      summary: Create Sales Lead
      description: Create Sales Lead
      operationId: postSalesLeads
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesLead"
      responses:
        "201":
          description: Created the Sales Lead Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedSalesLead"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/vehicle-preferences:
    summary: Create Sales Lead Vehicle Preferences
    get:
      tags:
      - sales-leads vehicle-preferences
      summary: List Sales Lead Vehicle Preferences By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead Vehicle Preferences based on the search criteria provided.  If no search criteria is provided, Sales Lead Vehicle Preferences are returned based on default sort order."
      operationId: getSalesLeadsVehiclePreferencesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllVehiclePreferencesResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads vehicle-preferences
      summary: Create Sales Lead Vehicle Preferences
      description: Create Sales Lead Vehicle Preferences
      operationId: postSalesLeadsVehiclePreferencesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VehiclePreference"
      responses:
        "201":
          description: Created the Preference Vehicle Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedVehiclePreference"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/lead-sources:
    summary: Create Sales Lead Sources
    get:
      tags:
      - sales-leads lead-sources
      summary: List Sales Lead Sources By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead Sources based on the search criteria provided.  If no search criteria is provided, Sales Lead Sources are returned based on default sort order."
      operationId: getSalesLeadsLeadSourcesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllLeadSourcesResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads lead-sources
      summary: Create Sales Lead Sources
      description: Create Sales Lead Sources
      operationId: postSalesLeadsLeadSourcesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LeadSource"
      responses:
        "201":
          description: Created the Lead Source Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedLeadSource"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/co-applicants/{co-applicant-id}:
    summary: Remove Sales Lead Co Applicant by co-applicant-id
    get:
      tags:
      - sales-leads co-applicants
      summary: Get Sales Lead Co Applicant by co-applicant-id
      description: Get Sales Lead Co Applicant by co-applicant-id
      operationId: getSalesLeadsCoApplicantsBySalesLeadIdCoApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: co-applicant-id
        in: path
        description: unique identifier for a sales process co-applicant
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetCoApplicantResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads co-applicants
      summary: Update Sales Lead Co Applicant by co-applicant-id
      description: Update Sales Lead Co Applicant by co-applicant-id
      operationId: putSalesLeadsCoApplicantsBySalesLeadIdCoApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: co-applicant-id
        in: path
        description: unique identifier for a sales process co-applicant
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedCoApplicant"
      responses:
        "200":
          description: Updated the CoApplicant successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads co-applicants
      summary: Remove Sales Lead Co Applicant by co-applicant-id
      description: ""
      operationId: deleteSalesLeadsCoApplicantsBySalesLeadIdCoApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: co-applicant-id
        in: path
        description: unique identifier for a sales process co-applicant
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the CoApplicant successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedCoApplicant"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/lead-sources/{lead-source-id}:
    summary: Get Sales Lead Sources by lead-source-id
    get:
      tags:
      - sales-leads lead-sources
      summary: Get Sales Lead Sources by lead-source-id
      description: Get Sales Lead Sources by lead-source-id
      operationId: getSalesLeadsLeadSourcesBySalesLeadIdLeadSourceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: lead-source-id
        in: path
        description: unique identifier for a sales process lead-source-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetLeadSourceResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads lead-sources
      summary: Update Sales Lead Sources by lead-source-id
      description: Update Sales Lead Sources by lead-source-id
      operationId: putSalesLeadsLeadSourcesBySalesLeadIdLeadSourceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: lead-source-id
        in: path
        description: unique identifier for a sales process lead-source-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedLeadSource"
      responses:
        "200":
          description: Updated the Lead Source successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads lead-sources
      summary: Remove Sales Lead Sources by lead-source-id
      description: ""
      operationId: deleteSalesLeadsLeadSourcesBySalesLeadIdLeadSourceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: lead-source-id
        in: path
        description: unique identifier for a sales process lead-source-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Lead Source successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedLeadSource"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quotes:
    summary: Create Sales Lead Quote
    get:
      tags:
      - sales-leads quotes
      summary: List Sales Lead Quote By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead Quote based on the search criteria provided.  If no search criteria is provided, Sales Lead Quote are returned based on default sort order."
      operationId: getSalesLeadsQuotesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllQuotesResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads quotes
      summary: Create Sales Lead Quote
      description: Create Sales Lead Quote
      operationId: postSalesLeadsQuotesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Quote"
      responses:
        "201":
          description: Created the requested Quote Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuote"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/opportunities/{opportunity-id}:
    summary: Remove Sales Lead  Opportunities by opportunity-id
    get:
      tags:
      - sales-leads opportunities
      summary: Get Sales Lead  Opportunities by opportunity-id
      description: Get Sales Lead  Opportunities by opportunity-id
      operationId: getSalesLeadsOpportunitiesBySalesLeadIdOpportunityId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: opportunity-id
        in: path
        description: unique identifier for an opportunity
        required: true
        schema:
          minimum: 0
          type: integer
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetOpportunityResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads opportunities
      summary: Update Sales Lead  Opportunities by opportunity-id
      description: Update Sales Lead  Opportunities by opportunity-id
      operationId: putSalesLeadsOpportunitiesBySalesLeadIdOpportunityId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: opportunity-id
        in: path
        description: unique identifier for an opportunity
        required: true
        schema:
          minimum: 0
          type: integer
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedOpportunity"
      responses:
        "200":
          description: Updated the requested Opportunity successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads opportunities
      summary: Remove Sales Lead  Opportunities by opportunity-id
      description: ""
      operationId: deleteSalesLeadsOpportunitiesBySalesLeadIdOpportunityId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: opportunity-id
        in: path
        description: unique identifier for an opportunity
        required: true
        schema:
          minimum: 0
          type: integer
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Opportunity successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedOpportunity"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-configs/{quote-config-id}:
    summary: Update Sales Lead  Quote Config by quote-config-id
    get:
      tags:
      - sales-leads quote-configs
      summary: Get Sales Lead  Quote Config by quote-config-id
      description: Get Sales Lead  Quote Config by quote-config-id
      operationId: getSalesLeadsQuoteConfigsBySalesLeadIdQuoteConfigId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-id
        in: path
        description: unique identifier for a quote config
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetQuoteConfigResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads quote-configs
      summary: Update Sales Lead  Quote Config by quote-config-id
      description: Update Sales Lead  Quote Config by quote-config-id
      operationId: putSalesLeadsQuoteConfigsBySalesLeadIdQuoteConfigId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-id
        in: path
        description: unique identifier for a quote config
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedQuoteConfig"
      responses:
        "200":
          description: Updated the requested Quote Config successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads quote-configs
      summary: Remove Sales Lead  Quote Config by quote-config-id
      description: ""
      operationId: deleteSalesLeadsQuoteConfigsBySalesLeadIdQuoteConfigId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-id
        in: path
        description: unique identifier for a quote config
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Quote Config successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfig"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quotes/{quote-id}:
    summary: Get Sales Lead Quote by quote-id
    get:
      tags:
      - sales-leads quotes
      summary: Get Sales Lead Quote by quote-id
      description: Get Sales Lead Quote by quote-id
      operationId: getSalesLeadsQuotesBySalesLeadIdQuoteId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-id
        in: path
        description: unique identifier for a sales process quote
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetQuoteResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads quotes
      summary: Update Sales Lead Quote by quote-id
      description: Update Sales Lead Quote by quote-id
      operationId: putSalesLeadsQuotesBySalesLeadIdQuoteId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-id
        in: path
        description: unique identifier for a sales process quote
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedQuote"
      responses:
        "200":
          description: Updated the requested Quote successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads quotes
      summary: Remove Sales Lead Quote by quote-id
      description: ""
      operationId: deleteSalesLeadsQuotesBySalesLeadIdQuoteId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-id
        in: path
        description: unique identifier for a sales process quote
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Quote successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuote"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/applicants/{applicant-id}:
    summary: Remove Sales Lead  Applicant by applicant-id
    get:
      tags:
      - sales-leads applicants
      summary: Get Sales Lead  Applicant by applicant-id
      description: Get Sales Lead  Applicant by applicant-id
      operationId: getSalesLeadsApplicantsBySalesLeadIdApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: applicant-id
        in: path
        description: unique identifier for a sales process applicant
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetApplicantResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads applicants
      summary: Update Sales Lead  Applicant by applicant-id
      description: Update Sales Lead  Applicant by applicant-id
      operationId: putSalesLeadsApplicantsBySalesLeadIdApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: applicant-id
        in: path
        description: unique identifier for a sales process applicant
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedApplicant"
      responses:
        "200":
          description: Updated the Applicant successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedApplicant"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads applicants
      summary: Remove Sales Lead  Applicant by applicant-id
      description: ""
      operationId: deleteSalesLeadsApplicantsBySalesLeadIdApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      - name: applicant-id
        in: path
        description: unique identifier for a sales process applicant
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the Applicant successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedApplicant"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-config-item-hrefs:
    summary: Create Sales Lead  Quote Config Item Reference
    get:
      tags:
      - sales-leads quote-config-item-hrefs
      summary: List Sales Lead  Quote Config Item Reference By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead  Quote Config Item Reference based on the search criteria provided.  If no search criteria is provided, Sales Lead  Quote Config Item Reference are returned based on default sort order."
      operationId: getSalesLeadsQuoteConfigItemHrefsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllQuoteConfigsItemHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads quote-config-item-hrefs
      summary: Create Sales Lead  Quote Config Item Reference
      description: Create Sales Lead  Quote Config Item Reference
      operationId: postSalesLeadsQuoteConfigItemHrefsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuoteConfigItemHref"
      responses:
        "201":
          description: Created the requested QuoteConfigItemHref Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfigItemHref"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/vehicle-preferences/{vehicle-preference-id}:
    summary: Remove Sales Lead Vehicle Preferences by vehicle-preference-id
    get:
      tags:
      - sales-leads vehicle-preferences
      summary: Get Sales Lead Vehicle Preferences by vehicle-preference-id
      description: Get Sales Lead Vehicle Preferences by vehicle-preference-id
      operationId: getSalesLeadsVehiclePreferencesBySalesLeadIdVehiclePreferenceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: vehicle-preference-id
        in: path
        description: unique identifier for a vehicle preference
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetVehiclePreferenceResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads vehicle-preferences
      summary: Update Sales Lead Vehicle Preferences by vehicle-preference-id
      description: Update Sales Lead Vehicle Preferences by vehicle-preference-id
      operationId: putSalesLeadsVehiclePreferencesBySalesLeadIdVehiclePreferenceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: vehicle-preference-id
        in: path
        description: unique identifier for a vehicle preference
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedVehiclePreference"
      responses:
        "200":
          description: Updated the Vehicle Preference successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads vehicle-preferences
      summary: Remove Sales Lead Vehicle Preferences by vehicle-preference-id
      description: ""
      operationId: deleteSalesLeadsVehiclePreferencesBySalesLeadIdVehiclePreferenceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: vehicle-preference-id
        in: path
        description: unique identifier for a vehicle preference
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested vehicle preference successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedVehiclePreference"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/opportunities:
    summary: Create Sales Lead  Opportunities
    get:
      tags:
      - sales-leads opportunities
      summary: List Sales Lead  Opportunities By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead  Opportunities based on the search criteria provided.  If no search criteria is provided, Sales Lead  Opportunities are returned based on default sort order."
      operationId: getSalesLeadsOpportunitiesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllOpportunitiesResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads opportunities
      summary: Create Sales Lead  Opportunities
      description: Create Sales Lead  Opportunities
      operationId: postSalesLeadsOpportunitiesBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Opportunity"
      responses:
        "201":
          description: Created the requested Opportunity Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedOpportunity"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-config-hrefs/{quote-config-href-id}:
    summary: Get Sales Lead  Quote Config Reference by quote-config-href-id
    get:
      tags:
      - sales-leads quote-config-hrefs
      summary: Get Sales Lead  Quote Config Reference by quote-config-href-id
      description: Get Sales Lead  Quote Config Reference by quote-config-href-id
      operationId: getSalesLeadsQuoteConfigHrefsBySalesLeadIdQuoteConfigHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-href-id
        in: path
        description: unique identifier for a sales process quote-config-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetQuoteConfigHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads quote-config-hrefs
      summary: Update Sales Lead  Quote Config Reference by quote-config-href-id
      description: Update Sales Lead  Quote Config Reference by quote-config-href-id
      operationId: putSalesLeadsQuoteConfigHrefsBySalesLeadIdQuoteConfigHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-href-id
        in: path
        description: unique identifier for a sales process quote-config-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedQuoteConfigHref"
      responses:
        "200":
          description: Updated the requested Quote Config Href successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads quote-config-hrefs
      summary: Remove Sales Lead  Quote Config Reference by quote-config-href-id
      description: ""
      operationId: deleteSalesLeadsQuoteConfigHrefsBySalesLeadIdQuoteConfigHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-href-id
        in: path
        description: unique identifier for a sales process quote-config-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Quote Config Href successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfigHref"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-config-item-hrefs/{quote-config-item-href-id}:
    summary: Get Sales Lead  Quote Config Item Reference by quote-config-item-href-id
    get:
      tags:
      - sales-leads quote-config-item-hrefs
      summary: Get Sales Lead  Quote Config Item Reference by quote-config-item-href-id
      description: Get Sales Lead  Quote Config Item Reference by quote-config-item-href-id
      operationId: getSalesLeadsQuoteConfigItemHrefsBySalesLeadIdQuoteConfigItemHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-item-href-id
        in: path
        description: unique identifier for a sales process quote-config-item-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetQuoteConfigItemHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads quote-config-item-hrefs
      summary: Update Sales Lead  Quote Config Item Reference by quote-config-item-href-id
      description: Update Sales Lead  Quote Config Item Reference by quote-config-item-href-id
      operationId: putSalesLeadsQuoteConfigItemHrefsBySalesLeadIdQuoteConfigItemHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-item-href-id
        in: path
        description: unique identifier for a sales process quote-config-item-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedQuoteConfigItemHref"
      responses:
        "200":
          description: Updated the requested Quote Config Item Href successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads quote-config-item-hrefs
      summary: Remove Sales Lead  Quote Config Item Reference by quote-config-item-href-id
      description: ""
      operationId: deleteSalesLeadsQuoteConfigItemHrefsBySalesLeadIdQuoteConfigItemHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-item-href-id
        in: path
        description: unique identifier for a sales process quote-config-item-href-id
        required: true
        schema:
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the requested Quote Config Item Href successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfigItemHref"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}:
    summary: Get Sales Lead by sales-lead-id
    get:
      tags:
      - sales-leads
      summary: Get Sales Lead by sales-lead-id
      description: Get Sales Lead by sales-lead-id
      operationId: getSalesLeadsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetSalesLeadResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - sales-leads
      summary: Update Sales Lead by sales-lead-id
      description: Update Sales Lead by sales-lead-id
      operationId: putSalesLeadsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedSalesLead"
      responses:
        "200":
          description: Updated the Sales Lead successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    delete:
      tags:
      - sales-leads
      summary: Remove Sales Lead by sales-lead-id
      description: ""
      operationId: deleteSalesLeadsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Deleted the Sales Lead successfully.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedSalesLead"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-configs:
    summary: List Sales Lead  Quote Config By Criteria with filtering support to narrow the results
    get:
      tags:
      - sales-leads quote-configs
      summary: List Sales Lead  Quote Config By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead  Quote Config based on the search criteria provided.  If no search criteria is provided, Sales Lead  Quote Config are returned based on default sort order."
      operationId: getSalesLeadsQuoteConfigsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllQuoteConfigsResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads quote-configs
      summary: Create Sales Lead  Quote Config
      description: Create Sales Lead  Quote Config
      operationId: postSalesLeadsQuoteConfigsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuoteConfig"
      responses:
        "201":
          description: Created the requested Quote Config Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfig"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /sales-leads/{sales-lead-id}/quote-config-hrefs:
    summary: List Sales Lead  Quote Config Reference By Criteria with filtering support to narrow the results
    get:
      tags:
      - sales-leads quote-config-hrefs
      summary: List Sales Lead  Quote Config Reference By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Sales Lead  Quote Config Reference based on the search criteria provided.  If no search criteria is provided, Sales Lead  Quote Config Reference are returned based on default sort order."
      operationId: getSalesLeadsQuoteConfigHrefsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllQuoteConfigsHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - sales-leads quote-config-hrefs
      summary: Create Sales Lead  Quote Config Reference
      description: Create Sales Lead  Quote Config Reference
      operationId: postSalesLeadsQuoteConfigHrefsBySalesLeadId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept
        in: header
        description: "Which content types, expressed as MIME types, the client is able to understands"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: sales-lead-id
        in: path
        description: unique identifier for a sales process sales lead
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuoteConfigHref"
      responses:
        "201":
          description: Created the requested QuoteConfigHref Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedQuoteConfigHref"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
