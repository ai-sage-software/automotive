openapi: 3.0.3
info:
  title: Deal API Specification
  description: Sales Process Domain Deal API Specification
  termsOfService: http://swagger.io/terms/
  contact:
    name: Star Standards
    url: https://www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  version: "1.0"
servers:
- url: https://virtserver.swaggerhub.com/star-standard/deals/1.0
  description: SwaggerHub API Auto Mocking
- url: "https://{host}/services/v1"
  variables:
    host:
      enum:
      - dev.appName.dmsDomain.com
      - test.appName.dmsDomain.com
      - qa.appName.dmsDomain.com
      - appName.dmsDomain.com
      default: appName.dmsDomain.com
security:
- AWSAPIGatewayAuthorizer: []
tags:
- name: Associate Assignment
  description: Manage Assignment of Dealer Staff to Sales Process Opportunity
paths:
  /deals/{deal-id}/quote-config-item-hrefs/{quote-config-item-href-id}:
    summary: Get Deals  Quote Config Item Reference by quote-config-item-href-id
    get:
      tags:
      - deals quote-config-item-hrefs
      summary: Get Deals  Quote Config Item Reference by quote-config-item-href-id
      description: Get Deals  Quote Config Item Reference by quote-config-item-href-id
      operationId: getDealsQuoteConfigItemHrefsByDealIdQuoteConfigItemHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-item-href-id
        in: path
        description: unique identifier for a sales process quote-config-item-href-id
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuoteConfigItemHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/co-applicants:
    summary: Create Deal Co Applicant
    get:
      tags:
      - deals co-applicants
      summary: List Deal Co Applicant By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Co Applicant based on the search criteria provided.  If no search criteria is provided, Deal Co Applicant are returned based on default sort order."
      operationId: getDealsCoApplicantsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/CoApplicantListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/applicants:
    summary: Create Deal Applicant
    get:
      tags:
      - deals applicants
      summary: List Deal Applicant By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Applicant based on the search criteria provided.  If no search criteria is provided, Deal Applicant are returned based on default sort order."
      operationId: getDealsApplicantsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/ApplicantListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quotes/{quote-id}:
    summary: Remove Deal Quote by quote-id
    get:
      tags:
      - deals quotes
      summary: Get Deal Quote by quote-id
      description: Get Deal Quote by quote-id
      operationId: getDealsQuotesByDealIdQuoteId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-id
        in: path
        description: unique identifier for a sales process quote
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuoteResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quote-configs/{quote-config-id}:
    summary: Remove Deal Quote Config by quote-config-id
    get:
      tags:
      - deals quote-configs
      summary: Get Deal Quote Config by quote-config-id
      description: Get Deal Quote Config by quote-config-id
      operationId: getDealsQuoteConfigsByDealIdQuoteConfigId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-id
        in: path
        description: unique identifier for a quote config
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuoteConfigResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quotes:
    summary: Create Deal Quote
    get:
      tags:
      - deals quotes
      summary: List Deal Quote By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Quote based on the search criteria provided.  If no search criteria is provided, Deal Quote are returned based on default sort order."
      operationId: getDealsQuotesByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuotesListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}:
    summary: Remove Deal by deal-id
    get:
      tags:
      - deals
      summary: Get Deal by deal-id
      description: Get Deal by deal-id
      operationId: getDealsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetDealResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    put:
      tags:
      - deals
      summary: Update Deal by deal-id
      description: Update Deal by deal-id
      operationId: putDealsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/savedOrUpdatedDeal"
      responses:
        "200":
          description: Updated the Deal successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedDeal"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/lead-sources/{lead-source-id}:
    summary: Update Deal Lead Sources by lead-source-id
    get:
      tags:
      - deals lead-sources
      summary: Get Deal Lead Sources by lead-source-id
      description: Get Deal Lead Sources by lead-source-id
      operationId: getDealsLeadSourcesByDealIdLeadSourceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: lead-source-id
        in: path
        description: unique identifier for a sales process lead-source-id
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealLeadSourceResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/co-applicants/{co-applicant-id}:
    summary: Remove Deal Co Applicant by co-applicant-id
    get:
      tags:
      - deals co-applicants
      summary: Get Deal Co Applicant by co-applicant-id
      description: Get Deal Co Applicant by co-applicant-id
      operationId: getDealsCoApplicantsByDealIdCoApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      - name: co-applicant-id
        in: path
        description: unique identifier for a sales process co-applicant
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/CoApplicantResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals:
    summary: List Deal By Criteria with filtering support to narrow the results
    get:
      tags:
      - deals
      summary: List Deal By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal based on the search criteria provided.  If no search criteria is provided, Deal are returned based on default sort order."
      operationId: getDeals
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: startDate
        in: query
        description: The start date from which the available deals are requested. If the start date is not provided the current date will be defaulted.
        required: false
        style: form
        explode: true
        schema:
          maxLength: 10
          minLength: 0
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
          type: string
          description: Start date
          format: date
        example: 2024-11-05
      - name: endDate
        in: query
        description: The end date upto which the available deals are requested. If the endDate is not provided the deals from the current date up to 14 days will be provided in the service response.
        required: false
        style: form
        explode: true
        schema:
          maxLength: 10
          minLength: 0
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
          type: string
          description: End date
          format: date
        example: 2024-11-26
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      responses:
        "200":
          $ref: "#/components/responses/GetAllDealsResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
    post:
      tags:
      - deals
      summary: Create the Deal
      description: API to create the Deal
      operationId: createDeal
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Deal"
      responses:
        "201":
          description: Created the Deal Successfully
          headers:
            Access-Control-Allow-Origin:
              $ref: "#/components/headers/Access-Control-Allow-Origin"
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/savedOrUpdatedDeal"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quote-config-item-hrefs:
    summary: List Deals  Quote Config Item Reference By Criteria with filtering support to narrow the results
    get:
      tags:
      - deals quote-config-item-hrefs
      summary: List Deals  Quote Config Item Reference By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deals  Quote Config Item Reference based on the search criteria provided.  If no search criteria is provided, Deals  Quote Config Item Reference are returned based on default sort order."
      operationId: getDealsQuoteConfigItemHrefsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuoteConfigItemHrefListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/applicants/{applicant-id}:
    summary: Remove Deal Applicant by applicant-id
    get:
      tags:
      - deals applicants
      summary: Get Deal Applicant by applicant-id
      description: Get Deal Applicant by applicant-id
      operationId: getDealsApplicantsByDealIdApplicantId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: applicant-id
        in: path
        description: unique identifier for a sales process applicant
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/ApplicantResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quote-config-hrefs/{quote-config-href-id}:
    summary: Get Deals Quote Config Reference by quote-config-href-id
    get:
      tags:
      - deals quote-config-hrefs
      summary: Get Deals Quote Config Reference by quote-config-href-id
      description: Get Deals Quote Config Reference by quote-config-href-id
      operationId: getDealsQuoteConfigHrefsByDealIdQuoteConfigHrefId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: quote-config-href-id
        in: path
        description: unique identifier for a sales process quote-config-href-id
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealQuoteConfigHrefResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/vehicle-preferences:
    summary: List Deal Vehicle Preferences By Criteria with filtering support to narrow the results
    get:
      tags:
      - deals vehicle-preferences
      summary: List Deal Vehicle Preferences By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Vehicle Preferences based on the search criteria provided.  If no search criteria is provided, Deal Vehicle Preferences are returned based on default sort order."
      operationId: getDealsVehiclePreferencesByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealVehiclePreferencesListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/vehicle-preferences/{vehicle-preference-id}:
    summary: Update Deal Vehicle Preferences by vehicle-preference-id
    get:
      tags:
      - deals vehicle-preferences
      summary: Get Deal Vehicle Preferences by vehicle-preference-id
      description: Get Deal Vehicle Preferences by vehicle-preference-id
      operationId: getDealsVehiclePreferencesByDealIdVehiclePreferenceId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: vehicle-preference-id
        in: path
        description: unique identifier for a vehicle preference
        required: true
        schema:
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealVehiclePreferenceResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/lead-sources:
    summary: List Deal Lead Sources By Criteria with filtering support to narrow the results
    get:
      tags:
      - deals lead-sources
      summary: List Deal Lead Sources By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Lead Sources based on the search criteria provided.  If no search criteria is provided, Deal Lead Sources are returned based on default sort order."
      operationId: getDealsLeadSourcesByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealLeadSourcesListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quote-configs:
    summary: List Deal Quote Config By Criteria with filtering support to narrow the results
    get:
      tags:
      - deals quote-configs
      summary: List Deal Quote Config By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deal Quote Config based on the search criteria provided.  If no search criteria is provided, Deal Quote Config are returned based on default sort order."
      operationId: getDealsQuoteConfigsByDealId
      parameters:
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/QuoteConfigsListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
  /deals/{deal-id}/quote-config-hrefs:
    summary: List Deals Quote Config Reference
    get:
      tags:
      - deals quote-config-hrefs
      summary: List Deals Quote Config Reference By Criteria with filtering support to narrow the results
      description: "Provides a paginated list of Deals Quote Config Reference based on the search criteria provided.  If no search criteria is provided, Deals Quote Config Reference are returned based on default sort order."
      operationId: getDealsQuoteConfigHrefsByDealId
      parameters:
      - name: api-version
        in: header
        description: API Version
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: API Version
          example: 1.0.0
          enum:
          - 1.0.0
          - 1.0.1
          - 2.0.0
          - 2.0.1
        example: 2.0.0
      - name: countrycode
        in: header
        description: Country Code
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Country Code
          example: "1"
      - name: locale
        in: header
        description: Locale
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Locale
          example: en-US
      - name: requestId
        in: header
        description: Request ID/ Transaction ID Header Parameter (UUID version 4)
        required: true
        style: simple
        explode: false
        schema:
          type: string
          format: uuid
        example: 16a7d408-aed4-43f8-8af4-7a17f407f799
      - name: brand
        in: header
        description: Brand
        required: true
        style: simple
        explode: false
        schema:
          type: string
          description: Vehicle Brand (default value - 'toyota')
          example: toyota
          enum:
          - toyota
          - lexus
        example: toyota
      - name: dealerId
        in: header
        description: Dealer Identifier
        required: true
        style: simple
        explode: false
        schema:
          maxLength: 5
          minLength: 5
          type: string
        example: "42278"
      - name: Accept-Language
        in: header
        description: "advertises which languages the client is able to understand (ex: en-US,en;fr-CH)"
        schema:
          minimum: 0
          type: string
      - name: Accept-Encoding
        in: header
        description: "The encoding algorithm, usually a compression algorithm, that can be used on the resource sent back"
        schema:
          minimum: 0
          type: string
      - name: Accept-Charset
        in: header
        description: Which character encodings the client understands
        schema:
          minimum: 0
          type: string
      - name: deal-id
        in: path
        description: unique identifier for a sales process deal
        required: true
        schema:
          type: string
      responses:
        "200":
          $ref: "#/components/responses/DealQuoteConfigHrefsListResponse"
        "400":
          description: |
            Bad Request, server cannot or will not process the request.
            See more details in the Error message
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "401":
          description: |
            The request requires user authentication. The response MUST include a WWW-Authenticate header field containing a challenge applicable to the requested resource.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "404":
          description: |
            The requested resource was not found.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "500":
          description: |
            The server encountered an unexpected condition which prevented it from fulfilling the request.
          headers:
            RequestId:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Errors"
        "503":
          description: downstream service error
